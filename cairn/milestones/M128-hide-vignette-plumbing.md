# M128: Vignettes show the package calls they teach, not the plumbing around them

- **Status:** review
- **Priority:** normal
- **Depends on:** M126
- **Driving RR:** —
- **Principles touched:** GP4, GP5
- **Resolves:** —
- **Surface tier:** user-facing — changes the code shown in nine shipped vignettes and adds three exported datasets
- **Branch/PR:** m128-hide-vignette-plumbing

## Goal

A vignette reader sees the circumplex calls and model-fitting calls being taught, while the code that simulates data, assembles or formats display tables, or checks internals is hidden, and simulated data is loaded from the package by name.

## Scope

**In:** the nine vignette sources: every `vignettes/*.Rmd.orig`, plus `bayesian-ssm-analysis.Rmd` and `using-instruments.Rmd`, which have no `.orig`. The chunks AC1's search flags at 73218afa: structure `variants` and `ipsatize`; growth `simulate`, `stack`, `draws`, `lowamp` and `lowamp-plot`; SEM `latent-table`; Bayesian `known-direction`, `data-prep`, `load-draws` and `induced-prior`; the using-instruments norm-sample count chunk; advanced-visualization `coord-built`, `occasions-data`, `individuals` and `curve-axis`. A flagged step is hidden, with no exceptions, and the prose says what it does. Hiding works per statement, not per chunk. A chunk that mixes a taught call with plumbing is split, so that the taught call stays shown. Three datasets from `data-raw/` under D-058: `simulated_occasions` from one script, and `simulated_growth` and `simulated_growth_origin` from one script that shares their person effects. A table made from `ssm_table()` shows the call as code and hides the `kable()` formatting. The growth vignette shows one `ssm_draws()` call for a single wave, hides the loop that builds the trajectory table, and shows the printed table and the plots. NEWS.md.

**Out:** SEM table width, doc claims (ii) to (v), the search for dashes in output, and the prose that reads M127's new prints → M129. An exported draws-to-trajectory helper → its candidate row. kable to gt → its candidate row. `vignettes/bayesian_ssm_draws.rds` stays a file, because it holds model output rather than simulated data. Only the chunk that reads it is hidden.

## Acceptance criteria

- [x] AC1: An echoed chunk is a fenced code block in a source whose header has no `echo = FALSE`, `echo = F`, `include = FALSE` or `#| echo: false`, in any spacing. `eval = FALSE` chunks and plain `r` fences count as echoed. The sources are every `vignettes/*.Rmd.orig` and every `vignettes/*.Rmd` with no `.Rmd.orig` of the same name. No line of an echoed chunk matches this extended regular expression: `lapply\(|sapply\(|vapply\(|mapply\(|Map\(|do\.call\(|Filter\(|Reduce\(|t?apply\(|stopifnot\(|stop\(|identical\(|all\.equal\(|kable|(^|[^A-Za-z0-9_.])r(norm|unif|binom|pois|exp|gamma|beta|chisq|t)\(|sample\(|mvrnorm|function ?\(|\\\(|for ?\(|rbind\(|reshape\(|subset\(|data\.frame\(|rownames\(|readRDS\(`. `tools/vignette-echo-sweep.R` runs this search, and on its fixture sources it prints exactly the expected lines, one for each hidden-marker form, each echoed form and each search alternative.
- [x] AC2: At the branch cut (the SHA T1 records), take each echoed statement whose top-level call, or the call on the right of its assignment, is a function exported by circumplex (`getNamespaceExports("circumplex")`), a `glmmTMB::` function, or `brm()` or `set_prior()`. Each such statement stays echoed in the same source on the branch head, calling the same function with the same argument names. The exception is a statement that sits inside a statement that simulates data, builds a display table or checks internals, and the T2 call list gives each such exception its reason.
- [x] AC3: `data/` holds `simulated_occasions`, `simulated_growth` and `simulated_growth_origin`. `data-raw/simulated_occasions.R` writes the first. `data-raw/simulated_growth.R` writes the other two, drawing the person effects once and using them for both. Each script sets its seed, and running it again writes objects `identical()` to the shipped ones. `simulated_occasions` has columns `id`, `wave` and the eight `PANO()` scales for 200 persons at waves T1, T2 and T3, with `wave` a factor in that level order. Its group profile is a cosine of amplitude 0.6 at displacements 330, 355 and 20 degrees, with a person offset of SD 0.5 and noise of SD 0.5. Both growth datasets have columns `person`, `wave` and the eight `PANO()` scales for 150 persons at waves 0 to 4. They have elevation 0.5, person effects of SD 0.30 (elevation) and 0.15 (x and y), and noise of SD 0.40. In `simulated_growth` the group (x, y) moves linearly from the amplitude-0.6 point at 350 degrees to the one at 10 degrees. In `simulated_growth_origin` the group x moves linearly from 0.5 to -0.5 with y at 0.02. Each dataset has a help page that says the data are simulated, states these parameters with "degrees" spelled out, and links its script on GitHub. Each has a `_pkgdown.yml` reference row, and NEWS.md announces the three datasets.
- [x] AC4: `advanced-visualization.Rmd.orig` and `growth-ssm-analysis.Rmd.orig` load their simulated data with `data()` on AC3's datasets. Each line that `grep -nE 'ssm_analyze_long\(|ssm_parameters_id\(|glmmTMB::glmmTMB\('` finds inside an R chunk of the two files takes data built from those datasets. Each `ssm_parameters_id()` call on growth data returns one row per person-wave, as many rows as its input dataset has. `grep -nE '(^|[^A-Za-z0-9_.])r(norm|unif|binom)\('` over the two files returns lines only inside hidden growth chunks that draw coefficient samples from a fitted model.
- [x] AC5: Take each chunk with `echo = FALSE` in which `kable()` formats `ssm_table(X, ...)`. A code block placed before that chunk shows `ssm_table(X)`, either as an `eval = FALSE` chunk or as a plain `r` fence.
- [x] AC6: Each rendered precomputed `.Rmd` has as many `<img src="figures/` lines as at the branch cut. For `bayesian-ssm-analysis` and `using-instruments`, the HTML from `pkgdown::build_article()` has as many `<img` tags as at the branch cut. These printed tables are still shown with the same column or row labels: the structure `variants` fit table and `ipsatize` comparison table, the SEM latent table, the occasions results table, and the growth trajectory table. The growth `mid` print still shows its certification note and its amplitude and displacement intervals. The using-instruments sentence still shows its six norm-sample counts. In the re-knitted growth output, every wave of the first trajectory is certified, and wave 2 of the origin trajectory is not.
- [x] AC7: In the `tools/prose-sweep.R --prose` output of `advanced-visualization.Rmd.orig` and `growth-ssm-analysis.Rmd.orig`, each sentence about the data, a printed output or a figure is true of the re-knitted output. Take each other source whose `#>` lines or `<img` count differ between the branch-cut build (T1) and the head build. Each sentence in it that holds a number, a code span or a quoted label and names a printed value, column or label names one the re-knitted output shows. Each name that a hidden chunk assigns and a later echoed chunk uses (`all.names()` of its parsed code) is named in the prose before that echoed chunk. Each function that echoed code calls from a package attached only in a hidden chunk follows prose that names that package.
- [ ] AC8: `tools/prose-sweep.R` exits 0 on each `vignettes/*.Rmd` and `vignettes/*.Rmd.orig` file that `git diff --name-only 66f8c2ed...HEAD` lists, other than `vignettes/using-instruments.Rmd`. On that file, the head run exits 0 or 1, and its standard output is compared with the standard output of `git show 66f8c2ed:vignettes/using-instruments.Rmd | Rscript tools/prose-sweep.R -`, after removing each line's `<label>:<line>: ` prefix. Counting repeats, every head finding appears at the cut at least as many times. `tools/check-vignette-staleness.R` passes after `tools/precompute-vignettes.R`, and the `vignette-precompute` CI job passes on the PR. `devtools::test()`, `devtools::check(args = "--no-manual")` and `devtools::check(manual = TRUE)` report no failure, warning or note that is new relative to master.

## Coverage

- AC1 → T2, T4, T5, T6
- AC2 → T2, T4, T5, T6
- AC3 → T3
- AC4 → T4, T5
- AC5 → T6
- AC6 → T7
- AC7 → T1, T2, T4, T5, T6, T7
- AC8 → T1, T8

## Tasks

- [x] T1: Cut the branch from master after M126 merges, and record the branch-cut SHA in the work log. Build the `bayesian-ssm-analysis` and `using-instruments` articles with pkgdown at that SHA as the baseline for AC6 and AC7. Run `devtools::install()` so the knit reads the branch's package (lesson M21 family).
- [x] T2: Write `tools/vignette-echo-sweep.R` with AC1's search, a mode that lists AC2's taught statements per source with a reason for each exception, and a mode that lists AC7's hidden-chunk names used by echoed chunks. Give it fixture sources under `tools/` with one chunk per hidden-marker form and per echoed form (plain `r` fence, tilde fence, `eval = FALSE`), and one line per search alternative, and assert its exact output on them. Run it on the branch-cut sources and save both lists. Also list each remaining echoed line that calls no circumplex, glmmTMB, brms or ggplot2 function, such as `round()` or `head()`, so review can judge whether it is plumbing.
- [x] T3: Write the two `data-raw/` scripts from the current simulation code, the help pages beside `simulated_items` in `R/example_data.R`, the `_pkgdown.yml` rows and the NEWS.md entry. Rerun each script and compare with `identical()`. Run `devtools::document()` and revert any `Config/roxygen2/version` change (lesson M85).
- [x] T4: advanced-visualization: load `simulated_occasions`, and hide the `subset()` table code, the `individuals` filter and the `curve-axis` data frame behind prose. Move `library(ggplot2)` into an echoed chunk if echoed code uses it.
- [x] T5: growth Sections 2 to 5: load both datasets, show `ssm_parameters_id()` on the octant scores, hide the reshape and the draws loop, and show one `ssm_draws()` call for one wave. Describe each hidden step and every name it defines (lesson M50).
- [x] T6: structure `variants` and `ipsatize`, SEM `latent-table`, the four Bayesian chunks and the using-instruments count chunk: keep the package calls shown, hide table assembly, checks and data building, and add the `ssm_table()` code blocks that AC5 needs.
- [x] T7: Re-knit every precomputed vignette with `tools/precompute-vignettes.R`, and build the two knit-at-build articles with pkgdown. Count figures, and check the named outputs and the certification verdicts. Read the prose of the sources AC7 names against the new output, fix what no longer matches, and record one work-log line per vignette.
- [x] T8: Run `tools/prose-sweep.R`, `tools/check-vignette-staleness.R`, `devtools::test()`, `devtools::check(args = "--no-manual")` and `devtools::check(manual = TRUE)`. Add a NEWS.md documentation entry for the vignette changes.

## Work log

- 2026-09-14: created by /milestone-plan. The plan gate kept kable, with a hidden render chunk and no-break cells, over switching to gt now, because gt needs a dependency gate. Falsified by a no-break kable table that still wraps or looks wrong in the pkgdown article.
- 2026-09-14: the plan gate simplified the growth vignette without new API over adding an exported draws-to-trajectory helper, because a helper is an irreversible API decision. Falsified by a rewrite that cannot reach AC3's half-length bar without one.
- 2026-09-14: the plan gate absorbed doc bugs (ii) to (v) of the doc-bug candidate row instead of routing them to /hotfix, because this milestone rewrites both pages.
- 2026-09-14: criteria audit (full mode, fresh [O] reader) found 7 items on this file's draft. All were fixed before writing: AC1 bounds the whole chunk, AC2 names a pkgdown build and viewport, AC3 and AC7 name the Bayesian `.Rmd`, AC4 is enumerated by git diff, AC5 lists sites and code referents, and AC6 exempts the placeholder and depends on M127.
- 2026-09-14: second fresh [O] audit of the written criteria found 5 items, all fixed. AC3 was unreachable for bayesian alone (at least 22 of 31 lines stay), so it now counts both vignettes together, gives bayesian a per-chunk bar and defines an echoed line. AC4 names the `--prose` output as its list. AC5 states the four claims, adds "weak" to its grep and corrects only wrong uses. AC6 matches a dash at a wrapped line end.
- 2026-09-14: re-cut by /milestone-plan on maintainer feedback. Vignettes must hide data simulation, table assembly and check code, and load simulated data by name. The earlier criteria, which kept that code shown but shorter, are superseded. Their doc claims, SEM table width, output dash search and M127 prose move to M129. Re-cut count on this milestone: 1, with no review returns.
- 2026-09-14: plan gate chose package datasets over `.rds` files in `vignettes/`, because readers can then load the data by name (D-058). Falsified by a CRAN size note from `data/` or a request to remove the datasets.
- 2026-09-14: plan gate chose to hide every flagged chunk, the Bayesian prior-predictive simulation included, over keeping named teaching exceptions. Falsified by a reader who cannot follow a hidden step from the prose.
- 2026-09-14: plan gate chose one shown `ssm_draws()` call with the loop hidden over hiding the whole step or exporting a helper. Falsified by a reader who cannot rebuild the trajectory table from the shown call and the prose.
- 2026-09-14: the plan showed the `ssm_table()` call with `kable()` hidden, over hiding the whole table code, without posing it (question cap), because the earlier gate chose that form for the SEM table. Falsified by a request to hide the calls too.
- 2026-09-14: criteria audit (full mode, fresh [O] reader) found 17 items across the M128 and M129 drafts. Twelve were fixed before writing: the call-kept check (AC2), `ssm_parameters_id()` averaging across waves (AC4), shared growth person effects and certification outcomes (AC3, AC6), hidden-marker forms moved to T2 fixtures, the `ssm_table()` wording (AC5), branch-cut baselines, named outputs with no review escape (AC6), a procedure for hidden names (AC7), phrase anchors, number coverage and exact exemptions in M129, and small items (GitHub link, factor waves, grep patterns). Four became gate questions, and the fifth was settled by the earlier SEM choice.
- 2026-09-14: checkpoint: criteria written, and a second fresh [O] audit of the written wording is running. The plan is not final until its findings are disposed of.
- 2026-09-14: second fresh [O] audit (full mode) of the written M128 and M129 criteria found 16 items, all fixed. AC2 now works per statement with reasoned exceptions and covers `brm()` and `set_prior()`. AC1 gains check and table tokens, and its fixture assertion. AC3 states the full generating parameters, column names and shared person effects. AC4's greps are limited to R chunks and to coefficient-draw chunks. AC6 names what `mid` and the using-instruments sentence must show. AC7 uses a branch-cut build and a package-attach clause. AC8 adds the PDF manual check. Coverage is widened. M127 now re-knits its own output, and M129's baseline is fixed at 73218afa. The fix for item 16 was none, because that item reported no defect. Growth numbers all change with the new data, and AC7 carries that.
- 2026-09-15: implement gate chose one shared help page for the two growth datasets, three written-out `cpm_fit()` calls in the structure `variants` chunk, and shown `coord$person` and `coord$wave` assignments after `ssm_parameters_id()` in the growth vignette.
- 2026-09-15: T1 done. Branch cut at 66f8c2ed. Baseline pkgdown builds at that SHA: `bayesian-ssm-analysis` has 2 `<img` tags and `using-instruments` has 1, and its sentence reads 23 samples across 15 instruments. The branch-cut precomputed `.Rmd` files are `git show 66f8c2ed:vignettes/<name>.Rmd`. `devtools::install()` ran.
- 2026-09-15: T2 done. `tools/vignette-echo-sweep.R` passes its self-test (44 lines), and three planted defects turned it red. Branch-cut lists are in `devel/m128-echo-sweep/`. The search finds 61 lines. `--taught` finds 86 statements, 7 nested, and 4 of them have reasons in `taught-exceptions.txt`. `devtools::test()`: 0 failures.
- 2026-09-15: T3 done. The two scripts ran twice, and all three objects were `identical()`. Wave means recover the stated amplitudes and displacements. `document()` left DESCRIPTION unchanged, and `check_pkgdown()` found no problems. `devtools::test()`: 0 failures.
- 2026-09-15: T4 done. advanced-visualization loads `simulated_occasions`, and `library(ggplot2)` is in the shown setup chunk. The column selections, the `NA` filter and the `curve` data frame are hidden behind prose. The plot chunks keep their labels, so figure files keep their names. The path prose now gives the estimated angles (about 332, 355 and 21 degrees).
- 2026-09-15: T5 done. growth loads both datasets and shows `ssm_parameters_id()` on them (750 rows each), one `ssm_draws()` call at wave 2, both `glmmTMB()` fits and `mid`. The reshapes, both draw steps and both trajectory loops are hidden, and prose names `long`, `B`, `trajectory`, `long2`, `B2` and `trajectory2`. Every wave of the first trajectory is certified, and wave 2 of the origin trajectory is not. The prose on displacement estimates now gives the printed values.
- 2026-09-15: T6 done. The structure vignette shows three `cpm_fit()` calls and hides both table builds. SEM shows `ssm_table(latent)` in a plain fence. Bayesian hides the two `stopifnot()` checks, the data build, the `readRDS()` call and the prior simulation. using-instruments hides the count chunk. All nine kable sites pass the AC5 check. Printed output of the two knit-at-build articles is unchanged.
- 2026-09-15: T7: all seven precomputed vignettes re-knit with no diff, and figure counts match the branch cut (3, 2, 13, 11, 10, 2, 0). The staleness check passes.
- 2026-09-15: T7 structure: 2 changed `#>` lines, both the masked elapsed time. The `variants` and `ipsatize` tables are unchanged, and no prose change was needed.
- 2026-09-15: T7 SEM: no `#>` change, and the latent table is unchanged.
- 2026-09-15: T7 advanced-visualization: 6 changed `#>` lines, all in the occasions table, whose labels are unchanged. The path prose was fixed to the estimated angles.
- 2026-09-15: T7 growth: 42 changed `#>` lines. The trajectory table keeps its columns, `mid` keeps its note and intervals, and the verdicts match AC6. Two sentences were fixed to match the output.
- 2026-09-15: T7 bayesian and using-instruments: pkgdown HTML output and `<img` counts match the branch cut, and the six counts read 23, 15 and so on as before.
- 2026-09-15: T7 AC7 checks: every `--hidden-names` hit is named in prose before its use. A package-attach scan finds nothing at head, and it flags ggplot2 in the branch-cut advanced-visualization.
- 2026-09-15: amendment gate: AC8 as written fails on `using-instruments.Rmd`, whose 38 prose-sweep findings were there at the branch cut. The user chose to narrow AC8 over rewriting that vignette's prose.
- re-audit: AC8 (full) — three wording gaps: touched fixture `.Rmd` files, the `<stdin>` label, and repeated findings. Fixed wording proposed. The amended state holds at head.
- re-audit: AC8 (full) — one gap: the head run's exit status is unbounded, and the cut run's input form is unstated. Replacement second sentence proposed. The other questions returned nothing.
- 2026-09-15: amendment: AC8 narrowed at the user's selection to the second reader's wording. using-instruments.Rmd must add no prose-sweep finding over the branch cut, and every other touched vignette file must exit 0. This is the second `re-audit: AC8` line, so any further AC8 change goes to the user.
- claim audit: 84 claims read, 4 corrected — tools/vignette-echo-sweep.R
- 2026-09-15: T8 done. The prose sweep exits 0 on the 9 touched vignette files other than using-instruments. That file's head run gives the same 38 findings as the branch cut, with none new. The staleness check passes. `devtools::test()`: 0 failures, 9512 passes. On the branch, both `check(args = "--no-manual")` and `check(manual = TRUE)` give Status OK, and the PDF manual builds. Master at 66f8c2ed, checked from a worktree, gave 1 NOTE in both runs, the worktree's `.git` file. No result is new relative to master. NEWS has a documentation entry. The `vignette-precompute` CI job runs on the PR at review.
- 2026-09-15: status set to review.
- 2026-09-15: /milestone-review started. No PR exists for the branch, and origin/master is still the branch cut 66f8c2ed, so no sync merge was needed.
- 2026-09-15: review checkpoint, half done. AC1 to AC7 are ticked against recorded evidence. AC8 waits on both package checks, a reinstall and re-knit, and the PR's `vignette-precompute` job. The findings are logged, and their dispositions wait for the merge gate.

## Decisions

## Review

Evidence gathered 2026-09-15 at head ff71c184, fresh runs only.

- AC1: `Rscript tools/vignette-echo-sweep.R` prints nothing and exits 0 over the 9 default sources. `--self-test` passes (44 expected lines from 2 fixture sources). A separate Python script copies the criterion's regex verbatim and parses fences and hidden markers itself. Over `git show HEAD:` of the same 9 sources, it reads 111 echoed chunks and finds 0 matching lines.
- AC2: `--taught` rerun on the 9 sources from `git show 66f8c2ed:` gives 86 statements, 7 nested, and matches the saved `devel/m128-echo-sweep/branch-cut-taught.txt` line for line. `--compare-taught` against that list and `taught-exceptions.txt` prints nothing and exits 0 at head. The 4 exceptions are nested statements with reasons. Two are `colnames(scores) <- PANO()` inside the growth simulation loops, now in `data-raw/simulated_growth.R`. Two are `ssm_draws()` inside the hidden trajectory loops, and a shown single-wave `ssm_draws()` call stands for each.
- AC3: both `data-raw/` scripts, rerun in a scratch package copy, write objects `identical()` to the three shipped `.rda` files. Read by line, each script sets its seed, and `simulated_growth.R` draws `u_e`, `v_x` and `v_y` once, before both datasets. The code uses the stated parameters: amplitude 0.6 at 330, 355 and 20 degrees, SD 0.5 offset and SD 0.5 noise. For growth, it uses elevation 0.5 and SDs 0.30, 0.15 and 0.40. The growth path runs from 350 to 10 degrees, and the origin path moves x from 0.5 to -0.5 with y at 0.02. `simulated_occasions` has columns `id`, `wave`, PA to NO, 200 persons and 600 rows, and `wave` is a factor with levels T1, T2, T3. Its wave means give displacements 332.4, 354.8 and 21.1. Both growth datasets have columns `person`, `wave`, PA to NO, 150 persons, waves 0 to 4 and 750 rows. The person means of the two growth datasets correlate at 0.961. Both help pages say the data are simulated, state the parameters with "degrees" spelled out, and link the script on GitHub. `_pkgdown.yml` has the three rows, `pkgdown::check_pkgdown()` finds no problems, and NEWS.md announces the datasets.
- AC4: at HEAD, `advanced-visualization.Rmd.orig` calls `data("simulated_occasions")`, and `growth-ssm-analysis.Rmd.orig` calls `data("simulated_growth")` and `data("simulated_growth_origin")`. The criterion's first grep finds 5 lines inside R chunks, and each takes data built from those datasets. Line 320 `ssm_analyze_long()` takes `simulated_occasions`. Lines 81 and 286 `ssm_parameters_id()` take the two growth datasets. Lines 121 and 308 `glmmTMB()` take `long` and `long2`, which the hidden chunks build from `coord` and `coord2`. The other grep hits are prose lines. `ssm_parameters_id(<dataset>, scales = PANO())` returns 750 rows for each growth dataset, the same as its input. The random-draw grep finds one line, line 180 `rnorm()`, inside the hidden `draws` chunk that draws coefficient vectors.
- AC5: the Python script finds 9 `echo = FALSE` chunks in which `kable()` formats `ssm_table(X, ...)`. Six are in intermediate, two in introduction and one in SEM (`latent`). For each, the code block just before it is shown and holds `ssm_table(X)` with the same `X`. All nine of those blocks are plain `r` fences.
- AC6: each rendered precomputed `.Rmd` has the same count of `<img src="figures/` lines at the cut and at HEAD. The counts are 13 (advanced-visualization), 0 (axes-reliability), 3 (structure), 2 (growth), 11 (intermediate), 10 (introduction) and 2 (SEM). Fresh `pkgdown::build_article()` HTML has 2 `<img` tags for bayesian and 1 for using-instruments, as in the T1 baseline. The structure `#>` lines differ from the cut in one line, the elapsed time, so the `variants` and `ipsatize` tables are unchanged. The SEM `#>` lines do not differ, so the latent table is unchanged. The occasions table header reads `Occasion a_est d_est d_lci d_uci` at the cut and at HEAD. The growth trajectory header reads `wave a_est a_lci a_uci d_est d_lci d_uci certified` at both. The growth `mid` print shows its note and the amplitude interval 0.005 to 0.049 and displacement interval 322.335 to 214.098. The built using-instruments sentence reads 23 samples, 15 instruments, 10 college, 6 under 300, 6 standardization and 2 unsourced. All five waves of `trajectory` print `certified` TRUE. The `mid` note fires, and `mid` is the same wave-2 `ssm_draws()` computation on `B2` that fills row 3 of `trajectory2`.
- AC7: I read every `--prose` sentence of the two files at HEAD (199 growth, 180 visualization) against the rendered HEAD output. The sentences about data, output or figures hold. Displacement drifts from 350 to 10 degrees, and the trajectory table's `d_est` runs 350.30, 356.33, 2.46, 8.55, 14.48. Amplitude stays near 0.6, at 0.61 to 0.62. The `coord` print's `id` column is the row number. The `mid` displacement interval, 322.3 to 214.1, spans more than half the circle. Every wave of the first trajectory is certified. The low-amplitude figure draws wave 2 hollow, and its amplitude band reaches toward zero there. The occasions table gives 332.4, 354.8 and 21.1 degrees, a 26.2-degree step, and the path figure draws the short arc across LM. Among other sources, only structure changes a `#>` line, the elapsed time, and none of its sentences names it. The Bayesian and using-instruments code lines are the same multiset at the cut and at HEAD. `--hidden-names` lists 10 names: `people`, `curve`, `dat`, `draws`, `long`, `B`, `trajectory`, `long2`, `B2` and `trajectory2`. The prose before each use names it. A scan of 8 sources finds no shown call into a package that only a hidden chunk attaches.

### Consistency gate

- `cairn_validate.py` exits 0. Its one advisory is the sizing tripwire: 8 criteria, over 7.
- `document()` with `cli.width = 500` leaves no diff and prints no `resolve link` line.
- `pkgdown::check_pkgdown()` finds no problems. README.Rmd and README.md are not in the diff.
- NEWS.md has the dataset entry and the vignette entry, with no milestone numbers.
- No new top-level file. `devel/`, `tools/` and `data-raw/` already exist.
- Master watches: the newest push runs of `R-CMD-check.yaml` and `test-coverage.yaml` on master reached `success` at 66f8c2ed.
- `tools/check-master-red-alert.R`, `tools/master-red-alert-dryrun.R` and `tools/check-branch-protection.R` all exit 0.

### Independent review

Three fresh reviewers ran: [O] diff-bug, [S] blame-history and [S] prior-review. Each ranked its own findings.

- O1: `tools/vignette-echo-sweep.R` hides a chunk that has a `#| echo: false` line anywhere in it, but knitr reads `#|` options only at the top. If a shown chunk has a later `#|` line, the search skips it. No vignette has a `#|` line.
- O2: the hidden-header regex also matches `echo = FALSE` inside a quoted option value, such as a `fig.cap` string. No vignette header has one.
- O3: the test file covers only `--self-test`. `--taught`, `--compare-taught` and `--hidden-names` have no fixture or assertion.
- O4: the fixtures lack some hidden-marker spacings (spaced `echo = F`, tight `echo=FALSE`, `#|` spacing) and a near miss such as `echo = FALSEY`. The four apply alternatives are also covered by `t?apply\(`.
- O5: NEWS.md says the growth vignette shows "one `ssm_draws()` call for a single wave", but it shows two single-wave calls, `wave2` and `mid`.
- O6: the shown `ssm_table(latent)` in the SEM vignette is a plain fence that is never run, so a signature change does not fail the knit.
- O7: the `simulated_occasions` help page does not say that the group elevation is 0.
- O8: the header rule of `data-raw/simulated_growth.R` ends in ` -` (cosmetic).
- S1 (blame): the Bayesian `atan2` argument-order `stopifnot()` checks, shown on purpose by an earlier plain-English pass, are now hidden behind a sentence. The check still runs.
- S2 (blame): `waves <- 0:4` is now set in the hidden `trajectory` chunk and reused by the hidden `lowamp-trajectory` chunk.
- P1-P3 (prior review): shown Bayesian chunks use `dat` (`data-head`, `brms-fit`) and `draws` (`draws-shape`), which hidden chunks define. This resembles M50 finding F2, but here the prose names each object.
