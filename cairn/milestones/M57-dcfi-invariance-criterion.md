# M57: ΔCFI secondary invariance criterion for `ssm_sem()`

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** —
- **Branch/PR:** `m57-dcfi-invariance-criterion` / https://github.com/jmgirard/circumplex/pull/83

## Goal

Add the Cheung–Rensvold (2002) ΔCFI as a labeled, reported-only secondary invariance criterion in `ssm_sem()`'s ladder — scope-gated to its validated envelope (two-group, plain ML) — and author the source note it owes; Δχ² stays the sole gating verdict statistic.

## Scope

**In:** compute `dcfi` per adjacent ladder rung and surface a scope-gated Cheung–Rensvold flag against the −.01 cutoff, per the operational rule in `devel/cr2002-transcription.md` (not the source's contradicted p. 251 sentence). Author `cairn/references/cheung2002.md` and correct the now-stale "uncited" prose it falsifies. Teach the criterion + full scope caveats in roxygen, NEWS, and the SEM vignette.

**Out:** ΔGamma-hat / ΔMcDonald's NCI (the CR trio's other members; their cutoffs are transcribed but need own wiring — candidate row if wanted). A *validated* flag for robust CFI or >2 groups → would need new simulation CR2002 never ran; this milestone declines to flag there rather than inventing a cutoff. Gating on ΔCFI → rejected by design (Δχ² sole gate; spec §12.2).

## Acceptance criteria

- [x] AC1: the invariance-ladder table gains a `dcfi` column = CFI(rung) − CFI(previous rung), differenced from the CFI the table already displays (plain `cfi` under ML, `cfi.robust` under MLR); NA for configural (no predecessor) and the strict-tier vacuous metric rung. Value matches a hand-computed difference on a fixture.
- [x] AC2: a labeled retain/reject flag against −.01 (direction per the transcription: ΔCFI **< −.01 → reject**) prints **only when the fit is two-group AND plain ML**; under a robust estimator or >2 groups the ΔCFI value prints with an explicit "cutoff not validated for this configuration (robust CFI / >2 groups); descriptive only" note and no binary verdict. The Cheung–Rensvold attribution + scope label (`α = .01, two-group ML simulation scope`) always accompanies the value.
- [x] AC3: ΔCFI is reported-only — `comparable`, the gating verdict string, and the estimation-fit selection are byte-identical with and without the ΔCFI machinery, proven on a fixture where the ΔCFI flag and the Δχ² gate would disagree.
- [x] AC4: `cairn/references/cheung2002.md` exists (Provenance block copied from an existing page; channel = the 2026-07-07 full-text transcription; `Traces to` the `R/ssm_sem.R` lines that now cite it); `INDEX.md` moves cheung2002 to a committed page; the stale INDEX "no shipped code … cites it" line and the design-doc §12.2 "unexercised offer" framing are corrected (M56 stale-prose lesson).
- [x] AC5: roxygen (`ssm_sem()`), a NEWS entry (exported print output change), and a short precise SEM-vignette paragraph **each** teach the criterion with its full scope caveats (two groups, ML, normality, Type-I only, not validated for robust indices), attributing the corrected direction to **the source's own Table 5 as recorded in `cairn/references/cheung2002.md`** — the citable record T3 established, superseding `devel/cr2002-transcription.md`, which user-facing docs must not cite; no prose implies validation for robust CFI, **a non-ML estimator,** or >2 groups. _(Amended 2026-07-25 at the M57 review send-back; original wording named the transcription note, which T3 superseded.)_
- [x] AC6: `devtools::test()` clean; `devtools::document()` run; `devtools::check(args = "--no-manual")` clean (0/0/0).

## Coverage

- AC1 → T1, T2
- AC2 → T1, T2
- AC3 → T1, T2
- AC4 → T3
- AC5 → T4
- AC6 → T5

## Tasks

- [x] T1: tests first (`tests/testthat/test-ssm_sem.R`) — (a) `dcfi` = hand-computed CFI difference + NA rows; (b) two-group ML: ΔCFI ∈ (−.01, 0] retains, < −.01 rejects; (c) scope-gate: MLR fit and a 3-group fit print value + note, no binary flag; (d) reported-only: gate/verdict/fit identical with ΔCFI present vs absent on a disagreement fixture.
- [x] T2: implement `dcfi` in `sem_fit_ladder` ([R/ssm_sem.R:776](R/ssm_sem.R:776)) and the scope-gated flag; wire into the table print ([R/ssm_sem.R:1604](R/ssm_sem.R:1604)). No touch to `comparable`/verdict/`fit_est` selection.
- [x] T3: author `cairn/references/cheung2002.md`; add the inline `# Cheung & Rensvold (2002)` citation at the implementing lines; update `INDEX.md`; correct the stale INDEX + `devel/m5-sem-design.md` §12.2 prose.
- [x] T4: roxygen on `ssm_sem()` + NEWS.md entry + SEM-vignette paragraph.
- [x] T5: D-entry (D-027) recording the decision; `devtools::document()`; `devtools::test()`; `devtools::check(args = "--no-manual")`.
- [x] T6 (from review, F1 — AC2): require ML estimation as well as a plain CFI in the scope gate — AND `identical(lavaan::lavInspect(fit, "options")$estimator, "ML")` into `cfi_plain`'s conjunction (it reads `"ML"` for ML/MLR/MLM, `"GLS"`/`"ULS"`/`"DWLS"` otherwise, so `estimator = "ML", se = "robust.huber.white"` stays admitted per the T2 gate decision); add the third `why` reason to `sem_dcfi_note()`; regression test at a two-group `GLS` fit asserting no `cr` column and a caveat naming the non-ML estimator. Record the conjunction by APPENDING a narrowing D-entry (D-027 is history under IP4 and is never edited). Re-verify AC2 and AC6.
- [x] T7 (from review, F2): print `dcfi` at `max(digits, 4)` so the displayed value always resolves against the 2-decimal cutoff; test that −0.0096 and −0.0104 render distinguishably under their opposite labels.
- [x] T8 (from review, AC5): add the Type-I-error-only caveat (and normality as the simulation's data assumption, not just "normal-theory CFI") to the NEWS bullet; then take the AC5 attribution clause through the substantive-amendment gate — its "the transcription note" target was superseded by T3's own `cairn/references/cheung2002.md`, and user-facing docs must not cite a `devel/` file. Re-sync `cheung2002.md`'s `Traces to` line anchors after T6/T7 move them (M56 lesson).

## Work log

- 2026-07-24: T1+T2 done, committed together — a tests-first pair cannot be green apart, so both tick at one checkpoint. Minor amendment: the ΔCFI tests live in `tests/testthat/test-ssm_sem_groups.R` (where every ladder test lives), not `test-ssm_sem.R` as T1 wrote — the criterion is a multi-group ladder feature.
- 2026-07-24: T1 fixtures — a second-harmonic-perturbed two-group population whose `eps` sets the population CFI drop while n sets Δχ², so the two criteria can be made to DISAGREE (eps = 0.12, n = 2000/group: ΔCFI −.0022 → "retain", Δχ² p = .0004 → reject). Oracles: invariant (dcfi == diff of the table's own cfi) + live (both rungs refitted outside the ladder, lavaan's own CFI differenced, agreeing to 1e-8) + a deterministic helper pin of the ≥ −.01 boundary.
- 2026-07-24: T2 — `sem_dcfi_cutoff`/`sem_dcfi_flag`/`sem_dcfi_note` added; `sem_fit_ladder` gains `dcfi`/`cr` columns and a `dcfi_scope` record carried into the returned `invariance` list; `print.circumplex_ssm_sem` gains the two columns and the attribution block. Full suite 0 failures / 3220 pass (4 pre-existing CPM-Hessian warnings in test-ci_accuracy.R). Print output rendered and read in all four configurations (in-scope retain, in-scope reject, MLR, 3-group).
- 2026-07-24: T3 — `cairn/references/cheung2002.md` authored. Not carried over from the 2026-07-07 transcription: every anchored page (233, 248–251) was re-read against the born-digital PDF text layer this session (M40 lesson — authoring a page is a fresh extraction). The two channels agree on every value; two typographic diffs recorded (the article prints "–0.01", and the general-criterion sentence spans pp. 250–251). Table 5's ΔCFI 1% column (−.0085 … −.0039, all negative, means ≈ 0) is the decisive internal evidence that the p. 251 sentence has the direction backwards — a stronger direction oracle than the transcription's reasoning alone. No DOI is printed anywhere in the 23-page PDF, so none is asserted.
- 2026-07-24: T3 stale-prose sweep (M56 lesson) grepped the OLD assertion's keywords, not just the touched lines: corrected in place the INDEX "owes no page / unexercised offer" block, `devel/m5-sem-design.md` §6.2's TBT paragraph and §12.2 item 2, and — found by the sweep, beyond AC4's named two — `cairn/references/wendt2019.md`'s "sources are not on the shelf" claim, which was already wrong for cheung2002 and gurtman2003 when written. `cairn/milestones/archive/M41-*.md` carries the same falsified claim and is left byte-untouched (history, IP4); `devel/cr2002-transcription.md` likewise (ROADMAP's byte-untouched-until-M7 constraint). `cairn_validate`: all checks pass, `references index<->disk` PASS.
- 2026-07-24: T4 — `@param invariance` + `@return` + a new `@references` entry; a NEWS bullet in the Latent-variable (SEM) section (v2.0.0 is unreleased and `ssm_sem()` is new in it, so this is a feature note, not a changed-behavior entry); vignette §7 gains two paragraphs, §8's caveat list a clause, and the reference list the citation. All three surfaces state cutoff + alpha, reported-only (with why the two criteria can disagree: ΔCFI is sample-size-insensitive, Δχ² is not), and the full scope. `document()` run; roxygen and the generated Rd are ASCII-only (M7's literal-Greek PDF-manual lesson). Tests 0 failures / 3220 pass.
- 2026-07-24: T5 — D-027 appended (reported-only; direction from Table 5's 1% tails, not the p. 251 sentence; scope gate keyed on the differenced statistic; extrapolation to robust CFI / >2 groups rejected as fabrication). `devtools::document()` produces no diff; `devtools::test()` 0 failures / 3220 pass; `devtools::check(args = "--no-manual")` **0 errors / 0 warnings / 0 notes** (8m 51s; tests, examples with --run-donttest, and vignette re-build all OK). Not read as coverage per the M7 lesson: the --no-manual gap is the PDF manual, and `R/ssm_sem.R` + `man/ssm_sem.Rd` were grepped ASCII-only.
- 2026-07-24: verified what the vignette's own reader sees — the `jz2017` Gender ladder prints `dcfi = -0.011` under the MLR default with the not-validated note and no verdict, while Δχ² rejects metric. A value that would cross the cutoff, correctly left unlabeled: exactly the case the scope gate exists for.
- 2026-07-24: all tasks done, local checks clean → status `review`.
- 2026-07-25: review FAILED at PR #83 → status back to `in-progress`. Exactly what failed: **AC2** — the scope gate keys on "no `cfi.robust`/`cfi.scaled`", which is TRUE for `GLS`/`WLS`/`ULS`/`DWLS` too, so a two-group non-ML fit gets a "retain"/"reject" label with no not-validated caveat (F1, scored 85, reproduced end-to-end). **AC5** — the NEWS bullet omits the Type-I-only caveat and states normality only as a property of the index, and no user-facing surface attributes the corrected direction to the transcription note as the criterion's wording requires. Also actioned: **F2** (85) `dcfi` printed at the −.01 cutoff's own precision, so −0.0096 and −0.0104 both render as −0.01 under opposite labels. AC1/AC3/AC4 verified with fresh evidence and stay ticked; AC6 passed at commit 983caefa but is owed again once `R/` changes. Sub-threshold and logged, not actioned: F3 (75), F4 (40).
- 2026-07-25: AC5 needs a **gated amendment**, not just a fix — its "attributing the corrected direction to the transcription note" clause was obsoleted by this milestone's own T3, which made `cairn/references/cheung2002.md` the citable record superseding `devel/cr2002-transcription.md`, and a user-facing doc must not cite a `devel/` internal file. Criterion wording is plan-owned; review does not reinterpret it.
- 2026-07-25: review-side discovery worth a lesson: `devtools::check()` defaults to `manual = FALSE`, so the PDF-manual step M7 was burned by is skipped even by the check the profile's `consistency-gate` slot calls full. Ran `R CMD Rd2pdf` directly — clean 73-page manual.
- 2026-07-25: T6 done — the scope gate is now a three-part conjunction (two groups AND ML fit function AND plain CFI). Verified `lavInspect(fit, "options")$estimator` myself rather than trusting the review's claim: it reads `"ML"` for ML/MLR/MLM and `"GLS"`/`"ULS"`/`"DWLS"`/`"WLS"` otherwise, so the conjunction excludes MLR/MLM by the CFI clause and GLS-family by the ML clause, and still admits `estimator = "ML", se = "robust.huber.white"` as D-027 intended. `dcfi_scope` gains `estimator` and `ml`; the print note now NAMES the reason ("non-ML estimator: GLS", "robust CFI", "N groups", or a combination). Recorded as **D-028 appended**, not as an edit to D-027 — my own review-written task text said "amend D-027", which IP4 forbids; task text corrected.
- 2026-07-25: T7 done — printed `dcfi` moves to `max(digits, 4)` so values either side of the cutoff never render alike. While fixing it, the longer reason list overflowed the hand-wrapped note past 80 columns, so `sem_dcfi_note()` now wraps with `strwrap()`; correspondingly the note assertions moved onto whitespace-normalized output via a new `dcfi_printed()` test helper, since an assertion keyed to where a line breaks fences the wrapper rather than the wording.
- 2026-07-25: F3 folded in (it was sub-threshold at 75, but the milestone was open and the guard is cheap): a COMPLEMENT fixture where ΔCFI rejects while Δχ² retains, asserting `comparable` TRUE and the contrast computed. Needed a new low-saturation population — a small baseline chi-square makes CFI move fast per unit of misfit while Δχ² stays modest at n = 150/group. Found by scanning (cc0, eps, n, seed) for the cell with the widest margin on both sides: ΔCFI −0.019 (0.009 past the cutoff) with p = 0.149 (3× alpha), no warnings. The two fixtures now fence both leak directions; the original alone passed against a hypothetical "comparable requires both criteria" bug.
- 2026-07-25: T8 done — NEWS gained the Type-I-only and multivariate-normality caveats; AC5 amended at a question gate (Jeff chose the full proposed text) to retarget the attribution from `devel/cr2002-transcription.md` to the source's own Table 5 as recorded in `cairn/references/cheung2002.md`, make the per-surface reading explicit, and extend the no-implication clause to non-ML estimators. The amended "each" made the direction attribution owed by NEWS and the vignette too, not just the Rd — both gained it. Scoped per-surface re-check: all seven items present in all three surfaces. `cheung2002.md` `Traces to` anchors re-synced and each range re-opened to confirm it contains what it claims.
- 2026-07-25: F4 left unfixed per its recorded triage (scored 40): the page's "at or below its critical value" refers to Table 5's per-hypothesis values (−.0085 …) while the implemented rule uses the rounded general −.01, so the two clauses have different referents rather than contradicting. Re-examined while editing the page anyway; the triage stands.
- 2026-07-25: vignette §7 restructured after reading it as a reader — the direction provenance had been spliced mid-paragraph, stranding "It is reported, never gating" from its antecedent; it is now its own paragraph. The direction is attributed to the article's own Table 5 and the `cairn/` path is deliberately NOT printed, since a vignette reader cannot follow it (AC5's "as recorded in" names where the repo keeps the record, not what the docs must cite).
- 2026-07-25: proved the three new guards have teeth by MUTATION, not by eye (LESSONS' standing rule), done via in-memory `assignInNamespace` so no file was touched while a check was mid-flight. (1) Dropping the ML clause → the GLS fit gets `in_scope` TRUE and `cr` "retain", failing the new guard. (2) Leaking a ΔCFI rejection into the gating `failed` set → `comparable` FALSE, no contrast, 2 result rows, failing the AND-leak guard. (3) Printing `dcfi` at `digits` → both boundary rows render "… 0.935 -0.01 retain" and "… 0.935 -0.01 reject", failing the precision guard and reproducing F2 exactly. Worth recording: the first attempt at mutation (2) patched `comparable` directly and ERRORED in the verdict builder, which assumes not-comparable implies a failed or untestable row — an error is not the wrong-answer channel a real leak would be, so the mutation was re-cut to flow through the real machinery.
- 2026-07-25: all send-back tasks done, local checks clean → status `review`. Final-tree verification: `devtools::test()` 0 failures / 3247 pass; `devtools::document()` no diff post-commit; `devtools::check()` **0/0/0** twice — once over the code tree (6m 11s) and again over the final tree after the vignette restructure, since the first run predated that commit and a check cannot vouch for a tree it never saw. Vignette re-build OK in both. AC2, AC5 and AC6 are the boxes the send-back left unticked and are re-review's to verify against the amended AC5 text.
- 2026-07-25: **correction to the 2026-07-25 T8 entry above.** That line claims "each range re-opened to confirm it contains what it claims" for the re-synced `cheung2002.md` anchors. The claim is too strong: the verification was `grep -q <identifying string>` *inside* each range, which proves containment and says nothing about the boundaries. Two of four were off by one — `sem_dcfi_flag` cited `773-789`, excluding its closing brace at 790; `sem_dcfi_note` cited `791-823`, starting a line early and excluding its brace at 824; and the scope block cited `930-949`, starting on a blank line and excluding line 950, the `table$cr <- sem_dcfi_flag(...)` line its own description names. Found by the prior-review lens at the second pass. Anchors corrected to `759-771`, `773-790`, `792-824`, `931-950`, each now verified by its FIRST and LAST line rather than by a contained string. The earlier entry stands as written (history); this is its correction.
- 2026-07-25: fourth probe-narrower-than-what-it-probed instance this milestone (M43's lesson family): whole-file greps leaking hits across surfaces; `-0.01\b` matching the note that states the rule; an unescaped `$` acting as an end-of-line anchor in a grep pattern; and now a containment grep standing in for a boundary check. The first three were caught before they reached a conclusion; the fourth reached a work-log claim. Worth extending the existing LESSONS line at post-merge hygiene rather than adding a fourth near-duplicate.
- 2026-07-25: second-pass review findings actioned at review (all doc/comment text, no behavioral surface): F5 (80) the `estimator = "ML"` gloss was false under `missing = "fiml"` — isolated the trigger to FIML itself, `se` irrelevant — fixed in all three surfaces; F8 (80) a NEWS grammar slip fixed. F6 (70) and F7 (52) logged sub-threshold; F7's line was rewrapped only because F5's fix rewrote that paragraph. The prior-review lens's anchor off-by-one fixed and verified by first/last line.
- 2026-07-25: while fixing F5 I introduced and then caught a regression in my own edit — the reworded NEWS bullet dropped the "not validated" caveat AC5 requires. Restored before commit. Two further grep misses this pass (roxygen wrapping "multivariate / normal" across lines; the Rd phrasing the caveat as "no cutoff here was validated for one") were probe failures rather than content gaps, confirmed by reading the passages instead of pattern-matching them.
- 2026-07-23: created by /milestone-plan. Promotes the "ΔCFI secondary invariance criterion" candidate (M41 surface; `devel/m5-sem-design.md` §12.2 item 2; transcription `devel/cr2002-transcription.md` landed 2026-07-07; T4 shipped without it). Q1 (robust-CFI scope) resolved to the scope-gated hybrid; Q2 → docs reach roxygen+NEWS+vignette; Q3 → rides v2.0.0, no M7 gate. No RB tripwire (numeric part is a subtraction; transcription is the value/direction oracle). No scope-supersession D-entry owed — completes M5's already-bundled `ssm_sem`, not a new feature family.

## Decisions

- 2026-07-24 (implement question gate): the ΔCFI surface. (a) The printed ladder gains a `dcfi` column plus, in scope, a `cr` retain/reject column, with one attribution + scope block beneath the table. (b) `invariance$table` stores `cr` as well as `dcfi`, so a programmatic caller gets the criterion applied rather than re-deriving the −.01 cutoff and the scope rule. (c) The scope gate keys on **the statistic actually differenced** — two groups AND no `cfi.robust`/`cfi.scaled` in `fitMeasures()` — not on `estimator == "ML"`. Keying on the statistic means a robust index can never be labeled against a normal-theory cutoff, and it correctly admits `estimator = "ML", se = "robust.huber.white"`, where the CFI is normal-theory (CFI does not read the standard errors).

## Review

Reviewed 2026-07-25 (PR #83). `master` had not moved since the branch was cut (`git merge-base --is-ancestor origin/master HEAD` true, 0/0 divergence), so no merge or re-run was needed. No `Driving RR:` → the projection-vs-outcome step no-ops.

**AC1 — evidence.** Fresh run on the disagreement fixture: the two CFI values print at full precision as 0.99876258863877743 (configural) and 0.99655630003876394 (metric); the hand difference −0.00220628860001348 is `identical()` to the stored `dcfi`. Independent live oracle in-suite refits both rungs *outside* the ladder and differences lavaan's own CFI, agreeing to 1e-8. The "differenced from the CFI the table displays" clause verified discriminatingly under MLR: stored `dcfi` = −0.004175874308830 matches the difference of `cfi.robust` (identical to 1e-12) and does **not** match the difference of plain `cfi` (−0.004267078825943) — so it tracks the displayed index, not a fixed field. NA clauses fenced by `test-ssm_sem_groups.R:581` (configural) and `:633-635` (the strict tier's vacuous metric rung, asserted vacuous first via its `note`, then `dcfi` and `cr` both NA).

**AC2 — evidence.** All four configurations executed fresh; the flag column appears in exactly one. 2-group ML: `in_scope` TRUE, `cr` = `.,retain`, header carries the `cr` column. 2-group MLR: `in_scope` FALSE, value present, no `cr` column, note names "(robust CFI)". 3-group ML: `in_scope` FALSE, value present, no `cr` column, note names "(3 groups)". 3-group MLR: note names **both** reasons, "(robust CFI; 3 groups)". The attribution line "ΔCFI: Cheung & Rensvold (2002) criterion, alpha = .01, two-group ML simulation scope" is present in all four, i.e. it accompanies the value unconditionally as the criterion requires. Out of scope every printed `cr` is NA and the text says "descriptive only, with no binary verdict".

**AC3 — evidence (literal with/without comparison).** `master`'s pre-M57 `sem_fit_ladder` was sourced into an environment enclosed by the package namespace (confirmed ΔCFI-free: `grepl("dcfi", deparse(old$sem_fit_ladder))` FALSE) and run on the disagreement fixture beside the new one. Disagreement confirmed: `dcfi` = −0.002206289 → `cr` "retain", while Δχ² p = 0.00044 < alpha 0.05. `identical()` TRUE for `comparable` (FALSE both), `verdict`, `required`, `gate`, `syntax`, the estimation fit's `coef()`, the fit's **entire `fitMeasures()` vector**, and the whole pre-existing column block (`chisq`/`df`/`cfi`/`rmsea`/`dchisq`/`ddf`/`p`/`note`). The only difference is the two added columns; the verdict string contains no "CFI".

**AC2 — FAILS (reproduced).** Ticked on first pass and **un-ticked**: my four-configuration probe (2-group ML / 2-group MLR / 3-group ML / 3-group MLR) never exercised a non-ML estimator that also yields plain-named fit measures, so it could not see the defect — the probe was narrower than what it probed (LESSONS M43, third recurrence this session). The [O] diff-bug lens reproduced it: a two-group `estimator = "GLS", se = "standard"` fit yields `dcfi_scope$in_scope` TRUE, a `cr` column reading "retain", and the attribution note with **no** not-validated caveat. `GLS`/`WLS`/`ULS`/continuous `DWLS` all lack `cfi.robust`/`cfi.scaled`, so `cfi_plain` is TRUE for all of them. AC2 requires the flag "only when the fit is two-group AND plain ML"; it currently prints for two-group non-ML. See finding F1.

**AC5 — FAILS as written, two clauses.** (i) *Full scope caveats per surface.* Scoped extraction of each surface's own ΔCFI prose (my first pass grepped whole files and leaked hits from unrelated passages — probe artifact, corrected): the Rd carries all five (`man/ssm_sem.Rd:96-98`) and the vignette carries all five (`:342-343`, `:347`, `:409`), but the NEWS bullet omits **Type-I-error-only** outright and states normality only obliquely, as a property of the index ("plain normal-theory CFI") rather than as the simulation's data assumption. (ii) *Attribution target.* The criterion requires "attributing the corrected direction to the transcription note"; no user-facing surface names `devel/cr2002-transcription.md`. The Rd attributes the correction to the article's own Table 5, and the code cites `cairn/references/cheung2002.md` — because T3 deliberately made that page the citable record superseding the transcription, and a user-facing doc must not cite a `devel/` internal file. This is a criterion the milestone's own later work obsoleted; it needs a gated amendment, not a charitable tick.

**AC6 — evidence (passes as written at commit 983caefa, re-run owed).** `devtools::test()` 0 failures / 3220 pass; `devtools::document()` produces no diff (`git status` on `man/` + `NAMESPACE` clean); `devtools::check(args = "--no-manual")` 0 errors / 0 warnings / 0 notes. Also run beyond the criterion: the profile's full `devtools::check()` 0/0/0 (8m 50s incl. tests, `--run-donttest` examples, vignette re-build). **Discovery worth keeping:** neither invocation covers the PDF manual — `devtools::check()` defaults to `manual = FALSE`, so "checking PDF version of manual" appears in neither log, which is the M7 trap ("a step that doesn't run reports success") sitting inside the check the profile calls full. Ran it directly instead: `R CMD Rd2pdf --no-preview --force` builds a clean 73-page manual; the two pdfTeX `dest` warnings are pre-existing and reference `ggplot2::coord_radial()` and `summary`, not `ssm_sem`. Left un-ticked because the F1/F2 fixes change `R/` and the whole battery is owed again on return.

**AC4 — evidence.** `cairn/references/cheung2002.md` present (150 lines) with one `**Provenance.**` block following `strack2013.md`'s shape, its `Extraction:` status on one physical line (359 chars, unwrapped — the M40/M45 parser trap). The 2026-07-07 transcription is named as a channel in two places, including a dedicated two-channel-history paragraph; the page records an additional fresh 2026-07-24 re-read, which is more than the criterion's named channel, not less. All four `Traces to` anchors into `R/ssm_sem.R` were opened and each contains what it claims. `INDEX.md` carries the catalog line with the filename as link text (the M40 mutation-caught format); "no shipped code" occurrences now 0; `devel/m5-sem-design.md` carries the §6.2 and §12.2 resolutions. (The F1 fix will shift these `Traces to` line ranges — re-sync them on return, per the M56 anchor lesson.)

**Consistency gate.** `cairn_validate` exit 0, 16/16 PASS, no FAILs (47 advisories, all M7's known legacy multi-line work-log entries). No `DESIGN.md` principle changed → `cairn_impact` skipped. Toolchain half, from the `r-package` profile's `consistency-gate` slot: `document()` no diff ✓; no hand-edited generated files (the no-diff check covers it) ✓; README.md newer than README.Rmd ✓; `pkgdown::check_pkgdown()` "No problems found" ✓; NEWS.md has an entry for the user-visible change ✓ (its content is AC5's problem, not this gate's); no new top-level files and no new exports, so no `.Rbuildignore` or `_pkgdown.yml` row owed ✓; full `devtools::check()` clean — see AC6. CI on PR #83: `pkgdown` pass, `ubuntu-latest (release)` and `test-coverage` still pending at review time (moot — the branch will change).

**Independent fresh-context review — three lenses + scorer.**
- **[S] blame-history: clean, 0 findings.** Verified the M5/T4 comparability block (`comparable`/`untestable`/`failed` and its NA-p-is-not-a-retention reasoning) is byte-for-byte untouched and that the new code is inserted before it without reading or writing `comparable`/`verdict`/`gate`/`required`/`fit_est`. Confirmed the 2026-07-07 robust-index decision (commit b5388e76: MLR default + the `sem_fm_pick` fallback, because naive chi-square over-rejects on skewed octant data) is respected — `dcfi` reuses that same `sem_fm_pick` call rather than reading a different field. Confirmed the append-only vs correct-in-place split was honored on every edited file.
- **[S] prior-review-comments: clean, 0 findings.** GitHub inline-comment surface probed empty (`pulls/comments?per_page=1` → `[]`), consistent with the standing LESSONS note that this lens is a permanent no-op here; no per-PR walk performed. Primary evidence `cairn/milestones/archive/` + `LESSONS.md` read: the M47 Extraction one-physical-line and copy-an-existing-block lessons, the M41-D1 not-a-human-attestation caveat, the M42-D1 born-digital-vs-OCR witness concern, the M40 INDEX link-text trap, the M49 miscategorized-framing lesson, and the M56 stale-prose sweep all checked and none regressed.
- **[O] diff-bug: 4 findings.** Verified clean on the risk areas that hold: `prev_cfi`/`prev_fit` can never disagree across the vacuous rung (the branch `next`s before touching either, confirmed on a live `model = "strict"` fit); the two row `data.frame()`s have identical names in identical order so `rbind` is correct rather than merely non-erroring; `cfi_plain` length 0 is unreachable (configural is never vacuous) and a single-rung ladder yields no `cr` column, no note, no crash; the cutoff/NA/`in_scope` handling verified at NA, 0, −0.01, −0.0100001 and with `in_scope` FALSE/NA/zero-length; every occurrence of `dcfi`/`cr`/`dcfi_scope` traced across `R/` with **no leak** into the gate; print gating is properly nested (`cr` ⊂ note ⊂ some non-NA `dcfi`); `n_groups` cannot be inflated by an unused factor level (`droplevels()` at `R/ssm_sem.R:1364`); ASCII-only across `R/ssm_sem.R`, `man/ssm_sem.Rd` and the test file.

**Scorer ([S], fresh agent, did not generate the findings).** F1 **85**, F2 **85**, F3 **75**, F4 **40**.

*Actioned (≥80):*
- **F1 (85) — the scope gate admits non-ML estimators.** `R/ssm_sem.R:901-903,921-932`. Fix on return: AND `identical(lavaan::lavInspect(fit, "options")$estimator, "ML")` with the existing plain-CFI test (it returns `"ML"` for ML/MLR/MLM and `"GLS"`/`"ULS"`/`"DWLS"` otherwise, so the conjunction is exactly "ML estimation AND plain CFI" and still admits `estimator = "ML", se = "robust.huber.white"` per this milestone's own gate decision); add a third `why` reason to `sem_dcfi_note()`; add a regression test at a non-ML plain-CFI estimator.
- **F2 (85) — `dcfi` printed at the cutoff's own precision.** `R/ssm_sem.R:1732`. `round(tab$dcfi, digits)` at the default `digits = 3` renders both −0.0096 (retain) and −0.0104 (reject) as −0.01, so two rows can show one value under opposite labels, directly beneath a rule stated in terms of that value. Fix on return: print at `max(digits, 4)`.

*Sub-threshold, logged not actioned (IP3):*
- **F3 (75) — the reported-only guard cannot see a conjunctive leak.** The disagreement fixture (ΔCFI retain, Δχ² reject, truth `comparable = FALSE`) passes against a hypothetical `comparable = both-retain` bug, so it fences OR-leaks only. Not a failure of AC3 as written (byte-identity against master's pre-M57 ladder is proven on such a fixture, and both the [O] trace and that comparison independently establish no leak exists) — it is a missing durable guard. The complement fixture closes it: in-scope, ΔCFI < −.01 with Δχ² p ≥ alpha, asserting `comparable` TRUE and the contrast computed. A natural fold-in while the milestone is open.
- **F4 (40) — the source note's boundary prose.** `cairn/references/cheung2002.md:76-78` vs `:85-86`: "at or below its critical value" reads as inclusive while the implemented rule retains at exactly −.01. Scorer judged the two clauses to have different referents (Table 5's per-hypothesis values vs the rounded general cutoff) rather than to contradict, with zero numerical consequence. Rejected as a defect; recorded here so the next reader of that page has it.

---

### Second pass (re-review), 2026-07-25

The first pass returned the milestone to `in-progress`; the second pass changed all five product files, so every criterion was re-verified from scratch rather than only the three unticked boxes. `master` still had not moved. PR #83 head `39e4ae1d`.

**AC1 — re-verified.** Hand arithmetic at full precision: cfi 0.99082336261134385 → 0.99384882217617665, difference 0.00302545956483280, `identical()` to the stored `dcfi`. Configural `dcfi` NA. Discriminating MLR check: stored `dcfi` = 0.003009282933818 matches the `cfi.robust` difference to 1e-12 and does **not** match the plain-`cfi` difference (0.003025459564833) — so it still tracks the displayed index, and T6's ML clause did not disturb which field is read. Strict tier: the vacuous metric rung carries a note, `dcfi` NA and `cr` NA, and the scalar rung differences against configural exactly.

**AC2 — re-verified across SIX configurations, exactly one in scope.** `2grp ML` (est=ML, ml=T, plain=T) → `in_scope` TRUE, `cr` = `.,retain`, `cr` column present. `2grp MLR` (ml=T, plain=F) → out, note "(robust CFI)". `2grp GLS` and `2grp WLS` (ml=**F**, plain=**T**) → out, note "(non-ML estimator: GLS/WLS)" — the exact hole F1 found, now closed, and the `plain=TRUE, ml=FALSE` combination is what proves the two clauses are independent. `3grp ML` → out, "(3 groups)". `3grp ULS` → out, both reasons. In all six the value prints and the attribution line with its published scope label is present; in the five out-of-scope cases every `cr` is NA and no `cr` column appears.

**AC3 — re-verified in BOTH disagreement directions** against `master`'s pre-M57 ladder (confirmed ΔCFI-free by deparse). OR direction (ΔCFI −0.00221 "retain" vs Δχ² p = 0.00044): `comparable`, `verdict`, `required`, `gate`, `syntax`, the fit's `coef()`, its whole `fitMeasures()` vector and the entire pre-existing column block all `identical()`; only `dcfi`/`cr` added; no "CFI" in the verdict. AND direction (ΔCFI −0.01902 "reject" vs Δχ² p = 0.14942): same, all `identical()`. The second direction is new this pass and is what forecloses an AND-leak.

**AC4 — re-verified.** One `**Provenance.**` block; `Extraction:` on one physical line (359 chars, unwrapped); transcription named as a channel twice; INDEX link text is the filename; "no shipped code" occurrences 0. All four re-synced `Traces to` anchors opened and confirmed — including `930-949`, which my first probe reported missing because an unescaped `$` in a grep pattern is an end-of-line anchor, not a literal (`grep -F` confirms it).

**AC5 — re-verified per surface, scoped to each surface's own ΔCFI prose** (26 / 21 / 32 lines for Rd / NEWS / vignette). All three carry all seven required items: two groups, ML, multivariate normality, Type-I-only, not-validated, the corrected direction attributed to Table 5, and the non-ML case. Nothing implies validation for a robust CFI, a non-ML estimator, or >2 groups. The amended criterion's "each" is satisfied distributively, not merely collectively.

**AC6 — re-verified.** `devtools::test()` 0 failures / **3247** pass (up 27 from the first pass; same 4 pre-existing CPM-Hessian warnings in `test-ci_accuracy.R`). `devtools::document()` produces no diff. `devtools::check(args = "--no-manual")`'s superset was run instead — the profile's full `devtools::check()` — **0 errors / 0 warnings / 0 notes**, `Status: OK`, with `checking tests` and `checking re-building of vignette outputs` both OK. Run three times across the second pass, because a check cannot vouch for a tree it never saw: once over the code tree, once after the vignette restructure, and once at review. The PDF-manual step remains outside every `devtools::check()` invocation (`manual = FALSE` is its default) and was covered separately by `R CMD Rd2pdf`, which built a clean 73-page manual; `R/ssm_sem.R` and `man/ssm_sem.Rd` are ASCII-only.

**Consistency gate.** `cairn_validate` exit 0, 16/16 PASS, 0 FAIL (`coverage complete` and `references index<->disk` both PASS). No `DESIGN.md` principle changed → `cairn_impact` skipped. Toolchain slot: `document()` no diff; README.md newer than README.Rmd; `pkgdown::check_pkgdown()` "No problems found"; no new exports so no `_pkgdown.yml` row owed; no `NAMESPACE`/`RcppExports` edits; NEWS carries the user-visible entry (+22 lines). **GitHub CI green on `39e4ae1d`, the exact commit reviewed** — `R-CMD-check.yaml`, `test-coverage.yaml`, `pkgdown.yaml` all `success`, verified by run SHA rather than by the checks list alone. PR `mergeStateStatus` CLEAN.

**Independent fresh-context review, second pass — three lenses + scorer.**
- **[S] blame-history: clean, 0 findings.** Independently byte-diffed `DECISIONS.md`'s pre-D-028 region against HEAD (identical — D-027 untouched, IP4 honored), confirmed the new ML clause gates only the label and never which CFI `sem_fm_pick` reads (so the 2026-07-07 MLR decision stands), confirmed no `_snaps/` baseline covers this print output, and confirmed the `dcfi_printed()` switch preserved every existing assertion.
- **[S] prior-review: 1 finding.** GitHub inline-comment surface probed empty again. Verified F1/F2/F3 were fixed as triaged and F4 correctly left alone; confirmed the stale-prose sweep left no statement of the old two-part rule outside D-027 (which must keep it, being history).
- **[O] diff-bug: 4 findings.** Cleared, by live probe: `lavInspect(fit, "options")$estimator` is always length-1 and uppercase-normalized across ML/MLR/MLM/MLMV/MLF/GLS/WLS/ULS/DWLS and lowercase input, so D-028's premise holds; rungs cannot disagree; vacuous rungs `next` before appending so `est_used`/`cfi_plain` stay aligned with fitted rungs; `est_used` cannot alter control flow; the reported-only gate reads only `p`/`note`/`rung`; all 8 note combinations render ≤71 chars; the `"configuration ()"` and `"non-ML estimator: NA"` renderings are unreachable from `sem_fit_ladder`; zero-length `dcfi` replacement paths print cleanly; and the low-saturation fixture is robust (population Σ min eigenvalue 0.60, no negative variances, 0 warnings, margins 0.0090 and 3× alpha).

**Findings and triage (scorer: fresh [S] agent that did not generate them).**

*Actioned:*
- **Anchor off-by-one (prior-review lens; verified directly rather than scored, being a checkable line-number claim).** Two of four re-synced `cheung2002.md` anchors excluded the closing brace of the function they name, and the scope-block anchor began on a blank line while excluding line 950, the very line its description points at. **Fixed** to `759-771`, `773-790`, `792-824`, `931-950`, each verified by first *and* last line. My work-log claim of having "re-opened" each range was a containment grep and is corrected by an appended line.
- **F5 (80) — the docs' `estimator = "ML"` gloss is false under `missing = "fiml"`.** Independently reproduced and then isolated: FIML alone adds `cfi.robust` to `fitMeasures()` (`test` stays `standard`, and `se` is irrelevant — plain/sandwich behave identically), so `cfi_plain` is FALSE and the label is withheld under `estimator = "ML", missing = "fiml"`. Behavior is correct; the docs were wrong, and the second pass had sharpened the parenthetical into what read as an exhaustive two-item list. **Fixed** in all three surfaces, which now state that plain ML is necessary but not sufficient and name FIML as a third route out of scope. Same proxy-vs-condition failure D-028 fixed in the code, left standing in the prose.
- **F8 (80) — `NEWS.md` grammar.** "The invariance ladder `print()` reports additionally carries `dcfi`" read as a half-finished edit in a user-facing changelog. **Fixed** to "The invariance ladder that `print()` reports now also carries".

*Sub-threshold, logged not actioned (IP3):*
- **F6 (70) — the print-precision fix narrows the value/label collision band 100× rather than closing it.** At `|dcfi + 0.01| < 5e-5` two rows still render alike under opposite labels. Any finite rounding has such a band, so this is inherent rather than a defect; the scorer also judged the "comment overclaims" sub-claim weaker than stated, and on re-reading the comment describes the `digits = 3` case specifically and does not claim elimination. Left as is. Its adjacent observation is recorded for whoever next touches this printer: when every non-NA `dcfi` in a ladder is tiny, `print.data.frame` switches to scientific notation (`8e-04`) beneath a note stating the rule as `-0.01`.
- **F7 (52) — a 133-column roxygen line** from the second pass, in a file otherwise ≤86. Linter territory by the taxonomy, so not actioned as a finding; the line was nevertheless rewrapped because F5's fix rewrote that same paragraph. Only the pre-existing 116-char `sprintf` at `R/ssm_sem.R:1002` remains.

**Post-fix re-verification.** All three doc surfaces re-checked and each carries all eight required items (two groups, ML, multivariate normality, Type-I-only, not-validated, the Table 5 direction, the non-ML route, and now the FIML route); nothing implies validation outside the envelope. `test-ssm_sem_groups.R` green; `document()` re-run; `R/ssm_sem.R` and `man/ssm_sem.Rd` ASCII-only. Note for the record: the AC5 matrix caught a real regression *I* introduced while fixing F5 — the reworded NEWS bullet had dropped "not validated" — restored before commit. Two further grep artifacts (roxygen wrapping "multivariate / normal" across lines; the Rd phrasing the caveat as "no cutoff here was validated for one") were probe failures, not content gaps, confirmed by reading the passages.
