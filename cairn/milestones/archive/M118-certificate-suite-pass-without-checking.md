# M118: Close three ways the certificate suite can pass without checking

**Status:** done (2026-08-31, PR #149 https://github.com/jmgirard/circumplex/pull/149)

**Goal:** Make three currently-green states of `tests/testthat/test-axes-certificate.R` red: a run that priced no case at all, a shipped pricing regression at the dyadic configuration, and a drift between the harness's written-down safety factor and the package's own.

**Outcome:** `cert_true_error()` now writes each case's disposition — `priced`, `skipped -- <reason>`, or `refused by the shipped pricing` — into the file-local `cert_dispositions` environment on every path, the skip branch recording before `skip()` unwinds and the priced branch only after all three `cert_rel()` calls; a test after the per-case tests reads that registry back, pins the case list at six, and fails when none was priced, naming every case and reason in its label. At the closed-form dyadic configuration, `axes_v_pricing()$corrected`, `$naive` and `axes_u_pricing()` are pinned to 97/128, 2 and 5/8 at a literal `tolerance = 4 * 2^-53`, beside the brackets rather than replacing them (M116 deleted the `expect_identical()` forms that had collapsed both bracket branches). `axes_certificate_safety_factor` is asserted identical to 10 beside the `cert_floor`/`cert_ceiling` comments explaining why neither reads it. `cairn/DESIGN.md` gained one Known-fragilities paragraph (six latent defects in the certificate's validation layers, sharing one ROADMAP row) and one Accepted-limitations paragraph (the quotient `cval` gap, `cert_hex()`, `axes_certificate_worst()`'s empty-certificate fail-open, `ratio_rel()`'s n-dependent measurement, M108's nine residues, M111 F16); the ROADMAP row that had absorbed findings since M108 was cut from 4,192 to 2,316 characters.

**Decisions:** none cross-cutting. Milestone-local: the quotient configuration's unchecked `cval` is recorded as an accepted limitation rather than hand-derived; the dyadic tolerance is a written-down literal rather than read from `cert_floor`; the unfixed residue splits between DESIGN.md's Known fragilities (keeps a row) and Accepted limitations (no row).

**Review:** Three-lens fan-out ([O] diff-bug, [S] blame-history, [S] prior-PR-comments). The prior-review lens found archived findings on the touched files and no regression of them; the GitHub inline-comment probe returned empty. Ten findings. Four fixed on the branch: the dyadic pins redden ten times tighter than the at-the-floor bracket beside them and the comment claimed the opposite (F1, measured — all three certificate fields sit exactly at `cert_floor` here, so the bracket asserts 4.4408921e-15 against the pins' 4.4408921e-16); the AC3 comment's drift direction was false in both directions (F2, 8 failures at 100 and 9 at 2); the ROADMAP row claimed M118 shipped while unmerged and misdated M116/M117's graduation (F4); DESIGN.md's "none reachable today" was false for two of the six (F5). Two filed as a candidate row: the all-skip platform now failing `R CMD check` with no release-risk record (F3), and the detector's unpinned disposition-string contract (F7). Four rejected: the `AC1:`/`AC3:` test-name collision (F6), the ulp-at-1.0 wording and `all.equal`'s scaling side (F8), the repeated skip boilerplate in the AC1 label (F9), and drift in T5's recorded byte figures (F10). CI green on macOS, ubuntu and windows. Nothing retired at hygiene: the M108-family LESSONS line's all-skip clause is now guarded in the one file it names, but the lesson is transferable craft for sites not yet written. `cairn/test-craft.md`'s M59 tolerance family gained the F1 clause.
