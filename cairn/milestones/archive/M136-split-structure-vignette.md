# M136: The structure vignette becomes four shorter vignettes

**Status:** done (2026-09-17, PR #169 https://github.com/jmgirard/circumplex/pull/169)

**Goal:** `evaluating-circumplex-structure` becomes four vignettes, each no longer than the older vignettes, with every sentence and chunk kept.

**Outcome:** The one structure page is now four `.Rmd.orig` sources: `evaluating-circumplex-structure` (the two questions, the CPM fit, its indices and model variants; Intermediate, 1635 prose words), `cpm-boundary-fits` (the boundary section behind a refit of the same model under the new chunk `cpm_refit` in its own masked region; Advanced, 1520), `ci-accuracy` (the published accuracy findings, `ssm_ci_accuracy()` and the checklist; Intermediate, 1971) and `structure-tests` (`fit_structure()` and `ipsatize()`; Intermediate, 1790). Every base chunk is in exactly one page byte for byte, the four masked regions moved with their chunks, and 486 base sentences map to 499 page sentences through 22 listed rewordings and additions. `tools/check-vignette-split.R` proves this against a base ref, reading its pairs from the milestone's Decisions section. Retargeted: `tests/testthat/test-cpm_boundary_vignette.R` (both reads, now runs `cpm_refit` and strips the section number before the help-page match), the roxygen links in `R/cpm_oop.R` and `R/fit_structure.R`, `VIGNETTES`, `_pkgdown.yml` (articles and navbar), the frame map and reading order 1 → 2, 1 → 3 → 4 → both Advanced pages in `test-vignette-frame.R`, the Level lines of the visualization and SEM pages, and the line-anchored citations in seven `cairn/references/` pages. Two figures moved to the pages that draw them. NEWS.md has one entry.

**Decisions:** M136-D1 lists the 17 reworded cross-references, the five added passages and the added chunk in the shape the guard reads. The plan gate chose four pages over three with a trimmed boundary section, and moving everything over listed cuts. The implement gate chose the page names, page 4 as the page the Advanced pages name first, and the refit in a new marked region. AC2 and AC4 were amended at the gate after two fresh-reader audits each (frame sentences excluded, added sentences and chunks listed, byte-for-byte chunks, both preamble chunks exempt).

**Review:** One pass. All seven criteria passed with fresh evidence: split guard exit 0, sweep exit 0 on each page, word counts under 2600, boundary test 15 pass, frame test 249 pass, staleness and width guards green, tests 0 failures (11283 pass), check 0 errors, 0 warnings, 0 notes. Three-lens fan-out: blame-history none; prior-review three items already on the frame-guards candidate row, whose promotion condition M136 met; diff-bug twelve. Fixed at the gate: a page 1 cross-reference "(defined on that page)" with no page in its paragraph, the plain-vignettes reference row that said "this page" of four, and the NEWS claim that every sentence was kept. Follow-up, folded into the promoted frame-guards row: the split guard's word cap is advisory, an empty added region passes, a listed text holding a second " => " splits wrongly, `strip_frame` blanks chunk comments, and the boundary test's scoping comment overstates on a one-section page. Rejected: the page 4 Wrap-up's "theoretical angles" (the body says RANDALL takes an order, not angles), the Advanced pages' read-first page (an implement-gate choice), page 1's Wrap-up trust claim (the plan moved every sentence), and "the two after it" (the reading order and navbar agree). The CI wait hit the session ceiling once and resumed. No lesson added.
