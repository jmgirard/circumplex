# M137: The axes-reliability and SEM vignettes each split into a core page and an advanced page

**Status:** done (2026-09-17, PR #170 https://github.com/jmgirard/circumplex/pull/170)

**Goal:** `axes-reliability` and `sem-based-ssm-analysis` each become a core page and an advanced page, each no longer than the older vignettes, with every sentence and chunk kept.

**Outcome:** Four pages replace two. `sem-based-ssm-analysis` (core, 2014 prose words) keeps sections up to what the parameters mean, and `sem-latent-contrasts` ("Latent Group Contrasts", 1665) takes group differences, the invariance-gated contrast, limitations and the literature, with one added chunk `lavaan-note-contrasts`. `axes-reliability` (core, 1047) keeps the worked example, the components and the correlation-matrix input, and `axes-reliability-caveats` ("Axes Reliability Caveats", 2534) takes the caveats section and the accepted-instrument Wrap-up. The caveats page ships as a live `.Rmd` because it prints no output, so it is not in `VIGNETTES`. `tools/check-vignette-split.R` passes on both splits against the merge base (axes 5 listed pairs, SEM 6). Retargeted: the three axes tests, the two roxygen `vignette()` pointers in `R/axes_reliability.R`, `VIGNETTES`, `_pkgdown.yml`, the frame map in `test-vignette-frame.R`, the pkgdown workflow comment, and the line citations in `wendt2019`, `browne1992`, `cheung2002` and `INDEX`. The contrast figure moved under its new name. NEWS.md has one entry.

**Decisions:** M137-D1: page names and one reading chain (SEM core, Latent Group Contrasts, Axes Reliability, Axes Reliability Caveats, terminal). M137-D2: the reworded and added sentences per split, in the split guard's pairs format. AC5 was amended at implement, because the planned grep matched the new page name and test file names.

**Review:** One pass, three-lens fan-out. All seven criteria passed with fresh evidence: split guards exit 0, word counts all under 2600, sweep, width and staleness guards exit 0, frame test 285 pass, tests 0 failures (11319 pass), check 0 errors, 0 warnings, 0 notes. Blame-history lens: no findings. Prior-review lens: one stale-citation claim, rejected because it read the `.Rmd.orig` while the citations name the rendered `.Rmd`. Diff-bug lens: 13 findings. Fixed at the gate: the `wendt2019` claims table and Open questions still carried master's line numbers, the `INDEX` browne1992 line said "both vignettes", and the caveats setup comment described chunks the page does not have. Follow-up row: the accepted-instrument material sits only in the caveats Wrap-up, the "Sections 2 to 5 take them in turn" lead-in sits inside Section 2, and bold lead-ins repeat the new headings. Rejected: the reading-chain prerequisite (M137-D1), a pointer that could name a section, NEWS "every code chunk kept" (true), a moved linear-parameter CI sentence, source lines past 80 columns, the Scope's section numbering, and old paths in D-033/D-034 (history). The first CI watch timed out with R-CMD-check pending; the resumed pass found all six checks green. No lesson added.
