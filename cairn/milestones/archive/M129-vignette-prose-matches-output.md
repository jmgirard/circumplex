# M129: Vignette prose matches the package's code and printed output

**Status:** done (2026-09-15, PR #162 https://github.com/jmgirard/circumplex/pull/162)

**Goal:** Four known wrong vignette claims are corrected, the SEM results tables fit on one line per estimate, and each sentence that quotes printed output matches the output after M127's print changes.

**Outcome:** Four vignette claims now match the code. In "SEM-Based SSM Analysis", only the scaled tier fixes the plane factors isotropic and orthogonal, and the strict tier frees them (`R/ssm_sem_syntax.R`). In "Evaluating Circumplex Structure", `print()` shows an uncertified displacement and its interval with a not-interpretable note (`R/ssm_oop.R`). The `fit_structure()` labels are "not clearly supported" in `print()` and "unsupported" in `summary()` (`R/fit_structure_oop.R`). The default `cpm_fit()` model estimates every angle but the reference scale's (`R/cpm_fit.R`). The SEM latent table uses `ssm_table(latent, drop_xy = TRUE)` with non-breaking spaces in its hidden kable chunk. In the pkgdown article at 1280 px, it is 776 px wide in an 800 px column, and no cell wraps. The evaluating and axes-reliability prose now names what the printed reports show. It names the `cert` column, the `Guardrail` lines and the `Verdict:` labels. It also names `Variance`, `INADEQUATE`, and `--` for a missing value. Gate fixes reworded the certification rule so that it keeps false-certification from clearly exceeding the benchmark, not "near its intended rate". They also named the coverage table in the `cert` bullet, tied the not-interpretable note to `ssm_analyze()`, and said that `Variance` and `Rotation` test interstitiality. NEWS.md has three documentation entries. The one-line table fix is scoped to the website, because the `html_vignette` rendering already fit. `cairn/references/plain-vignettes.md` rows no longer point at the removed doc-bug candidate row. `R/`, `src/` and `tests/` did not change.

**Decisions:** The implement question gate chose `drop_xy = TRUE` with non-breaking spaces over keeping X and Y with sideways scroll or smaller text. No cross-cutting decision.

**Review:** All five criteria had fresh evidence. AC2's selection was rebuilt: both knit-at-build articles print identical `#>` lines at 73218afa and at the branch head, and a fresh reader found no mismatch in 92 selected sentences. All 6 AC3 grep hits hold against `R/`. The AC4 search returns only the 3 axes-reliability missing-value lines. Tests gave 0 failures and 9512 passes, and the 9 warnings come from files the branch did not change. `check()` in the main checkout gave 0 errors, warnings and notes. A worktree run's `.git` note came from the worktree itself. The fan-out found 12 diff-bug findings and 2 blame-history observations, and the prior-review lens found none. The AC2 reader raised 10 doubts. The gate fixed F1 to F7. The intended-rate wording went, because D-007 gives the rule no nominal level. The `cert` bullet antecedent, the note placement wording and the `Variance` and `Rotation` purpose were fixed. The NEWS table scope, the NEWS axes filing and the older "code did not change" bullet were fixed. The stale ledger pointers were fixed. After the fixes, prose-sweep, the staleness check and `check()` passed again. Rejected: the shown and hidden SEM table captions differ, which predates the branch. The non-breaking-space substitution over `Profile` has no effect. Also rejected: wrap and style nits, an unmodified FIML `NA` sentence, a run-time number, a backtick nit, and 9 reader doubts on unmodified lines. The PR conversation was empty. The first CI wait hit its time limit, and the merge went ahead on green CI with approval re-given. All six PR jobs passed, including `vignette-precompute`, which is AC5's CI clause. Local commits after the last push (PR URL, AC5 tick, CI stop, resume approval) did not reach master, and this summary holds them. No lesson was added.
