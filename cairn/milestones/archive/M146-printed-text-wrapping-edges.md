# M146: Printed text wraps at its edge cases, and the CPM table drops Communality

**Status:** done (2026-09-21, PR #181 https://github.com/jmgirard/circumplex/pull/181)

**Goal:** Close the print-side items (ii)-(viii) of the code-box-width candidate row, so that the package's printed text stays inside the reader's console width at the edge cases those items name.

**Outcome:** `items()` and `scales(items = TRUE)` wrap item, Prefix and Suffix text with a hanging indent. The new internal `cat_item()` in `R/instrument_oop.R` does this. `scales(items = TRUE)` prints the notice of `iip32` and `iip64` once instead of stopping with "subscript out of bounds". `R/ssm_sem.R` has no `strwrap()` call. Its ladder notes, verdict line and verdict facts wrap through `wrap_prose()`. `axes_fit_scaled_note` is two elements, so the lavaan sentence starts a line. In `R/cpm_oop.R`, a `keep` argument holds "(ζ > 0.995," in the Heywood note as one unit. The bootstrap marker note hands its opening clause over as words. `cpm_display_results()` drops Communality from the printed table, and `fit$results` keeps it. `wrap_prose()` refuses a non-finite width and a tab in `prefix` or `continuation`. `ssm_ci_cat_line()` prints its label alone on empty text. NEWS has four entries, and the two CPM vignettes were re-rendered. Item (iii)'s premise was false: `strwrap()` already counts display columns.

**Decisions:** Plan-gate choices: drop Communality for all fits, fold the `scales()` crash in over a separate hotfix, and protect "(ζ > 0.995," as a unit rather than reword the note. The spliced-reason half of item (iv) was dropped as no defect. The work log records the falsification conditions.

**Review:** Three passes. Pass 1 returned the milestone because the AC4 sweeps covered one fixture (defect return 1). Pass 2 made an amendment return: AC2 promised double-width text to a formatter that takes no text, so AC2 was reworded. Pass 3 verified all seven criteria. Across passes, [O] diff-bug reported 16 findings and no code bug. [S] blame-history and [S] prior-review found no regressions. Fixed at the gate: the indent test that asserted nothing, and the width loop that missed vanished item text. Also fixed: the NEWS clause, narrowed to 16-character names, a named width message, and a comment. Three latent findings went to DESIGN.md Known fragilities, and the unwrapped CPM `Fit:` line joined the code-box-width row. The PR conversation was empty, and CI was green on all six checks. At hygiene, one LESSONS line was added and the stalest line (M25) was pruned to stay under the byte budget.
