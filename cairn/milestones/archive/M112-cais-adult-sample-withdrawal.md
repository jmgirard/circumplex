# M112: Withdraw the CAIS adult normative sample
**Status:** done (2026-08-30, PR #143 https://github.com/jmgirard/circumplex/pull/143)

**Goal:** Remove `cais` sample 2 from the shipped roster before v2.0.0 on the ground that an unusable sample should not ship — not on the metric question being settled — leaving the refusal machinery and the transcription record intact.

**Outcome:** `data-raw/cais.R` and `data/cais.rda` build one sample; `Norms[[1]]` is 8 rows all `Sample == 1`, `Norms[[2]]` one row. Asking for sample 2 now gets the unmatched-sample error, not the anchor-range refusal. D-040's roster-wide refusal and the disclosure's usable-count clause are untouched, their positive controls rebuilt on `off_metric_instrument()`, which mutates a shipped object rather than stubbing one. The anchor-range sweep expects an empty violation set with its domain pinned at 23 pairs. `?cais` and NEWS.md carry withdrawal notes naming neither `sample = 2` nor the three off-range means. The audit CSVs lose the two cais sample-2 rows and gain one standing non-exempt coverage gap — `sodano2006.md` still tables the withdrawn sample and no batch pass claims it — which the M79 and M80 drop-detection fences now assert by identity and subtract, instead of counting raw gaps and going vacuous.

**Decisions:** D-052 (withdrawal on unusability; annotates D-040). Milestone-local: the audit's standing gap is pinned by identity rather than exempted at the call site, so the transcription stays and a second gap is still a finding.

**Review:** Three-lens fan-out (user-facing tier). Blame-history and prior-review: no findings. Diff-bug: nine, none reaching the return floor. Eight fixed at the gate — the offending-scale assertions did not fence non-offenders (a mutant blaming all eight octants passed 22/22; after the fix it reddens 2), the audit's standing gap was recorded nowhere durable (now a section in `references/norms-audit.md`), four stale or falsified comments, the sweep domain pinned, an index coupling removed, and a frozen "the query is still open" claim dropped from `?cais`. One rejected: `?cais` naming a path `.Rbuildignore` excludes is deliberate and honestly labelled. Process notes: the seven acceptance boxes arrived ticked by the implement commit with no evidence and were all reset and re-verified; a `build_readme()` gate run's regenerated README plot PNG was swept into a review checkpoint by `git add -A` and reverted. Nothing graduated or retired.
