# M156: Peer-review status notices on the four package-original vignettes

**Status:** done (2026-09-25, PR #191 https://github.com/jmgirard/circumplex/pull/191)

**Goal:** Each vignette that teaches a method the package proposes without a peer-reviewed source opens with a notice that says so.

**Outcome:** A pandoc fenced div `::: {.alert .alert-warning role="alert"}` sits after the Level paragraph and before `## 1. Overview` on "SEM-Based SSM Analysis", "Latent Group Contrasts", "Bayesian SSM Analysis" and "Growth Models on SSM Parameters". Its text opens `**Not yet peer reviewed.**`, names the page's method (the latent SSM, the invariance-gated latent contrast, the Bayesian SSM recipe, the growth-model recipe on SSM coordinates), and says the package's authors have not yet published it in a peer-reviewed venue. pkgdown styles it as a warning box; the installed vignette shows a plain paragraph. `frame_notice` in `tests/testthat/helper-vignette-frame.R` lists the four pages; `test-vignette-frame.R` pins the opener count, the position between the Level paragraph's blank line and the Overview heading, the closer, the bold lead, and the absence of any `alert-warning` or `peer[- ]reviewed` line on the other ten pages, reporting each missing landmark per page instead of erroring out. The three pre-computed pages were re-rendered from `.Rmd.orig` with only the notice lines changing. NEWS carries a Documentation entry naming the four pages. Help pages and the three published-method vignettes stay in the "Peer-review status notices beyond M156's four pages" candidate row.

**Decisions:** none.

**Review:** three-lens fan-out. [O] eight candidates: fixed now — the position checks erroring on a missing closer or Overview heading (per-page landmark failure with `next`), the unlisted-page regex missing the hyphenated "peer-reviewed", a NEWS clause whose "its" pointed at the notice; rejected — a second notice under another spelling on a listed page (AC1 fixes the exact opener), "Its authors" in the notice (Scope wording), the authorship claim against Zimmermann & Wright (2017) (claim as written stands), `role="alert"` (Bootstrap's alert markup); noted — the suite reads pre-computed pages from `.Rmd.orig`. [S] history and [S] prior-review record: zero findings. Six criteria evidenced: suite 0 failures, check 0/0/0, staleness check exit 0, four renders with the div before `id="overview"`. One LESSONS line added at hygiene (`pkgdown::build_article()` leaves `docs/deps` stale).
