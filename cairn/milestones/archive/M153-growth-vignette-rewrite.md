# M153: The growth vignette on the helper workflow

**Status:** done (2026-09-24, PR #188 https://github.com/jmgirard/circumplex/pull/188)

**Goal:** Rewrite the growth vignette so that every code chunk is shown and the workflow is five short calls: `ssm_growth_data()`, `ssm_growth_formula()`, the pasted fit, `ssm_trajectory()` and `ssm_plot_trajectory()`.

**Outcome:** `vignettes/growth-ssm-analysis.Rmd.orig` Sections 1 to 6 rewritten on the three helpers, with the hidden reshape, draw function and per-wave loop gone; the only hidden chunks are the options chunk and the labelled `glmmtmb-note`. Both fits read `f_glmmTMB$formula` and `f_glmmTMB$dispformula`. The wave-2 `ssm_draws()` demonstration was dropped and the cross-block check became prose about the helper's refusal; the VarCorr print stays. New Section 10, placed after Caveats because the trajectory print cites Section 7 by number, shows the nlme call and a brms fit `eval = FALSE`; `data-raw/growth-brms-draws.R` (seed 20260716, brms 2.23.0) writes `vignettes/growth_brms_draws.rds`, 4000 by 6 `b_` columns, read in an echoed chunk and summarized by `ssm_trajectory(draws = )`. Measured gaps to the glmmTMB table: `x_est` 3.3e-4, `y_est` 5.0e-4, intercept-draw correlation 0.032; the Section 5 table equals the previous render at two decimals. `devel/m27-growth-recipe.R` builds its call from the builder and its table from `ssm_trajectory()`, with circular containment. NEWS Documentation bullet; `?simulated_growth` names the vignette under See also. Gotcha: with `sigma ~ 0 + dv`, brms's `^b_` also matches the three `b_sigma_` columns, so the generator selects the six names exactly.

**Decisions:** none milestone-local. Implement gate: Section 10 placement, and the nlme demonstration as the printed call only.

**Review:** claim audit at implement read 52 claims and corrected 5, one twice. Three-lens fan-out: prior-review lens none; history lens three notes (the printed cross-block chunk and its guidance intentionally superseded, the brms intercept correlation's sign flip inside the bound); diff-bug lens 22 findings, none a criterion failure. Fixed at the gate (7c1f721a): O1 caution wording, O2 the REML note under draws stated inapplicable in prose, O4 "almost never exactly zero", O6 and O7 the nlme sentence made procedural against `test-ssm_growth_parity.R`, O8 residual-variance wording, O9 generator comment on `refresh = 0`, O16 defaults sentence, O17 call count, O18 recipe containment on the circle, O22 "each input row". Follow-up: O5 (Section 7's remedies and the `draws =` path) folded into the caution-wording candidate row. Rejected: O10 the shipped `.rds` (parity with the Bayesian fixture), O11 the echo sweep's `readRDS` hit, O12 to O15, O19 to O21 pre-existing or unmodified text. CI green on six checks; check status OK. Hygiene: the M150 row pruned for terminal-row retention; no lesson added, LESSONS at its byte budget.
