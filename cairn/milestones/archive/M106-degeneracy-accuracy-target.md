# M106: Price the degeneracy refusal region on a stated statistical argument

**Status:** done (2026-08-23, PR #135 https://github.com/jmgirard/circumplex/pull/135)

**Goal:** Give `axes_degeneracy_tau` a stated statistical footing, set it from that footing, and fence the κ band it governs.

**Outcome:** τ is now `axes_degeneracy_delta_star / axes_degeneracy_calibration_ceiling` = 1e-4/10 = 1e-5, the accuracy target and the calibration ceiling documented beside it as separate quantities, so the constant's stated definition and its enforced cap no longer differ by the slack factor. The refusal threshold moves κ 1.37e4 → 4.3e4 at p = 24 (7.5e4 at p = 8, 1.06e5 at p = 4): a near-duplicate pair at r = .9999, whose SEs an exact oracle measures accurate to 2.0e-13, now computes, while RR18's counterexample B (κ = 6.65e6, SEs 3.4% wrong) still refuses. `axes_degeneracy_hint()` gives the `"ill_conditioned"` refusal an actionable clause at both surfaces — the condition number where λmin > 0, numerical rank-deficiency otherwise, plus every item pair whose correlation forces the refusal by Cauchy interlacing on its own account, basis-free rather than read off an eigenvector that multiplicity leaves undefined. `devel/degeneracy-oracle/exact_oracle.R` gains a reachable-geometry family (p = 4, 8, 9, model-implied) with its own window asserting the opposite property to the fixture sweep's: attainment 6.8e-8 to 3.8e-7, six decades loose. A derivation block beside the constant states the target through three channels and eight premises; certifying that block's prose exited to a follow-on at the descope.

**Decisions:** D-048 (τ = δ*/C, superseding D-044's floor), D-049 (RR20's repaired grounds, annotating D-048), D-050 (D-048's trigger (i) met in full, so the per-fit certificate is due rather than conditional). Milestone-local: the diagnostic scoped to the ill-conditioning literal alone; pair naming derived from the criterion rather than an eigenvector; one n-free τ shared by both surfaces.

**Review:** Four rounds, three fresh-context lenses each. Rounds 1–3 returned on defects (19, 20 and 11 findings); two amendment returns repaired AC3's unsatisfiable wording and AC4's premise the work itself falsified; RB20/RR20 escalated round 2's derivation findings. At the third return the thrash rule fired on both triggers and Jeff chose descope: AC1 exited to a follow-on, the ten directed findings repaired. Round 4 verified all seven live criteria fresh; the two [S] lenses found nothing, [O] returned 11 findings, none meeting the return floor — eight fixed at the gate, one routed to the follow-on row, one left as recorded history, one taken as a superseding work-log line. Nothing retired from LESSONS.md.
