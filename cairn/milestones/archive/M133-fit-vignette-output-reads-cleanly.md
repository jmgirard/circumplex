# M133: Fit vignette output reads cleanly

**Status:** done (2026-09-16, PR #166 https://github.com/jmgirard/circumplex/pull/166)

**Goal:** In the evaluating-circumplex-structure vignette, readers see the CPM table in one block, a notice before each Hessian warning, and the short accuracy report first.

**Outcome:** `print()` and `summary()` for `cpm_fit()` results print a display copy of `results` from `cpm_display_results()` in R/cpm_oop.R. Its headers are Scale, Theory, Angle, lci, uci, Zeta, lci, uci, Communality, plus VarRatio under free scaling. The returned `results` keeps its names. `print.circumplex_ci_accuracy()` wraps its settings heading with `wrap_prose()`, and that heading left the M131 width ledger in test-print-width.R. The test helper `expect_cpm_table_one_block()` (helper-cpm-table.R) checks one header row, one row per scale and values equal to `cpm_round_df(results)` at width 77. It runs on all seven fixtures of test-cpm_summary_markers.R and on the test-cpm_api.R fits. test-ci_accuracy_print_width.R checks the four accuracy fixtures at width 77. The structure vignette names *When a fit sits at a boundary* before the three chunks that print the ill-conditioned-Hessian warning, shows `print(acc)` first, and holds `summary(acc)` and its reading guide in a new "The full report" subsection. Snapshots re-captured, vignette re-rendered, NEWS.md entries added.

**Decisions:** Announce the Hessian warning over investigating its cause. Shorter printed headers over dropping Communality or a smaller site font. `print(acc)` first over shortening `print()`. Header set with plain lci/uci pairs (66 columns on jz2017) over Angle_lci/Zeta_lci with Comm.

**Review:** Two passes. Pass 1 returned on AC2 (one-block assertions missing for five fixture/method pairs), fixed by T5, with T6 fixing the NEWS heading contradiction. Pass 2 ran the three-lens fan-out and passed all seven criteria. Tests FAIL 0 / PASS 11034, check 0 errors, 0 warnings, 0 notes, both vignette guards and all six PR checks green. At the gate [O] a was fixed on the branch: the M131 NEWS entry said column headers were unchanged. [O] b, confirmed by probe, is that free scaling with 16-character scale names puts VarRatio in a second block. It replaced item (vii) of the output-width candidate row, which M133 resolved. The other findings were rejected as wording, test-style or out-of-scope points. No lesson added.
