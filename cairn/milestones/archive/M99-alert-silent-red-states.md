# M99: Alert on the red states the gate ignores

**Status:** done (2026-08-21, PR #128 https://github.com/jmgirard/circumplex/pull/128)

**Goal:** Make the master-red alert fire on every way a watched workflow's push run can end badly on the default branch, not only `conclusion == 'failure'`.

**Outcome:** `.github/workflows/master-red-alert.yaml`'s job gate now admits by EXCLUSION — `!contains(fromJSON('["success","cancelled","skipped","neutral","stale"]'), github.event.workflow_run.conclusion)` — so `timed_out`, `startup_failure`, `action_required` and any conclusion GitHub adds later alert, while the five named benign endings (green, or no verdict) stay quiet, each with its reason in the workflow comment. `tools/check-master-red-alert.R` re-pins the whole expression in `EXPECTED_IF`, written independently of the workflow so the two sides cannot agree by construction, and — added at the review gate — pins `on.workflow_run.types` to exactly `completed`: exclusion made a null-conclusion payload (`requested`/`in_progress`) alert on every run, a hole the widening itself opened. The workflow header records, as a dated observation, what the widening does not establish: a run that never starts may deliver no `workflow_run` event, and `workflows:` matches a `name:` an unparseable file cannot declare. No R package code touched; `tools/master-red-alert-dryrun.R` changed in prose only — new fixtures were declined on the merits at the plan gate, since the shell body has no conclusion-dependent branch.

**Decisions:** none promoted. Plan gate weighed and recorded three alternatives in the work log: an allowlist of three named conclusions (rejected — reopens the same silence for the next ending), conclusion-varying dry-run fixtures (rejected — no branch to exercise), and a scheduled sweep as a second detector (deferred — its own design, audit, and dedupe surface).

**Review:** One pass, no returns; all four criteria ticked against fresh evidence. [S] prior-PR-comments and [S] blame-history both clean. [O] diff-bug returned 12 ranked items: F2 fixed at the gate (the `types:` hole, mutation-proved by two mutants); F4 fixed (the `skipped` justification was factually wrong — `paths-ignore` yields no run at all, not this conclusion); F8 fixed (two stale phrases, plus a work-log correction — the T2 sweep claim read as exhaustive and was not); F1 to a new candidate row (the alert and `PROFILE.md`'s master-watch now classify the same run oppositely); F3 rejected at the maintainer's call (no local GHA-expression validator; `actionlint` is a dependency decision); F5/F6 accepted and recorded verbatim under AC1 — a whole-string pin establishes the equality check is wired, not the expression's semantics; F7 closed by measurement (the `master-red` label does not exist yet); F9/F11/F12 corroborating verifications. Gate: `check()` 0/0/0, `pkgdown` clean, `document()` no-diff, suite PASS 8395, `cairn_validate` exit 0. Hygiene: M99's lesson consolidated into the M60-family line as its mirror clause; the M75 lesson trimmed to its uncovered remainder, its instance now owned by `references/horowitz2003.md`; the graduated M97 `CONTRIBUTING.md` tombstone pruned to hold the ROADMAP line cap.
