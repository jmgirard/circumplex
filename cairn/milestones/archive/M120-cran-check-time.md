# M120: Bring the Windows CRAN check under 8 minutes

**Status:** done (2026-09-02, PR #151 https://github.com/jmgirard/circumplex/pull/151)

**Goal:** Cut the Windows CRAN check cost under 8 minutes without deleting coverage, after CRAN's pretest returned a 24-minute check-time NOTE on r-devel-windows-x86_64 for v2.0.0.

**Outcome:** Seven vignettes ship pre-computed — sources moved to `.Rbuildignore`d `vignettes/<name>.Rmd.orig`, knitted by `tools/precompute-vignettes.R` with a `fig.alt` hook, figures under `vignettes/figures/`. `.github/workflows/vignette-precompute.yaml` re-knits on push and fails on a stale copy via `tools/check-vignette-staleness.R`, byte-exact except digits and blank-run padding in knitr output lines inside a declared `precompute:volatile-numbers` region (three, all in `evaluating-circumplex-structure`, where an ill-conditioned CPM fit moves across BLAS implementations). `skip_on_cran()` on 416 blocks chosen top-down by measured cost; nothing deleted, CI runs everything. All 32 exported functions losing CRAN coverage keep a live block, with two new files: `test-pole-values.R` (angular *values* at the 0/360 pole, not relational properties, which survive a uniform branch shift) and `test-plot-cran-guards.R` (numbers the built plot layers carry, absolute bounds not relative `tolerance`). Roxygen examples pass `boots = 200`. Four `tools/m120-*` instruments enumerate the domain, prove skipped blocks live off CRAN, and redden 22 block-defect pairs under ten planted defects. No runtime change: `R/` diff is roxygen only, `src/` untouched. Local `--as-cran` tests 259 s -> 17 s, vignettes 40 s -> 5 s, total 6 min 14 s -> 1 min 34 s; win-builder R-devel 3 min 23 s - 4 min 23 s, Status OK. Tarball 5.61 -> 6.21 MB.

**Decisions:** AC4 amended mid-implementation after two fresh-context audits — the suite's existing 0/360 blocks assert relational properties that survive a uniform branch shift, so the criterion now demands value-asserting pole blocks and scopes each wrap defect to the angular path its function uses.

**Review:** Three-lens fan-out, two passes. First pass returned the milestone on F1 (the byte-exact staleness guard could not pass), F2/F3/F4/F6/F7 carried back, all repaired in T8-T11. Second pass met all six criteria on fresh evidence and passed the gate; 24 findings triaged. Fixed at the gate: stale check-time figures in `cran-comments.md`, two condition numbers shipping outside any volatile region, a dead test helper. Deferred to two new candidate rows — CRAN-dark boundary invariants past milestones added, and m120 instrument strength. Tarball weight recorded; a README-figure finding rejected as verified in sync.
