# M139: The growth vignette shows the correlated person block and a zero check on the cross block

**Status:** done (2026-09-17, PR #172 https://github.com/jmgirard/circumplex/pull/172)

**Goal:** The growth vignette shows the reader that the fit is joint and gives one check the reader can carry to their own fits. The recipe script gets the same check.

**Outcome:** `vignettes/growth-ssm-analysis.Rmd.orig` gained a `varcorr` chunk (`glmmTMB::VarCorr(fit)`, 38 columns, no width exemption) after the `fit` chunk, with prose naming the person block's three correlations as joint structure and explaining the `Residual NA` row (residual variances live in `dispformula`). Section 5 gained a `cross-block` chunk, `any(V[c("dvx", "dvx:wave"), c("dvy", "dvy:wave")] != 0)`, printing `TRUE`, with prose stating that separate fits assembled block by block hold exact zeros in every cross block, that the check is structural (three of the four entries here are residue near 1e-20), that it has no power outside the exact-zero case, that a nonzero block does not show the model is right, and that the four names are this model's. Section 4's warning ends with a sentence pointing to the check. `devel/m27-growth-recipe.R` replaced `stopifnot(abs(xy_cov) > 0)` with a labeled print of the 2x2 cross block and `stopifnot(any(V_xy != 0))`. NEWS entry under the development version. Vignette re-rendered; figures unchanged.

**Decisions:** none milestone-local. Implements D-060 recommendations 2 and 3. Mid-implementation amendment (mini gate): the check moved from `all(... != 0)`, which passed only by floating-point accident, to `any(... != 0)`, the not-identically-zero test RR23 asked for; AC4 reworded to state both directions.

**Review:** AC1-AC7 verified fresh (tests FAIL 0 / PASS 11319; check Status OK, 0 notes; staleness and width guards exit 0; recipe exit 0; guard probed at an all-zero block and a 4e-21 entry). Gate: `cairn_validate` clean; `devtools::document()` could not run (installed roxygen2 8.0.0 under the pinned 8.1.0), recorded as an environment fact with no roxygen touched. Three-lens fan-out: blame-history and prior-record lenses zero findings; diff-bug lens eight. Fix now (commit 5f48cdcd): O1 residue entries called "tiny" as if estimated, O2 the check's lack of power outside the exact-zero case unstated, O3 "where the fit is joint" located in the random-effects block, O4 an unhedged "only when" in NEWS, O6 hardcoded column names with no note, O7 unlabeled recipe print. Rejected: O5 ("directly after" holds in chunk order). Noted: O8 (`Residual NA` explanation verified correct). PR conversation empty at the resumed gate. Nothing graduated or retired.
