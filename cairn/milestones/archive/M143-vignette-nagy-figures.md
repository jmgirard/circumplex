# M143: The visualization vignette's latent-circumplex figures follow Nagy, Etzel and Lüdtke (2019)

**Status:** done (2026-09-20, PR #177 https://github.com/jmgirard/circumplex/pull/177)

**Goal:** Redraw the circle figures of the visualization vignette's section 7 on the M142 cartesian canvas in the form of Nagy, Etzel and Lüdtke (2019, Figures 3 and 6): estimated angles only on the rim, measures as labelled points joined to the origin, one annotated measure, and the confidence ellipse with its tangent and distance lines.

**Outcome:** Section 7 of `vignettes/advanced-visualization.Rmd.orig` is redrawn on the cartesian canvas with `angle_labels = TRUE`. `latent-ticks` is the canvas alone, its ticks at the angles `cpm_fit()` estimated and the theoretical angles only in the table above. `latent-vectors` draws the five PANO measures as black points joined to the origin, text labels keyed by measure name. The new `latent-measure` annotates SCZPD (187°) with an arc sweeping increasing displacement from 0 and its `d_est` and `a_est` labels. The echoed `ellipse-lines` chunk computes, from the one-row `ssm_ellipse_data()` result and its level, the two tangent angles from the origin and the nearest and farthest boundary distances (tangents from the slope quadratic, distances from a grid refined by `optimize()`, `NA` tangents when the origin is inside). `ellipse-figure` draws them as dashed tangents to the rim and dotted distance paths over the ellipse, the wedge at alpha 0.15. New tests: `test-vignette-latent-plots.R` inspects the purled plot objects through the `vignette_chunk()` helper in `helper-vignette.R`; `test-vignette-ellipse-lines.R` holds three oracles (the `GeomSsmEllipse` outline at n = 3600, closed-form circular ellipses, closed-form rotated non-circular ellipses) plus origin-inside cases. Phrase test re-pinned to four sentences and the Nagy reference; the Gurtman (1992) entry removed; source note `cairn/references/nagy2019.md`; one NEWS entry.

**Decisions:** none cross-cutting. Milestone-local: estimated angles only on the rim; both measure figures; the lines as echoed vignette code with the geom aesthetic parked; the arc sweeps increasing displacement; SCZPD is the annotated measure; black points, text labels, no legend. One amendment return on AC6: the split guard dropped as inapplicable, the sweep's file arguments supplied.

**Review:** three-lens fan-out on head 527f21e5. [S] blame-history: none. [S] prior-review: one, P1 (`lines` masking `graphics::lines()`, the M140 `pairs` lesson). [O] diff-bug: 17 findings; 11 fixed on the branch (direction tolerance loosened to 1e-4 with its reason, a third closed-form non-circular oracle, three prose corrections, a heading, `lines` renamed `four_lines`, rewraps, one `level` object feeding chunk and layer, a real-roots comment, `expect_within()` reporting the observed gap, label placement keyed by name) and 6 rejected with reasons (an unreachable `polyroot()` zero, tangents to the rim per AC4, `%% 360` per AC3, no origin-inside guard in the figure chunk, `vignette_chunk()` fence robustness, the 0.1° outline tolerance per AC3). Re-verification clean; CI green on all six checks. Nothing graduated or retired at hygiene; the exported tangent-helper candidate already sits on the covariate-extension ROADMAP row.
