# M104: Graduate the matured verification-craft families out of LESSONS.md

**Status:** done (2026-08-22, PR #133 https://github.com/jmgirard/circumplex/pull/133)

**Goal:** free durable byte headroom in `cairn/LESSONS.md` by graduating six matured verification-craft families into a repo-local doctrine module, landing the two lessons M102 could not.

**Outcome:** `cairn/test-craft.md` is new and holds six families moved byte-for-byte under the **maturation** exit — M13 (mutation proves a guard), M82 (the mutation harness itself lies), M59 (a tolerance calibrated on one machine is not a tolerance), M60 (a general check drops what the narrow one refused), M75 (an expectation from the implementation's own expression), M95 (a check can pass by not checking). `cairn/LESSONS.md` went 45 lines / 19,996 bytes → 40 / 14,861, keeping one pointer line naming all six headlines; its header now admits a pointer line as a line-format exception. M102's greedy-span and YAML-plain-scalar lessons landed folded into the graduated M95 entry. The parked-lessons candidate row is a tombstone. The file is uncapped by M104's choice, not by rule — nothing measures it.

**Decisions:** D-046 (the pass runs standalone; supersedes M44's forced-time holding, narrowly — the maturation exit cannot be reached from a shipping milestone's scope, so post-merge hygiene stays the ordinary route for the enforcement and ownership exits).

**Review:** single [O] diff-bug lens (internal tier, docs-only diff), 12 ranked findings, every factual claim verified against the record before triage. Seven fixed on the branch: F1 (D-046's ellipsis elided the antecedent of "that milestone's", inverting M44's holding), F2 (D-046's "freed nothing" was false — the M100 stamp records that pass retiring under the *ownership* exit), F9 (four hygiene passes, not three), F4 (tombstone misdated its own subject), F3 (ROADMAP stamp asserted what the same commit falsified), F5 (a caps rationale no record holds, licensing the uncapped status), F7 (pointer line broke the format its header declares). F8 recorded not fixed — the T6 byte figures went stale, the M87 clause recurring inside the milestone that moved it. F6 (ownership exit dismissed on capacity, not content) and F11 (reachability rests on one unenforced pointer no gate measures) → candidate row at Jeff's call. F10 discharged here; F12 no defect. Gate: `cairn_validate` 16/16 PASS; `devtools::check()` 0/0/0; both master watches green; both alert audits exit 0. PR CI absent by `paths-ignore: cairn/**`, not red. Defect returns: 0.
