# M7: v2.0.0 CRAN release preparation

**Status:** done (2026-09-03, PR #64 https://github.com/jmgirard/circumplex/pull/64)

**Goal:** Ship the accumulated M2–M5 work (inference, visualization, Browne model + CI trustworthiness, structure tests, SEM) plus the CIRCUM free-scaling family, the longitudinal SSM builds, and the visualization expansion to CRAN as one v2.0.0 release, with no target date — it ships when the bundle is complete and validated (D-008).

**Outcome:** **circumplex 2.0.0 is on CRAN** (page reads Version 2.0.0, Published 2026-09-02). DESCRIPTION bumped to 2.0.0 with `Depends: R (>= 4.1)` (the real floor, per D-014/D-019, finally declared); NEWS.md consolidated under `# circumplex 2.0.0`, breaking changes first; `cran-comments.md` rewritten claim-by-claim against the repo, then trimmed 115 lines → 29 at Jeff's judgment. The bundle grew by dependency re-pointing across fifteen milestones (M16→M18→M19→{M20,M21}→M22→{M25,M26,M27}→viz track→{M119,M120}) rather than by re-planning M7 itself. Two submissions: 2026-09-02 03:00 UTC on `3d07b024`, rejected at CRAN's incoming pretest for a 24-minute Windows check time; and 2026-09-02 21:11 UTC on `ad475b65` after M120 brought that under 8 minutes, which passed the pretests and published. Jeff ran `submit_cran()` both times; nothing was self-submitted. Post-merge, hotfix PR #152 answered CRAN's own check farm (a cos(225°) ulp difference on macOS x86_64) and sits on master unreleased.

**Decisions:** The 2026-07-19 deferred-AC4 exception — review verified AC1–AC3 and merged, then deliberately did **not** tick AC4, set `done`, or archive, because `submit_cran()` cannot run from an unmerged branch. M7 returned to `in-progress` post-merge and reached `done` only once CRAN published. Recorded at the time as an exception, not a precedent. Cross-cutting: D-008 (no release date), D-012, D-018.

**Review:** Reviewed 2026-07-19 (three-lens fan-out), PR #64 squash-merged at `39a97013` with CI 9/9 green and `check(manual = TRUE)` 0/0/0. Actioned: F1 (92) — the Rd-LaTeX regression guard skipped under `R CMD check` and so ran in no gate that ships a release; fixed and verified by mutation (a planted `->` in `man/octants.Rd` reddened it). F2 (55) fixed alongside. F4 (78, NEWS undercounted the new analysis families) and F5 (50, the Imports-count sentence never named the `parallel` addition) fixed at Jeff's instruction at the gate though below the actioned bar. F3 (5) rejected as factually wrong — `git show v1.2.0:DESCRIPTION` shows 7 Imports, not the 8 the reviewer assumed. AC4's evidence was recorded post-merge on 2026-09-03 against the live CRAN page. Nothing graduated or retired at hygiene.
