# M110: Correct the calibration-domain claim in the accuracy target and its shipped surfaces

**Status:** done (2026-08-24, PR #140 https://github.com/jmgirard/circumplex/pull/140)

**Goal:** State the accuracy target's calibration domain at the worst measured geometry as well as at the anchor, so no shipped surface implies the domain reaches 1.7 decades past the published sample-size ceiling at every geometry.

**Outcome:** The `1e-4` target's channel-3 noise-dominance reading now carries both tenth-margin endpoints with the coefficients they belong to, at every surface that states one: the derivation block at `R/axes_corrected_se.R:519-527`, both roxygen sites in `R/axes_reliability.R` (`:731`, `:1049`), the regenerated `man/axes_reliability.Rd`, and `NEWS.md:79`. The endpoint scales as `n = 1e6 * a^2`, so the anchor `a = 1/sqrt(2)` gives `n = 5.0e5` — 1.7 decades past the `n ~ 1e4` of published circumplex correlation matrices — while the worst measured `a = 0.045` gives `n = 2.0e3`, below it; each surface now says which of the two falls below the ceiling, and the roxygen's "above the endpoint" clause is geometry-relative rather than bound to one figure. A help-page guard at `tests/testthat/test-axes-reliability.R:3236+` reads the Rd through the dual man/-or-`Rd_db()` source and requires each figure within a 120-character window of its coefficient, asserting occurrences rather than text chunks. No constant moved; no principle changed.

**Decisions:** none. D-048 and D-049 hold — the constants stand and this repairs only the wording of the domain they are calibrated over.

**Review:** One round, three fresh-context lenses (user-facing tier). All three criteria verified with fresh evidence; consistency gate and CI clean. Twelve findings, none meeting the return floor. Five fixed at the gate: the new guard's sentence split was not sentence-local (the `\value` "sentence" ran 2,635 characters, so a coadjacency claim was really a same-section claim) — repaired to a bounded window and proved discriminating against the pre-fix form on that exact plant; the NEWS insertion's unpaired em-dashes, its dropped "up to about", its unglossed coefficient, and the ambiguous "above the endpoint". One routed to M111: `R/axes_corrected_se.R:537`'s `6.5e-6` derives from a = 0.046, not the 0.045 the block states (pre-existing). Six rejected with reasons, all logged. One lesson extended the M95 family in `cairn/test-craft.md`; nothing retired.
