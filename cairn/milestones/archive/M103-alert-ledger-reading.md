# M103: Record what the alert's per-run ledger implies about its watched-workflow list

**Status:** done (2026-08-22, PR #132 https://github.com/jmgirard/circumplex/pull/132)

**Goal:** Write into `.github/workflows/master-red-alert.yaml`'s header the reading of M102's per-run ledger that M102 banked the measurement for but deliberately left unrecorded.

**Outcome:** The alert's header now carries the reading beneath the ledger. Its claim: for the five broken runs that ran while the path-spelled subscriber was live (32545583419, 32545779577, 32545943649, 32545999116, 32546052474), a subscriber listing that run's own reported name produced no run for it, while 32545706555 — a VALID run declaring no `name:`, so reporting the path too — did produce one (32545711782). That is the cell M101 lacked, where name spelling and file validity varied together in every case. Three refusals ship with it: it does not claim no `workflow_run` event was delivered; it does not price any edit to this alert's own `workflows:` list, since what was driven was a separate one-spelling file and `path-match-probe.yaml` is not this alert with a spelling swapped; and it does not establish why those runs reported the path (M101's 32540622138 declared a `name:` and still reported it). Four limits are stated: no `startup_failure` in the window, the two one-spelling subscriber files, the probe-not-this-repo scope with M101's and M102's equivalence records attributed separately, and the subscriber's d0cc1cd→bbf43b2 presence window with the two boundary runs marked unseparable. Every run-level cell was re-read from the GitHub REST API (observed 2026-08-22). The diff is comment-only: `on:` to EOF byte-identical to master, so `EXPECTED_IF` is unmoved. The ROADMAP lineage row drops to a pointer with item (b) restated as what stays unmeasured.

**Decisions:** none milestone-local; nothing promoted to DECISIONS.md — the reading is a finding about evidence, not a choice, and the header owns the alert's rationale (plan gate).

**Review:** Two rounds, one defect return plus one amendment return. Round 1: the ledger preamble still declared the reading "deliberately NOT recorded here" five lines above the reading — the M56-family stranded-prose shape, invisible because the diff had zero deletions. AC2 was judged NOT MET with the criterion the primary defect: its line-level test cannot be satisfied by hard-wrapped prose (the M114 unsatisfiable-as-written shape). It was amended to a sentence-level test over five named per-run cell types, cleared by two fresh readers; AC4 was HELD rather than widened, per the return-adjacent direction rule, and the text repaired instead. Round 2: all seven criteria met, eight findings, seven actioned and one a non-defect; blame-history and prior-PR lenses returned zero defects, each having independently re-verified every cited cell. Before the text ever reached the workflow, three T3 adversarial rounds stripped the counterfactual that sank M102's two attempts. Gate: `test()` PASS 8395, `check()` Status OK 0/0/0, both alert audits exit 0, `document()` no diff, `pkgdown` clean, `cairn_validate` clean, CI green. No lesson added or retired: the stranded-prose recurrence is already owned by the M56-family line, and `LESSONS.md` had 4 bytes of headroom. The plan-gate criteria audit ran in REDUCED mode (internal tier), which by rule omits the satisfiability question — that omission is why an unsatisfiable criterion reached review, and it is a plugin-level observation, not a repo lesson.
