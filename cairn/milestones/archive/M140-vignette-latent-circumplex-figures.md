# M140: The visualization vignette draws a fitted quasi-circumplex, measure vectors and the correlation function from existing layers

**Status:** done (2026-09-20, PR #173 https://github.com/jmgirard/circumplex/pull/173)

**Goal:** Add a section to `advanced-visualization` that builds three figures from a `cpm_fit()` and an `ssm_analyze()` result with the layers the package already exports: the estimated scale angles as rim ticks, measure locations as vectors from the origin, and the fitted correlation function against angular separation.

**Outcome:** `vignettes/advanced-visualization.Rmd.orig` gained section 7, "The latent circumplex from a CPM fit", with four echoed chunks and no hidden chunk. `latent-fit` runs `cpm_fit(jz2017, scales = PANO(), angles = octants(), ci_method = "analytic")` inside a precompute volatile-numbers region. The fit is ill-conditioned and Heywood-type, and the prose names both and points to the boundary page. `latent-ticks` draws each estimated `Angle` as a two-point radial `geom_ssm_path()` tick (amplitudes 0.94 and 1) on a `ggcircumplex(angles = Angle_theory, labels = PANO())` canvas. `latent-vectors` adds five measure vectors (NARPD, ASPD, HISPD, AVPD, SCZPD) from the origin to `a_est` at `d_est` from `ssm_analyze()`, with the ticks scaled by `amax = 0.3`. `latent-corfun` draws `corfun` over 0 to 180 degrees, the upper triangle of `matrices$R` as filled points at the inline modular separation `abs(((a - b + 180) %% 360) - 180)`, and `matrices$Phat` as hollow points with a shape legend. The prose explains the attenuation gap through the `Zeta` column. Sections 7 to 9 were renumbered 8 to 10, the Overview map gained one sentence, and Browne (1992) joined References. `tests/testthat/test-vignette-latent-figures.R` asserts the four AC5 phrases in the rendered page and no `echo = FALSE` or `include = FALSE` fence in the section's source. NEWS entry under the development version. The new `ssm_analyze()` call consumes RNG, so section 8's bootstrap PNGs re-rendered with different draws (point estimates unchanged).

**Decisions:** none milestone-local. Gate choices: analytic CPM intervals (only point estimates drawn), five spread measures, and the Hessian warning shown inside a volatile region rather than hidden. AC6 was amended at a mini gate to admit the heading renumbers, the Overview map edit and a new References entry that `test-vignette-frame.R` forces, and two re-audits refined its wording. The `cpm_plot_curve()` export and the ellipse layer stay out (candidate row and M141).

**Review:** AC1 to AC7 verified fresh twice, the second time after master moved (hotfix #174) and was merged in. The purled section run under `Rscript --vanilla` exits 0. The plot objects were inspected: 16 tick rows at `Angle` with breaks equal to `Angle_theory`, 10 vector rows from the origin to `a_est` at `d_est`, 28 points equal to `matrices$R` at the modular separation, and hollow points equal to `Zeta_i * Zeta_j * corfun(sep)`. Phrase test 7 passes, three vignette guards exit 0, check Status OK with 0 notes, source diff bounded to five hunks. Gate clean (`cairn_validate`, `document()` no diff, `check_pkgdown()`, master watches green, alert and branch-protection audits exit 0). Three-lens fan-out: the prior-record lens found nothing, the history lens two (S1 RNG re-render, rejected, and S2 test guard, fixed), the diff-bug lens fifteen. Fix now: O1 Heywood note, O2 PA fixed angle, O3 eight sentence splits, O4 attenuation wording, O6 `NA` intervals, O9 `Zeta` column, O10 shape legend, O11 warning causality, O12 signed residual, O13 test guard, O14 `pairs` shadowing, O15 `ggcircumplex()` canvases. Kept with an added sentence: O5 (hollow points are reproduced, not observed). Rejected: O3's ninth sentence (an AC5 phrase), O7 echo-sweep flags, O8/S1 a seed outside the diff bound. PR conversation empty at both gates, CI green on the merged head. Hygiene retired the M106 `pgrep` lesson (the rulebook's wait rule owns waiting) and corrected the M105 merge-guard lesson in place.
