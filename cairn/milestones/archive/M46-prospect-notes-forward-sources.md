# M46: Prospect notes for the four forward-looking shelf sources

**Status:** done (2026-07-21, PR #72 https://github.com/jmgirard/circumplex/pull/72)

**Goal:** Triage the four deliberately-shelved forward-looking sources into one committed survey synthesis note plus four candidate ROADMAP rows — per the supply-push exploration doctrine — so a later planner starts warm without owing any per-source reference page.

**Outcome:** Added `cairn/references/forward-source-prospects.md`, a supply-push survey synthesis note triaging nagy2019 (CPM covariate extension), weide2021 (Bayesian/ML IIP CFA), rogoza2021 (three-step SSM + Procrustes), tracey2000 (circumplex-evaluation chapter) — one E1–E4 ledger row each (content, seedable oracle/method/reference/feature, what a taking-up milestone must verify), no verbatim values banked. Added its `INDEX.md` line, corrected the stale prospects comment, and registered four `candidate` ROADMAP rows (E1–E4). Corrected a false plan claim: tracey2000 is uncited (the CAIS `@source` is Sodano & Tracey 2006 at `R/instrument_data.R:5`), not CAIS provenance.

**Decisions:** D-024 (committed at plan time) supersedes D-023's per-source-brief mechanism with the supply-push output — survey note + candidate rows, no `prospects/` home, no per-source pages — keeping D-023's intent-not-citation principle.

**Review:** Three fresh-context lenses (diff-bug/Opus, blame-history/Sonnet, prior-review/Sonnet) — zero actioned findings; one logged below threshold (the D-042 citation, score 20 — a cairn-framework decision this repo bare-cites by convention). `cairn_validate` clean; toolchain checks N/A (docs-only).
