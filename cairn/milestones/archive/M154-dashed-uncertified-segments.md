# M154: Dashed displacement segments at uncertified time points

**Status:** done (2026-09-24, PR #189 https://github.com/jmgirard/circumplex/pull/189)

**Goal:** On the displacement panel of `ssm_plot_trajectory()`, draw each line segment that touches an uninterpretable time point as dashed, so the path through such a point reads as an unsupported interpolation while the unwrapped branch stays visible.

**Outcome:** `ssm_trajectory_segments()` (`R/ssm_trajectory.R`) builds one row per pair of consecutive displacement rows within a group, in time order, pairing each row with its successor so an `NA` displacement pairs with nothing and the gap stays. `ssm_trajectory_ggplot()` draws the displacement panel as `geom_segment()` on that frame and keeps `geom_line()` for the other four panels. Under a verdict, `Interpretable = both endpoints TRUE` maps to `scale_linetype_manual()` (solid, dashed, both limits kept), sharing the `Displacement interpretable` title with the shape scale so the two merge into one legend of a filled point on a solid line and a hollow point on a dashed line, keys pinned black by one `override.aes`. `legend.key.width` widened to 2.4 lines because the dash gap fell under the point at the default width. An `NA` verdict on a defined displacement is coerced by `%in% TRUE`, so it draws hollow on dashed segments instead of vanishing. `xend`, `yend` and `Interpretable` joined `ssm_trajectory_reserved()`. Test helpers `traj_segments()` and `traj_segment_lty()` (`helper-ssm-trajectory.R`) read the built segment layer, and `legend_key_lines()` reads each key's line grobs. Six vdiffr baselines regenerated. Help page, growth vignette Section 6, advanced-visualization trajectory paragraph, both vignettes re-rendered, NEWS bullet.

**Decisions:** none milestone-local. Plan gate: dashed over a lighter line, fail-closed on an `NA` verdict, both vignettes re-rendered.

**Review:** three-lens fan-out. The history lens found no conflict, the prior-review lens no findings with the PR-thread probe empty, and the diff-bug lens ten findings. Fixed at the gate (1ac7e40b): O1 `rep("d", length(from))` so a group with no drawable pair no longer errors (a regression against master, with tests on both entry paths), O2 the three reserved segment names, O3 the vignette sentence on the near-origin wave, O4 the legend-line assertion on the uncertified fixture, O6 `%||%` replaced for the R 4.1 floor, O7 NEWS qualified, O9 an unused test variable. Rejected: O5 a verdict on an undrawn row alone switches the legend on (fails closed), O8 invisible Group-key overdraw, O10 a redundant sort, and O9's `skip_on_cran()` half (the file's convention holds). Suite and check clean on the fixed tree, CI green on six checks. Hygiene: the M151 row pruned for terminal-row retention, and the M123 encoding lesson pruned as stalest to admit the M154 line under the byte budget.
