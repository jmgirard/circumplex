# M94: Print the fired-marker list on the bootstrap path

**Status:** done (2026-08-18, PR #123 https://github.com/jmgirard/circumplex/pull/123)

**Goal:** Make `summary()` on a bootstrap-CI `circumplex_cpm` fit print the fired boundary/weak-identification marker list with its own validation limit stated, where only the analytic caution ever named the marker vocabulary.

**Outcome:** `summary.circumplex_cpm` (R/cpm_oop.R) prints, on `ci_method = "bootstrap"` fits at every N, a Diagnostics-section note naming the fired `cpm_marker_labels()` labels — wrapped at whole-label boundaries by a greedy packer — with a fixed caveat that what has been measured about the markers covers analytic intervals only, not every marker was measured, and none is validated as a predictor of the bootstrap intervals shown; the note opens the Diagnostics section itself when no diagnostic line fires. Analytic-path output and `print()` are byte-unchanged, fenced by merge-base snapshots in `tests/testthat/test-cpm_summary_markers.R` (same-machine by measured CI evidence: third-decimal cross-platform deltas; skip_on_ci/cran/covr). The roxygen, the vignette locus paragraph, and NEWS state the new behavior; `test-cpm_boundary_vignette.R` pins the corrected paragraph and fails on the retired "rather than the list" wording. D-010's analytic caution untouched.

**Decisions:** none promoted; gate choices in the work log (git holds the full file).

**Review:** Two rounds, three fresh-context lenses each; blame-history and prior-review clean both rounds. Round 1 returned the milestone: the note printed outside `# Diagnostics` when no diagnostic line fired (fixed, mutation-verified) and the caveat overclaimed "validated as interval predictors" against the record for the removed and multimodal markers (gated AC2 amendment, five surfaces re-worded). Round 2 verified all fixes and raised eight more: seam spacing + byte pin, packer width, NEWS caveat completed, named forward pointer all fixed; AC1's stale "one line" amended (audited, raw-label wrap assertions added); register drift and apostrophe style rejected, as were the local-only print fence and an RNG-convention nit in round 1. Post-approval CI red measured the analytic byte fence platform-fragile; scoped same-machine, then all platforms green. One defect return; amendment returns on AC1 and AC2, each a first occurrence. Hygiene: extended two lesson families (extraction helpers certify content, never placement; committed snapshots are same-machine fixtures) and added the negative-claim rewording lesson.
