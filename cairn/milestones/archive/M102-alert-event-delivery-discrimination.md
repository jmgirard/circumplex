# M102: Separate a filtered-out alert event from one never delivered

**Status:** done (2026-08-21, PR #131 https://github.com/jmgirard/circumplex/pull/131)

**Goal (amended at the review gate):** Drive, on the existing probe repo, an experiment that observes the two spellings `on.workflow_run.workflows` can match separately, and record the result as a per-run ledger where the alert's readers will find it. Reading that ledger is out of scope.

**Outcome:** A second `workflow_run` subscriber listing the PATH spelling `.github/workflows/R-CMD-check.yaml` was added to `jmgirard/gha-startup-failure-probe`, and nine push runs were driven against it — including a control M101 never had: a VALID workflow declaring no `name:`, whose run name also resolves to the path. `.github/workflows/master-red-alert.yaml`'s header now carries the ledger of that window: each run's id, watched-file state, reported `name`, conclusion, job count, and which subscribers produced runs, plus the date measured, the probe-equivalence check, and two recorded limits (two one-spelling subscriber files, never one listing both; the subscriber's text at probe commit 758ab1b). No run reported `startup_failure`. The workflow diff is comment-only — `on:` and the job `if:` byte-identical to master, so `EXPECTED_IF` is unmoved. The probe repo is public and restored to its two-workflow baseline.

**Descoped at review return 2.** What the ledger IMPLIES is deliberately unrecorded. Two attempts at writing that reading — that name resolution is not the mechanism, that a `workflows:`-side fix is ruled out — each overstated the evidence and were returned; the second was introduced by the commit repairing the first. Jeff chose banking the verified ledger over a third attempt. The ROADMAP lineage row carries the reading as owed and unstarted, and restates items (b) and (c) as open, unstruck, with their promotion conditions.

**Decisions:** the milestone-local entry asserting the reversal is superseded by an appended entry withdrawing it; the measurement is not withdrawn. Nothing promoted to DECISIONS.md.

**Review:** Three rounds, two defect returns, one amendment return (AC2), plus three pre-commit wording audits. Round 1: fifteen findings, ten actioned — the "RULED OUT" overclaim and a Scope promise the record broke. Round 2: the overclaim recurred via the row headline, AC2 shown unsatisfiable (it demanded the header say which explanation was ruled out; the result ruled out neither). AC5 was amended earlier after quoting its own grep pattern and matching its own text. The measurement itself was verified clean in every round by readers that did not author it; every failure was in prose about it. Gate: `check()` Status OK, `test()` PASS 8395, both alert audits exit 0, `cairn_validate` clean.
