# M148: Close the five remaining accuracy-certificate validation-layer fragilities

**Status:** done (2026-09-22, PR #183 https://github.com/jmgirard/circumplex/pull/183)

**Goal:** The five latent defects DESIGN.md's Known fragilities records in the accuracy certificate's validation layers can no longer turn a real regression green.

**Outcome:** In `tests/testthat/test-axes-certificate.R`: `cert_pin_length()` asserts the measured vector's length against the committed exact array before each of the three priced-loop `cert_rel()` comparisons (a one-short plant reddens all 18 sites); `cert_root_rel()` stops naming the input at a relative variance error below -100%, -1 admitted; a new test multiplies the naive arm by (1 + delta) at `cert_anchors()[[1L]]` and brackets `fiml_ratio` while `se` and `cval` stay bit-identical, so a denominator-blind certificate reddens; `cert_priced_not_degraded()` runs first in the counterexample-B priced branch and fails on the sentinel (a mocked `axes_dd_selftest()` FALSE fires it). In `devel/degeneracy-oracle/exact_oracle.R`: `exact()` is split into `exact_parse()` plus the process call, `exact_keys()` lists every key the Python script prints, a missing key or a non-numeric value stops naming it, asserted in process at load. Comments claiming a NaN passes testthat silently were false and are corrected (testthat reddens on NaN; the guards exist to name the input). DESIGN.md's two Known-fragilities paragraphs corrected in place: none open; the vacuous `cval` aggregation clause dropped; which sentinel route fired stays unnamed (a route tag is shipped code). No shipped behavior changed.

**Decisions:** none.

**Review:** Two passes, three lenses each. Pass 1: AC1-AC5 passed on plants; AC6 (whole-suite zero warnings) failed on twelve warnings from untouched files and installed libraries, an amendment return; AC6 was re-audited twice and narrowed to warnings reported against the changed test file under `reporter = "check"`. Pass 1's [O] lens reported seven findings: one fixed (a wrong NaN rationale in a comment), four rejected, one noted, one deferred. Pass 2 re-confirmed every disposition and added three comment corrections, all fixed at the gate, plus the M122 lineage sentence in the sentinel helper. The `identical()`-against-sentinel finding was rejected with reason in both passes (a coincidence fails, never passes). Deferred: an empty `HEX_*` value in the oracle output reaches `cert_record()` unchecked, filed as (iv) on the ROADMAP degeneracy row. Defect returns 0, amendment returns 1. PR conversation empty; six CI checks green. Hygiene: one LESSONS line added, the M75 line pruned for the byte budget.
