# circumplex Roadmap

_The only authority on milestone status. Grouped by status, not ID. Pre-migration history: `cairn/legacy/` and git log._
_Last hygiene check: 2026-09-24 (M153 done: the caution-wording candidate row extended with the Section 7 remedies finding, the M150 row pruned for terminal-row retention, no LESSONS line since the file sits at its byte budget and nothing was retired, no RB/RR to archive, validate green.)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M152 | Growth output helper, coefficients or draws to a trajectory | done | M151 | high | milestones/archive/M152-growth-trajectory-helper.md |
| M154 | Dashed displacement segments at uncertified time points | planned | — | normal | milestones/M154-dashed-uncertified-segments.md |
| M153 | The growth vignette on the helper workflow | done | M152 | high | milestones/archive/M153-growth-vignette-rewrite.md |
| M151 | Growth input helpers, the long table and the formula builder | done | — | high | milestones/archive/M151-growth-input-helpers.md |

## Candidates

_Candidates carry no milestone ID — an `M<NN>` is assigned at planning time. Legacy IDs (≤ M6) remain valid citations into `cairn/legacy/`; M7 is the first cairn-era ID. Struck rows are tombstones; their histories live in the named archives and git._
- **`ssm_trajectory()`'s printed caution under draws input, and the Section 7 remedies' path back into the helper** (M152 review O8; M153 review O5). The caution is unconditional by its acceptance criterion and under a brms draws matrix does not describe how the intervals were made; the vignette now says so in prose. Section 7 names lme4 and nlme remedies while the engines are glmmTMB and nlme, and does not say that parametric-bootstrap coefficient replicates go in as `draws =`. Promote when a print-side branch on the input shape is decided, or with the next milestone opening that page.
- **`ssm_growth_data()` input guards not shipped** (M151 review, findings O8 and O9). Duplicate column names in `data` resolve silently to the first match, in the id, time and scale columns alike. No refusal covers an `id` or `time` that is also a scale, scale numbers that point at the id or time column, `Inf` in `time`, or duplicate person-by-time rows. Promote on a user report, or when a milestone next opens `R/ssm_growth_data.R`.
- ~~Vignette output lines past the website's code-box width~~ **graduated 2026-09-15 → M131 + M132**. M146 closed items (ii)-(viii), and (iii)'s premise was false: `strwrap()` already counts columns. **Open:** (i) no width guard reads the two pages not pre-rendered (`bayesian-ssm-analysis`, `using-instruments`). (ix) The width guard does not read output under another `comment` prefix, `results = "asis"` or indented output. (x) The CPM `Fit:` line is not wrapped: 89-90 columns at width 77 (M146 review O10). Promote (ix) when such a line reaches the site. Promote (x) with the next milestone opening `R/cpm_oop.R`. The M134 exemption gaps (no width cap, whole-vignette scope) were closed 2026-09-20 by stating them in the guard's header. Hardening them widens an internal checker's promise, and they affect only the two 86-column `cx`/`cy` lines.
- **Ladder print test gaps and edge cases** (M130 review; findings in its archive). Test side: `test_stat()` derives the expected Δχ² string from the code's own `format(round())`, no test requires a wrapped line, none covers `comparable = NULL`. Print side: under `LC_ALL=C`, Δ prints as `<U+0394>` and block lines reach 84 characters where the width tests skip; the vacuous metric rung is guessed from any metric-row note; the no-`required` fallback drops `Also:` and the "latent metric" qualifier; a strict-tier fit at a configural gate calls metric invariance untested where it holds by construction. Promote on a user report, a second rung-note kind, or when those tests next open.
- **The arm64 harness's two uncovered edges** (M121 review; both stated in `tools/arm64/README.md`). (i) Only the Dockerfile's base layer is digest-pinned, so `apt-get` runs against Debian unstable and both `install.packages()` layers fetch current CRAN, and a rebuild months on is a different image. (ii) The harness runs plain `R CMD check`, not `--as-cran`, and CRAN's arm64 options line is not in the repo, so whether the flavor uses it is unverified. Promote when a milestone next opens the harness, or on a CRAN arm64 result it cannot reproduce.
- **Second-order follow-ons from RR22** (none blocking). (i) An `ubuntu-24.04-arm` CI job (rec 10) to sample CRAN's BLAS family on every push; promote once `setup-r` and arm64-Linux binaries are verified. (ii) and (iii) (recs 11 and 12) graduated 2026-09-22 → M149. (iv) From the M147 review: sample floor-admitted designs with four or more scales whose `rcond(info)` sits in [eps, 1e-10] before D-062's Reopens clause is read as closed (the sweep's in-domain routed rows all sit above 1e-9); and thread the once-priced core into `axes_accuracy_certificate()`, which re-prices it twice for a routed fit. (v) From the M147 second review pass: on the selector route the `"uncertified"` warning's conditioning clause names a condition number the floor accepted, since `axes_degeneracy_hint()` reads sigma alone; a clause naming the derivative structure would say what was refused. (vi) From the M149 review: graduated 2026-09-22 → M150, done. Its factor-10 item for `u` at B was dropped at the M150 gate, because the margin test's comment discloses it and a wrong bound can only give a false red.
- **The accuracy summary's text checks cannot catch wording drift**, added 2026-09-15 by the M127 review (finding O5). `expect_short_ci_summary()` in `tests/testthat/helper-ci-accuracy-summary.R` compares `summary()` with the current verdict and structure functions, so a reworded caution still passes. The seeded and contrast length checks also skip on CI. M127's review compared old and new text with a one-off script. Promote if a verdict or caution changes wording without a test failing, or into whichever milestone next opens that summary.
- **Vignette prose follow-ons** (rules in `cairn/references/plain-vignettes.md`). (i) Plain-English pass over `introduction-to-ssm-analysis`, `intermediate-ssm-analysis` and `using-instruments` (8, 15 and 30 sentences over 25 words at `125b76db`); the ebook row applies. (ii) The prose sweep as a merge gate: CI and testthat run `tools/prose-sweep.R` (nestedtune M088 shape); review-only for now because nestedtune's gate needed two parser fixes. (iii) `axes-reliability-caveats` prose after the split (M137 review O3-O5): rules only in the Wrap-up, "Sections 2 to 5" sited inside Section 2 and skipping 6, bold lead-ins repeating headings 3 to 5; needs split-guard pairs. The after-M125 trigger for (i) and (ii) has fired; also promote on a reader finding a vignette hard to follow, or a vignette regressing past the sweep.
- **A Quarto-based website through `altdoc`**, added 2026-09-17 by the M138 plan gate. pkgdown has no Quarto version, and the installed pkgdown 2.2.1 is current, so M138 restyles the pkgdown site instead. A Quarto site would mean another tool (`altdoc`), a new build workflow, and hand-written reference pages. Promote if pkgdown cannot give the site something the maintainer wants, or if pkgdown stops being maintained.
- **Vignette tables from `knitr::kable()` to gt**, added 2026-09-14 by M124 gate (the maintainer is moving from kable to gt). The vignettes hold 22 `kable` calls, and gt is not a dependency yet, so a switch needs a dependency gate and D-entry. By default gt gives each rendered table a random id, so the `vignette-precompute` staleness check likely needs the id pinned (not yet tried). Promote when the maintainer schedules the switch.
- **Windows compile cost in the CRAN check** — the install step carries no timing in any of the three Windows logs read, so the compile cost stays unmeasured. M120's win-builder run bounds the whole untimed residual at 105 s against a 3 min 23 s - 4 min 23 s overall, so the promotion condition — the install step alone eating the budget — is **not met**. Promote when a log finally times the install step and it dominates, or when a check cannot reach 8 minutes with tests and vignettes at zero.
- **M120 remainders** (M120 review G4-G6; full lists in the M120 archive). (i) **CRAN-dark boundary invariants**: five past bug guards run on CI but not on CRAN (`ssm_analyze_long` occasion order, SEM latent contrast near ±180° and relabel flip, `test-ssm_trajectory.R` seam battery, `test-cpm_boundary.R` free-scaling pole recovery, `angle_dist` half-turn atom). Promote with the milestone that next opens these files, or on a wrap or order regression reaching CRAN. (ii) **Instrument strength**: the `tools/m120-*` instruments and the vignette staleness guard credit more than they check (eleven weaknesses). Promote with the milestone that next opens them, or when one is relied on outside M120.
- **Master-red alert open questions** (M99 lineage; experiments M101-M103, probe repo `jmgirard/gha-startup-failure-probe`, ledger in `.github/workflows/master-red-alert.yaml`'s header): (a) a scheduled sweep as a second detector, declined at the M99 gate, promote when a master break goes unannounced; (b) whether `workflow_run` fires for a run that fails to start; (c) what produces `startup_failure`, promote on such a run seen in the wild. Each also promotes with the milestone that next opens the alert workflow.
- **The graduated doctrine module has no size discipline of its own** (M104 review, [O] F11 and F6): `cairn/test-craft.md` is 26 lines / 7,293 bytes (observed 2026-08-22), and no cap or validator watches it. Mechanizing a cap belongs in the cairn plugin repo, which owns `LINE_CAPS`. This repo's remedy is the module's own stated budget (< 35 lines, < 9,000 bytes), read at hygiene passes. The ownership exit, folding it into `PROFILE.md`, is refused by D-046. Promote on the module drifting past its budget.
- **An Algolia docsearch API key stays in git history** (M138 review, [O] finding 7): `api_key: ec4004481ba8d410e8e20c9e90fa5e60` sat in `_pkgdown.yml` from `abfc81eb` (2018) until M138 deleted the dead docsearch block, and deleting it from HEAD does not revoke it. A docsearch `api_key` is a search-only public key by design, so this is a retire-or-rotate question for Jeff's Algolia account, not an exposure the repo can close. Promote once Jeff decides; the same shape as the Codecov row below.
- **A Codecov upload token is committed in plaintext** (M95 review, [O] diff-bug F14): `codecov.yml:36` carries `token: b885341a-…`, committed since M12 (7480a67a), while `.github/workflows/test-coverage.yaml:94` already supplies `secrets.CODECOV_TOKEN` — so the committed copy is redundant as well as readable in a public repo. Left at M95's gate (Jeff's call): rotation happens in Jeff's Codecov account. Promote once Jeff rotates the token, and delete the dead line then; deleting it first hides the evidence without closing the exposure.
- **An always-reporting aggregator job in `R-CMD-check.yaml`** so `cairn/**`-only PRs report a required check (PR #133), declined at the M105 gate (M93/M96/M105 lineage; rulesets in D-047, pinned by `tools/branch-protection.json`; split 2026-09-21). The maintainer's `always` bypass stays either way, so it binds only a non-admin contributor and reopens M93's fail-closed classifier. Promote when a non-admin contributor path exists, or with the milestone that next opens `R-CMD-check.yaml`.
- **Revert the macOS Package Manager workaround in `R-CMD-check.yaml`** (hotfix PR #174, 2026-09-20; split 2026-09-21). pak 0.11.1 (pkgdepends 0.9.1) cannot extract CRAN's zstd macOS binaries (r-lib/pkgdepends#485, still open 2026-09-21), so the check job sets `use-public-rspm: always` and a macOS step points `PKG_CRAN_MIRROR` at Package Manager and blocks `mac.cran.dev`. Promote when a released pak extracts zstd: set the input back to `true`, delete the step, confirm macOS green from CRAN.
- ~~The degeneracy criterion's accuracy floor, and the guards that skipped under check~~ **graduated → M108–M111, M113–M117, M118** (histories in those archives, D-044/D-048–D-054, RR18–RR21). The six latent defects once under **Known fragilities** in `cairn/DESIGN.md` are closed (two by M122, one by M147, the rest by M148 on 2026-09-22); the rest of the remainder is under **Accepted limitations**. Also open: (i) the relocated exemplar-B fixture has no generator or seed; promote on a provenance decision for artifacts with no reproducible origin. (ii) Nothing detects the silent skip recurring if lavaan renames again. (iii) RR21 rec 5 (show the certificate's estimate on computed fits) wants its own GP4 gate. (iv) From the M148 review: `exact_parse()` in `devel/degeneracy-oracle/exact_oracle.R` checks that every `HEX_*` key is present but not that its value has two hex words, so an empty value reaches `cert_record()` as an empty literal; promote with the milestone that next opens the oracle driver.
- **Shrink the norms-audit argument guards?** (M84 → M86 lineage; parked at the M87 plan gate). `validate_batch()`, `validate_roster()` and `roster_from_objects()` in `data-raw/audit-norms.R` (~225 lines) are called only by tests and the run block, but `validate_batch()` still guards the hand-edit of `AUDIT_BATCH` when instrument 16 is added. Promote on evidence no maintainer path reaches the roster-side guards, or when a later milestone opens that file.
- **Norms-audit same-binding-twin conflation** (M83/M87/M88 lineage; M88's F11, F12, F4 closed by M98, D-045). The repair is a shared base deriving "same (kind, key)", with the differing-binding filter layered on for the stack roster (M83 archive). Promote on evidence a twin is added.
- Longitudinal deferrals (D-013 lineage; Builds A/B/C → M25/M26/M27, §1.1 sugar → M28, §1.4 occasions → M29). Remaining, each with standing rationale against building now: pairwise-deletion occasions semantics (§1.3, ships listwise-only on RR06 R6 estimand grounds, reopening needs a use case plus a superseding D-entry); occasions × measures correlation path (§1.2a, defer until a use case exists); occasions × contrast × grouping difference-of-differences (§1.2b, a new estimand needing its own design pass); Stan companion (§5.4 stay-out criteria, revisit only if ≥2 of 3 conditions hold). Also here since 2026-09-24: a shared-intercept `(1 | person)` cell in `devel/m27-coverage-oracle.R` (RR23 rec 4, D-060), which needs a pre-registered acceptance line first and promotes on its own plan gate; and builder options beyond the fixed linear joint model (covariates, polynomial time, D-064), which promote on a user design the coverage oracle later validates. The draws-to-trajectory helper row graduated 2026-09-24 → M152.
- CPM simulation paper (research track, not a milestone): engine ratified in `devel/cpm-sim/` (plan `devel/cpm-simulation-paper-plan.md`; not yet run). A package milestone follows only if results motivate one.
- Continuous / infrastructure refactors, parked fold-ins per M14's scope note (fold into the milestone that next touches the code): `cpm_analytic_se` recomputes a Hessian it could reuse (minor perf; oracle-validate when done); the D-003 pole-snap cosmetic; strict-tier syntax emission single-sourcing in `R/ssm_sem_syntax.R` (descoped from M8 T4, 2026-07-12; the two emitters are byte-pinned by `exp_strict_*` snapshots, so revisit only if that emitter is reworked for another reason).
- Disposition of the three `devel/` transcription files once M7 archives (deferred out of M40/M41, 2026-07-19): `ar2004-`, `cr2002-` and `m4-zw-transcription.md` are cited from M7's open work log, so they stay byte-untouched until M7 archives; then decide per file between a "superseded by `cairn/references/<citekey>.md`" pointer header and outright retirement — the transcription-protocol records (two-channel diffs, paper-internal inconsistencies found) are the part worth keeping.
- CIRCUM free-scaling post-v2.0.0 extensions (deferred out of M18; see its archive + D-009): bootstrap σ CIs; covariance-matrix input — shipping it re-triggers the D-011 paired T calibration at non-unit σ truths first. The point-estimate oracle already shipped in M18.
- **True animation** for trajectory viz: excluded from M37 at its plan gate on minimal-deps + custom-ggproto-coord grounds — revisit only on a concrete teaching use case, and only via the dependency gate (no new Import or Suggests without a D-entry). The rest of the visualization expansion shipped across M30–M37 (D-018).
- **Covariate extension of the CPM (Nagy, Etzel & Lüdtke 2019, SPMC-E)**, registered 2026-09-19 after a SITAR reader's question. Attach external measures to the free-angle Browne model in `cpm_fit()`: per covariate a level scaling, a content-sensitivity scaling and an angle, with the paper's three least-squares identification constraints and a confidence ellipse on the Cartesian coordinates. A lavaan 0.7.2 smoke test (2026-09-19) recovered `cpm_fit()`'s one-harmonic angles on `jz2017` within 0.4° (`pi` inlined in `:=`); covariates and higher harmonics are untested, and the native optimizer is the likelier home. The plan gate decides the engine, checks the paper's parameterization against `cpm_fit()`'s convention, and oracles the intervals against the paper's simulation and wester2026's SPMC-E tables (E5 in the prospects note). Fable tier: the identification constraints are where a plausible-but-wrong fit hides.
- **Ellipse and curve follow-ons** (parked 2026-09-19 and 2026-09-20 from M140, M141 and M143; split 2026-09-21 from the covariate-extension row). (i) A `cpm_plot_curve()` export (M140 shows it as vignette code); promote on a reader request. (ii) `ssm_ellipse_data()` methods for `circumplex_ssm` and `circumplex_ssm_sem` objects, which keep no draws (the SEM covariance re-derives from `x$sem` and `x$model$weights`), plus Fig. 4 tangent lines as an aesthetic; promote when a user asks for an ellipse on a non-draws object, or with the covariate extension. (iii) M141 review R5: the draws method returns `NA` columns for a one-draw or `NA`-draw object and the geom drops the row silently, so the next method adds a guard naming the cause. (iv) An exported helper for the ellipse's tangent angles and nearest and farthest distances (Nagy et al. 2019, Fig. 4, Appendix B), now echoed vignette code (M143); promote on a reader request or with the covariate extension.
- **Forward-source prospects** (weide2021/E2, rogoza2021/E3, tracey2000/E4; nagy2019/E1 and wester2026/E5 are folded into the covariate-extension row): shelved research sources characterized in `cairn/references/forward-source-prospects.md`. No per-source page is owed until a milestone relies on one (D-023/D-024). tracey2000's shelf copy is an OCR scan, so any extraction needs a second channel (M42-D1).
- **FIML follow-ons, post-build** (RR12 B-4/§4; neither rejected): planned-missingness designs (three-form and similar) are excluded by the mandatory zero-joint-coverage refusal even though the structured model stays identified there — support means coverage-aware standardization, shadow and PD machinery; and lavaan's `missing = "two.stage"` as an SE-corrected alternative to one-stage FIML, needing the same FIML-metric standardization first. Promote either on a concrete use case.
- **Axes-reliability deferred-in-spec extensions** (out of M54, per RR09/D-026). Remaining: **block membership on the `circumplex_instrument` class** — M63 takes `blocks =` as an argument only, since no bundled instrument records blocks and a class field would ship empty; revisit if an instrument whose blocks are known is bundled. Quasi-circumplex/unequal-spacing weights stay refused on RR09 §4's standing holding, not deferred. (Everything else graduated across M59–M65; lineage in those archives.)
- **Didactic vignettes → ebook** (2026-08-04 design interview, GP5 note): Jeff may move the teaching content into an ebook and simplify the vignettes to demonstration size. GP5's precision bar travels with the content. Promote only when Jeff opens the project.
- **Norms provenance: ask Hatcher and Markey where the iis32 and ipipipc norms came from** (audit shipped M72-M75; status in `cairn/references/norms-audit.md`). Neither cited article publishes them and the 2018 correspondence is lost, so both ship an "unconfirmed" credit and a `source-not-identified` ledger disposition. Ask Robert Hatcher (IIS-32, the n = 1380 octant M/SD) and Patrick Markey (IPIP-IPC, Study 2's n = 274). Closes on a reply identifying either sample; promote only if a reply shows a shipped value wrong.
- **A `getOption()` global mute beside `norm_standardize()`'s `quiet` argument** (M76/M77 lineage, parked 2026-08-08; the rename question closed by RR16, D-041). Promote if `quiet` proves insufficient in loops or knitted documents.
- **IIP T-score conversion tables** (declined at the M75 gate): the manual norms the IIP through per-scale T lookup tables (Appendices B/F), not the shipped M/SD z-scoring. The Mind Garden permission covers means and SDs only; the measured divergence (2026-08-07: a high-score S-shape, median 0.3 T-points) is smaller than a change of reference sample; and a normalized T would distort the interval metric SSM's cosine fit assumes. Promote only on a permission covering the tables plus a concrete use case; it would be a new estimand needing its own design pass.
- **CSIG source may print duplicated PA/NO statistics** (M72): `locke2014` Figure 2 (p. 436) prints identical α/M/SD (.73/2.96/.68) for both PA and NO; the package transcribes both faithfully, so the question is about the source (see `cairn/references/locke2014.md`). Flagged to Locke at Jeff's choice. Promote if he confirms an error (a shipped-value fix); leave if the figure is correct.
- **`cais`'s adult-sample norms exceed the instrument's 1-5 anchors** (M74 review): sample 2's PA 5.19, LM 6.52 and NO 6.14. The leading explanation (2026-08-08) is that sodano2006 Table 4's M/SD rows are transposed between its CAIS and IAS blocks (`references/sodano2006.md`). M112 withdrew the sample (D-052). Open: the author query to Sodano (Tracey copied, 2026-08-08, with the 204-vs-213 *N* question). A reply does not promote this row: "swapped" re-adds corrected values under a fresh gate (D-039), and "correct as printed" closes it.
- **Six sub-threshold M75 review findings against the norms-audit instrument-tagged-block machinery** (M79/M80 lineage; details in the M75 archive). Promote when the audit next extends to a new instrument, since a silently dropped sample is what extension would hit first.
- **Boundary-regime interval improvement** (post-M4 lineage, 2026-08-04 design interview): interval methods with better coverage where the record fails, with β ~.77 flat in N at boundary truths (percentile bootstrap) and analytic angle CIs outside the band until N ≈ 50000. BCa is refused for circular displacement (D-002). The search space is other interval families or corrections, each needing its own coverage oracle. Until promoted, the `summary()` cautions are the mitigation.
- **Scaled-statistic residues** (lineage RR13 B-1 → M66/M67/M68/M70, D-036; histories in those archives): still open — the **scaled *difference* test** for nested model comparison (no such comparison in the API today; promote if one appears), and a **Swain/Bartlett-type small-sample mean correction to `T`** (RR14), which oracle mean-recentering shows would essentially close the small-N over-rejection M68 documents rather than fixes. Promote the latter on evidence the documented small-N behaviour misleads users, and only with its own oracle and calibration cells.
