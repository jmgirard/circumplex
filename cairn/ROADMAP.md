# circumplex Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-08-30 (M116 and M117 planned off the degeneracy row's cleanup disposition, which graduates; the 1e3 ceiling, the two closed-form floor identities and the dimension skip go to M116, the double evaluation to M117, and the M116 plan's criteria audit added one item — `:723-725`'s exactness identities — declined at that gate. The row's cosmetic and latent remainder stays. Adding two table rows put the file 9 bytes over its 24,000-byte budget, so this stamp replaces the M115 one at a fifth its length; now 56 lines / 23,715 bytes. LESSONS.md untouched at 47 lines / 19,960 against 50 / 20,000; `test-craft.md` untouched at 29 / 8,718 against 35 / 9,000. The graduate-or-prune pass over the remaining candidate rows is still owed.)_

Pre-migration history: `cairn/legacy/` and git log.

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M7 | v2.0.0 CRAN release preparation | blocked | — (all deps done) | high | milestones/M7-v2-release-prep.md |
| M116 | Make three passing-without-checking assertions in the certificate suite redden | review | — | normal | milestones/M116-certificate-suite-vacuities.md |
| M117 | Price the accuracy certificate once per checked fit | planned | — | normal | milestones/M117-certificate-once-per-fit.md |
| M114 | Pin the shared refusal predicate, and assert the dead literal rather than dropping it | done | M113 | normal | milestones/archive/M114-refusal-predicate-fences.md |
| M115 | Make the packaged accuracy bracket assert where the shipped pricing differs | done | M113 | normal | milestones/archive/M115-certificate-bracket-reach.md |
| M111 | Shrink the ill-conditioning refusal to what the certificate cannot certify | done | — | normal | milestones/archive/M111-certificate-refusal-region.md |
| M112 | Withdraw the CAIS adult normative sample | done | — | normal | milestones/archive/M112-cais-adult-sample-withdrawal.md |
| M113 | Certify the ratio the reported FIML standard error is multiplied by | done | — | high | milestones/archive/M113-fiml-ratio-certificate.md |

## Candidates

_Candidates carry no milestone ID — an `M<NN>` is assigned at planning time. Legacy IDs (≤ M6) remain valid citations into `cairn/legacy/`; M7 is the first cairn-era ID. Struck rows are tombstones; their histories live in the named archives and git._
- ~~The master-red alert stays silent on `startup_failure` and `timed_out`~~ **graduated 2026-08-21 → M99**; follow-on experiments ran as M101–M103 on probe repo `jmgirard/gha-startup-failure-probe` (public, at its two-workflow baseline). Per-run ledger and M103's reading of it in `.github/workflows/master-red-alert.yaml`'s header; histories in the M99/M101/M102/M103 archives. Three stay open, and any of them also promotes into whichever milestone next opens the alert workflow. (a) **Scheduled sweep as a second detector**, declined at the M99 gate as its own design, audit and dedupe surface — promote when a master break actually goes unannounced. (b) **Whether a `workflow_run` event is delivered for a run that fails to start, and under what name** — undecided; the remaining experiment is an unfiltered subscriber, left out of M102 on a self-retrigger hazard, so promote when that hazard is worth designing around. (c) **What produces a `startup_failure` conclusion at all, and whether such a run carries a resolvable workflow name** — three constructions aimed at it failed (M102 archive); promote on such a run seen in the wild, or when an untried construction is worth driving.
- **The graduated doctrine module has no size discipline of its own** (M104 review, [O] F11 and F6): `cairn/test-craft.md` is 26 lines / 7,293 bytes — observed 2026-08-22 — and no cap or validator watches it, so it can drift as `LESSONS.md` cannot. `cairn_validate`'s `LINE_CAPS` lives in the cairn plugin repo, not here, so mechanizing a cap belongs to that repo and is owed a candidate row there; this repo's remedy is the budget the module's own header now states (< 35 lines, < 9,000 bytes), read at ROADMAP hygiene passes. The **ownership exit** — folding the module into `PROFILE.md`'s `test-doctrine` slot — is refused by D-046, which names "a preference for fewer tracking files" explicitly insufficient to reopen; superseding it is its own decision. Reachability is not at issue: `LESSONS.md`'s pointer is read whole at every `/milestone-plan` session start and was followed on 2026-08-22. Promote on the module actually drifting past its stated budget.
- **A Codecov upload token is committed in plaintext** (M95 review, [O] diff-bug F14): `codecov.yml:36` carries `token: b885341a-…`, committed since M12 (7480a67a), while `.github/workflows/test-coverage.yaml:94` already supplies `secrets.CODECOV_TOKEN` — so the committed copy is redundant as well as readable in a public repo. Out of M95's diff and left there at its review gate (Jeff's call): rotating it happens in Jeff's Codecov account, not in the repo. Promote when Jeff has rotated the token, at which point the dead line is deleted in the same pass; deleting it before rotation removes the evidence without closing the exposure.
- ~~Master's CI gates: GitHub-native branch protection, and the PR check gate blind to the release platforms~~ **graduated → M105 (2026-08-22), M93 (2026-08-17) and M96 (2026-08-18)**; histories in those archives, the two rulesets in D-047 and pinned by `tools/branch-protection.json`. **Open remainder, declined at the M105 gate:** an always-reporting aggregator job in `R-CMD-check.yaml` (drop workflow-level `paths-ignore`, filter in the existing `matrix` job, add an `if: always()` gate job) so a required check reports on `cairn/**`-only PRs, which today report none (PR #133). Declined because the maintainer needs `always` bypass for direct docs-only pushes regardless — required checks block pushes as well as merges, and a `cairn/**`-only push runs no workflow — so the aggregator buys enforcement only against a non-admin contributor while reopening M93's deliberately fail-closed classifier. Promote when a non-admin contributor path exists, or into whichever milestone next opens `R-CMD-check.yaml`.
- ~~The degeneracy criterion's accuracy floor, and the guards that skipped under check~~ **graduated 2026-08-24 → M108–M111, then 2026-08-30 → M113–M115** — all six shipped; the full text and gate disposition of every item below is in the M89/M106–M108/M110/M111/M113–M115 archives' Review sections, D-044/D-048–D-054 and RR18–RR21. Everything below is still open, each on its own condition. **Its own condition:** (i) the relocated exemplar-B fixture has no generator and no seed, so it fails the profile's fixture-provenance rule as written — `dput(control = c("all", "hexNumeric"))` round-trips it bit-identically, so a code form exists if provenance is reworked; promote on a decision about what provenance means for an artifact with no reproducible origin. (ii) Nothing detects a repeat of the silent-skip failure: if lavaan renames again, both spellings fail, both comparisons skip and the suite stays green; promote on evidence a cross-check has gone quiet again, or into whichever milestone next opens that helper. (iii) RR21 rec 5, surfacing the certificate's estimate on computed fits and not only refused ones — never adopted or declined; it adds a field to an exported return under GP4, so it wants its own gate. General certification of the derivation block's prose stays **descoped, not deferred** (M106 failed it over three rounds). **Promote into whichever milestone next opens the certificate, its tests, the oracle driver or either consuming surface:** `R/axes_corrected_se.R:537`'s `6.5e-6` corner derives from a = 0.046, not the 0.045 the block states (M110 [O] F3); plus M111 F16; M108's nine cosmetic and performance residues, each with its text in that archive's Review; and four from the M113 review — `axes_certificate_worst()` failing open on an empty certificate (unreachable today, declined for M114), the planted-perturbation layer never extended to the new field's denominator, `ratio_rel()` measuring an n-dependent quantity against an n-free exact value (conservative, sub-ulp), and the 1e3 bracket ceiling against measured ~10. M113's F1/F2 (the widened predicate refusing a non-FIML fit on `fiml_ratio`) were **rejected as defects** on GP2 fail-closed grounds, the reachable widening measured 0.46–2.1x. **Added 2026-08-30 by the M115 review**, nine deferred whole at the maintainer's triage, all in `tests/testthat/test-axes-certificate.R` unless said otherwise: nothing distinguishes six cases asserted from six skipped, so an all-skip run is green (the review gate's CI read is the only detector); the at-the-floor branch is selected by `identical(est, cert_floor)`, a value coincidence; the measured side's component count is unpinned against the committed pair's length, so `cert_rel()` can recycle silently; `cert_hex()` is dead code still reading as the live precondition; `cert_root_rel()` returns NaN rather than failing at a relative variance error at or below -100%; `cert_rel()` divides by `hi + lo`, zero when the exact quantity is (latent); `exact_oracle.R:70-79`'s list `[[` turns a missing oracle key into a silent NULL pasted into the regeneration;. ~~The bounded cleanup the M115 hygiene disposition chose~~ **graduated 2026-08-30 → M116** (the 1e3 ceiling, the two closed-form cases' floor identities, the dimension-change skip) **and → M117** (the double evaluation per fit); the cosmetic and latent remainder above stays. **Added 2026-08-30 by the M116 plan's criteria audit:** `:723-725` asserts `axes_v_pricing()` lands on 97/128, 2 and 5/8 exactly on every machine — the same claim shape M116 removes one level up, declined at that plan's gate to keep the diff to the sites the disposition named; promote into whichever milestone next opens that test.
- ~~The norms-audit roster builder's remaining unnamed and undiscriminated shapes~~ **graduated 2026-08-15 → M86** (all six M84 review findings; histories in the M84/M86 archives). Open remainder, parked at the M87 plan gate: **whether the audit script's ~225-line argument-guard surface should shrink at all** — `validate_batch()`, `validate_roster()` and `roster_from_objects()` in `data-raw/audit-norms.R` are called only by the test suite and the run block, though `validate_batch()` still protects the one human-reached path (a hand-edit of `AUDIT_BATCH` when instrument 16 is added). Promote on evidence no maintainer path reaches the roster-side guards, or when a later milestone opens that file anyway.
- ~~The norms-audit cross-discrimination matrix reddens against a same-binding twin~~ — the matrix was deleted by M87 and the walk-helper half (M87 review F13) **graduated 2026-08-15 → M88**, but the **same-binding-twin conflation stays open on its original condition**: the repair is a shared base deriving "same (kind, key)" with the differing-binding filter layered on for the stack roster (history in the M83 archive). Promote on any evidence a twin is added. ~~Also open from the M88 review: F11, F12 and F4~~ **all three closed 2026-08-20 by M98, dispositions in D-045**, each carrying its own reopening class there; the full scored list stays in M88's archived Review.
- Longitudinal deferrals (D-013 spec lineage; Builds A/B/C promoted → M25/M26/M27, 2026-07-16; §1.1 sugar → M28 and §1.4 `ssm_ci_accuracy()` occasions → M29, 2026-07-17). Remaining, each with standing rationale against building now: pairwise-deletion occasions semantics (spec §1.3 — ships listwise-only on RR06 R6 estimand grounds; reopening needs a use case + a superseding D-entry); occasions × measures correlation path (spec §1.2a — defer until a concrete use case exists); occasions × contrast × grouping difference-of-differences (spec §1.2b — a new estimand needing its own design pass before any build); Stan companion (spec §5.4 stay-out criteria, revisit only if ≥2 of 3 conditions hold).
- CPM simulation paper (research-paper track, not a package milestone): the engine is written, reviewed, and ratified in `devel/cpm-sim/` (registered plan `devel/cpm-simulation-paper-plan.md`; not yet run). A package-side milestone (e.g., sim-backed guidance in docs/vignettes) is replanned only if the paper's results motivate one.
- ~~Two `axes_scaling_factor()` fitted-diagonal guard defects — the nonpositive-diagonal guard NA-unsafe, and a `+Inf` diagonal scaled instead of refused~~ **graduated 2026-08-04 → M70** (AC5/T5; promotion condition waived at the M70 amendment gate) **and → M71** (AC1/T1–T3); histories in those archives.
- ~~`axes_scaling_factor()` scales silently at a huge-but-finite fitted diagonal~~ **resolved 2026-08-16 by the M89 re-cut** (RR18, D-044): the degeneracy criterion now prices `cov2cor(Σ̂)` at both surfaces under a τ = 1e-6 floor, scale-invariance pinned by test and the load-bearing `p` factor pinned by near-threshold mutants (round-3 F6). The full history — the O1 rescaling counterexample, RR18's 3.4%-wrong-SE finding, the df = 0 division, every correction — is in the M89 archive and D-044; the open τ-calibration question is its own row above.
- ~~`axes_corrected_se()` reports `"unidentified"` for an infinite fitted diagonal~~ **graduated 2026-08-15 → M89** (AC2), on its stated condition, met rather than waived.
- Continuous / infrastructure refactors (fold into the milestone that next touches the code): analytic-CI Hessian recomputation — `cpm_analytic_se` recomputes a Hessian it could reuse (minor perf; oracle-validate when done); the D-003 pole-snap cosmetic; the strict-tier syntax single-sourcing row below. Parked opportunistic fold-ins per M14's scope note, never rejected. (Graduated: RR11 never-NaN gap → M62; the `wc` unit-diagonal assumption → M69.)
- Disposition of the three `devel/` transcription files once M7 archives (deferred out of M40/M41, 2026-07-19): `ar2004-`, `cr2002-` and `m4-zw-transcription.md` are cited from M7's open work log, so they stay byte-untouched until M7 archives; then decide per file between a "superseded by `cairn/references/<citekey>.md`" pointer header and outright retirement — the transcription-protocol records (two-channel diffs, paper-internal inconsistencies found) are the part worth keeping.
- Strict-tier syntax *emission* single-sourcing across the single/multi-group branches in `R/ssm_sem_syntax.R` (descoped from M8 T4, 2026-07-12): the two emitters are structurally distinct and byte-pinned by `exp_strict_*` snapshots — a high-risk rewrite of statistical output for minor DRY gain. Revisit only if that emitter is reworked for another reason.
- CIRCUM free-scaling post-v2.0.0 extensions (deferred out of M18; see its archive + D-009): bootstrap σ CIs; covariance-matrix input — shipping it re-triggers the D-011 paired T calibration at non-unit σ truths first. The point-estimate oracle already shipped in M18.
- **True animation** for trajectory viz: excluded from M37 at its plan gate on minimal-deps + custom-ggproto-coord grounds — revisit only on a concrete teaching use case, and only via the dependency gate (no new Import or Suggests without a D-entry). The rest of the visualization expansion shipped across M30–M37 (D-018).
- **Forward-source prospects (nagy2019/E1, weide2021/E2, rogoza2021/E3, tracey2000/E4)** — four sources Jeff shelved as future research material, characterized in `cairn/references/forward-source-prospects.md` (what each contains, what it could seed, what a taking-up milestone must verify). No per-source page owed until a milestone relies on one (D-023/D-024). tracey2000's shelf copy is an OCR scan → any extraction needs a second channel (M42-D1).
- **FIML follow-ons, post-build** (RR12 B-4/§4; neither rejected): planned-missingness designs (three-form and similar) are excluded by the mandatory zero-joint-coverage refusal even though the structured model stays identified there — support means coverage-aware standardization, shadow and PD machinery; and lavaan's `missing = "two.stage"` as an SE-corrected alternative to one-stage FIML, needing the same FIML-metric standardization first. Promote either on a concrete use case.
- **Axes-reliability deferred-in-spec extensions** (out of M54, per RR09/D-026). Remaining: **block membership on the `circumplex_instrument` class** — M63 takes `blocks =` as an argument only, since no bundled instrument records blocks and a class field would ship empty; revisit if an instrument whose blocks are known is bundled. Quasi-circumplex/unequal-spacing weights stay refused on RR09 §4's standing holding, not deferred. (Everything else graduated across M59–M65; lineage in those archives.)
- **Didactic vignettes → ebook** (2026-08-04 design interview, GP5 note): Jeff may move the teaching content into an ebook and simplify the vignettes to demonstration size. GP5's precision bar travels with the content. Promote only when Jeff opens the project.
- **Norms provenance audit** — the transcription audit itself shipped across M72–M75 (all fifteen instruments verified; per-instrument status in `cairn/references/norms-audit.md`). Open correspondence: **ask Hatcher and Markey where the iis32 and ipipipc norms came from** — neither cited article publishes them, no other venue was found, and the 2018 correspondence is unrecoverable, so both ship an "unconfirmed" credit and a `source-not-identified` ledger disposition. Ask Robert Hatcher (IIS-32: the n = 1380 sample's octant M/SD) and Patrick Markey (IPIP-IPC: Study 2's n = 274 descriptives). Closes on a reply identifying either sample; promote to a milestone only if a reply shows a shipped value wrong.
- ~~Norms fitness and what the standardizing call tells the user~~ **graduated 2026-08-08 → M76 + M77**; the rename question was closed on the merits by RR16 (D-041, reopening conditions there). Still parked: a `getOption()` global mute beside M76's `quiet` argument — promote if `quiet` proves insufficient in loops or knitted documents.
- ~~Per-sample reference-kind field in `Norms[[2]]`~~ **graduated 2026-08-08 → M78** (history in its archive; the kind derivation is recorded in `data-raw/` per IP5).
- ~~`shipped_roster()` and the `roster` argument accept or silently drop what they should refuse~~ **graduated 2026-08-14 → M84 + M85** (histories and corrections in their archives; the twelfth finding declined on the merits). ~~Open remainder from the M80 review (F1): the `note-only-sample` emitter emits no cell for the `anchor` its dedupe key discriminates on, so two rows the key correctly keeps apart emit identically and any reader deduplicating the CSV drops one~~ **declined 2026-08-20 by M98, disposition in D-045** (finding 4): both fixes widen an internal checker's promise for a case that has never occurred. Promote into whichever milestone next opens `audit_norms()`; D-045 additionally reopens it on a source note citing one sample to two different tables actually arriving.
- **IIP T-score conversion tables** (M75 gate): the manual norms the IIP through per-scale T lookup tables (Appendices B/F), not the shipped M/SD z-scoring. Declined: the Mind Garden permission covers means/SDs only, and the printed tables are not the linear transform the manual's own p. 30 describes. Measured 2026-08-07: the divergence is a mild S-shape confined to high scores (median 0.3 T-points; octant-differential spread reaching ~0.44 SD only above raw 11) — smaller than the effect of choosing a different reference sample — and a normalized T would distort the interval metric SSM's cosine fit assumes. Promote only on a fresh permission covering the tables *and* a concrete use case; it would be a new estimand needing its own design pass.
- **CSIG source may print duplicated PA/NO statistics** (M72): `locke2014` Figure 2 (p. 436) prints identical α/M/SD (.73/2.96/.68) for both PA and NO; the package transcribes both faithfully, so the question is about the source (see `cairn/references/locke2014.md`). Flagged to Locke at Jeff's choice. Promote if he confirms an error (a shipped-value fix); leave if the figure is correct.
- **`cais`'s adult-sample norms exceed the instrument's own printed response anchors** (M74 review): sample 2's PA 5.19 / LM 6.52 / NO 6.14 exceed the 1–5 anchors; the leading explanation (2026-08-08) is that **sodano2006 Table 4's M/SD rows are transposed between its CAIS and IAS column blocks** — evidence and ruled-out alternatives in `references/sodano2006.md`. The code half shipped 2026-08-08 (hotfix PR #103, D-040): `norm_standardize()` refuses any shipped sample whose means leave its instrument's anchor range. **M112 withdrew the sample outright (D-052)** on unusability, not on the metric question being settled, so the refusal now has no shipped violator. Open: the author query to Sodano (Tracey copied, 2026-08-08, also carrying the 204-vs-213 *N* question). **This row is NOT promoted by a reply arriving.** A "swapped" reply re-adds corrected values as a numeric change to shipped norms under a fresh gate (D-039); "correct as printed" leaves the withdrawal standing and closes this row. The correspondence stays open either way.
- ~~`data-raw/audit-norms.R` robustness follow-ons~~ **graduated 2026-08-08 → M79 + M80** (histories and corrections in their archives; the untagged-block fallback declined on the merits as deliberate). Open remainder: six sub-threshold M75 review findings against the instrument-tagged-block machinery (untagged-note `claimed`-key collision, loose marker matching, permissive tag extraction, untested abort paths, `"citekey (tag)"` extending the coverage-join mangle; scored 50–72, details in the M75 archive). Promote when the audit next extends to a new instrument — a silently-dropped sample is what extension would hit first.
- **Boundary-regime interval improvement** (post-M4 lineage; 2026-08-04 design interview): interval methods with better coverage where the measured record fails — β ~.77 flat in N at boundary truths (percentile bootstrap), analytic angle CIs outside the band until N ≈ 50000. BCa is refused for circular displacement (D-002); the search space is other interval families or corrections, each needing its own coverage oracle. Until promoted, the `summary()` cautions are the mitigation.
- ~~Heywood-regime user guidance~~ **graduated 2026-08-16 → M92** (vignette section; deliberately qualitative — measured rates return only if the simulation paper publishes them citably). The open residue (bootstrap fits never saw the marker vocabulary) **graduated 2026-08-18 → M94**; its promotion condition had not fired — Jeff overrode the parking at the M94 plan gate.
- **Scaled-statistic residues** (lineage RR13 B-1 → M66/M67/M68/M70, D-036; histories in those archives): still open — the **scaled *difference* test** for nested model comparison (no such comparison in the API today; promote if one appears), and a **Swain/Bartlett-type small-sample mean correction to `T`** (RR14), which oracle mean-recentering shows would essentially close the small-N over-rejection M68 documents rather than fixes. Promote the latter on evidence the documented small-N behaviour misleads users, and only with its own oracle and calibration cells.
- ~~`cpm_fit.R` roxygen unresolvable-link warnings~~ **fixed 2026-08-08 by direct commit** (nine sites, all `@noRd`; the real cost was warning desensitization, so `PROFILE.md`'s consistency-gate now requires a warning-free `document()`).
