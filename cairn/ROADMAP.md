# circumplex Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-09-15 (M129 archived, M126 row pruned.)_

Pre-migration history: `cairn/legacy/` and git log.

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M130 | The invariance-ladder verdict prints as labeled lines | in-progress | — | normal | milestones/M130-ladder-verdict-labeled-lines.md |
| M129 | Vignette prose matches the package's code and printed output | done | M127, M128 | normal | milestones/archive/M129-vignette-prose-matches-output.md |
| M128 | Vignettes show the package calls they teach, not the plumbing around them | done | M126 | normal | milestones/archive/M128-hide-vignette-plumbing.md |
| M127 | Printed reports are shorter and easier to scan | done | — | normal | milestones/archive/M127-shorter-printed-reports.md |

## Candidates

_Candidates carry no milestone ID — an `M<NN>` is assigned at planning time. Legacy IDs (≤ M6) remain valid citations into `cairn/legacy/`; M7 is the first cairn-era ID. Struck rows are tombstones; their histories live in the named archives and git._
- **Vignette output lines past the website's code-box width**, added 2026-09-15 by the M130 plan gate. The pkgdown code box cuts `#>` lines longer than about 80 characters. On master `0f8f3289`, 23 such lines are in `evaluating-circumplex-structure`, `growth-ssm-analysis`, `intermediate-ssm-analysis` and `introduction-to-ssm-analysis`, mostly warnings, notes and a wide data frame. Two more are the lavaan syntax lines in `sem-based-ssm-analysis`. M130 fixes only that vignette's ladder block, because `options(width)` does not wrap warning text or `cat()` output. Promote when a reader reports clipped output, or into whichever milestone next opens those print methods or vignettes.
- **The ladder print tests share the code's formatting and never require a wrap**, added 2026-09-15 by the M130 review (O8, O9): `test_stat()` builds the expected Δχ² string with the code's own `format(round())`, and no test requires a wrapped line. Same class as the M127 O5 row. Promote on a ladder formatting bug reaching a user, or into whichever milestone next opens those tests.
- **The arm64 harness's two uncovered edges** (M121 review, findings 15-16; both stated in `tools/arm64/README.md`, neither fixable by documentation). (i) Only the Dockerfile's base layer is digest-pinned — `apt-get` runs against Debian unstable and both `install.packages()` layers fetch current CRAN, so a rebuild months on is a different image. (ii) The harness runs plain `R CMD check`, not `--as-cran`, and CRAN's arm64 options line is not transcribed in the repo, so whether the flavor uses it is unverified. Promote into whichever milestone next opens the harness, or on a CRAN arm64 result it cannot reproduce.
- **Second-order follow-ons from RR22** (each independent, none blocking). (i) An `ubuntu-24.04-arm` CI job (rec 10) — samples CRAN's BLAS family on every push rather than at release; promote once `setup-r` and arm64-Linux binaries are verified available. (ii) dd-versus-exact assertions at the five anchors, and a `?axes_reliability` sentence saying the certificate's estimate is a property of the fit on this machine (rec 11); promote into whichever milestone next opens those surfaces. (iii) Record this file's CRAN-live posture as a decision (rec 12) — keeping the brackets and detector CRAN-live is defensible, but the lower-risk reading of the same obligation (bracket suite in CI only, refusal-on-every-route on CRAN) has not been weighed on the record; promote at the next gate that opens the file. (iv) The four Known-fragilities items M122 does not take are already routed by the degeneracy row.
- **Let the certificate be the sole conditioning judge** (RR22 rec 9, "C′"). Since M111 the refusal decision is the certificate's, but `axes_pricing_core()`'s `solve(info)` gate predates it and duplicates it badly — a binary refusal keyed to an rcond estimate against a graded estimate keyed to the actual error. Passing `tol = 0` there would remove the whole class of platform-dependent refusal from the shipped package; the exact-singular path still errors, so `"unidentified"` keeps a meaning. Changes when the shipped package emits a number, so it wants its own plan gate and D-entry; its oracle is a sweep over the admitted domain and the M89/M106 near-threshold families showing the certificate refuses every matrix the default gate did. Not for 2.0.1. Promote at a plan gate once 2.0.1 is published.
- **An exported helper from coefficient draws to a trajectory table**, added 2026-09-14 by the M128 plan gate. The growth vignette builds a `ssm_plot_trajectory()` table by hand from model draws at each wave. M128 instead shows one `ssm_draws()` call and hides the loop that builds the table (re-cut 2026-09-14), because an export is an irreversible API decision. Promote if readers cannot rebuild the trajectory table from the shown call and the prose, or if users ask for a helper.
- **The accuracy summary's text checks cannot catch wording drift**, added 2026-09-15 by the M127 review (finding O5). `expect_short_ci_summary()` in `tests/testthat/helper-ci-accuracy-summary.R` compares `summary()` with the current verdict and structure functions, so a reworded caution still passes. The seeded and contrast length checks also skip on CI. M127's review compared old and new text with a one-off script. Promote if a verdict or caution changes wording without a test failing, or into whichever milestone next opens that summary.
- Plain-English follow-ons (M123-M125 plan, 2026-09-14, rules in `cairn/references/plain-vignettes.md`). (i) **Plain-English pass over the three older vignettes**: `introduction-to-ssm-analysis`, `intermediate-ssm-analysis`, `using-instruments` (8, 15 and 30 sentences over 25 words at `125b76db`). Promote after M125, or if a reader finds one hard to follow. The ebook row applies. (ii) **The prose sweep as a merge gate**: CI and testthat run `tools/prose-sweep.R` over rewritten vignettes (nestedtune M088 shape). Review-only for now, because nestedtune needed two follow-ups to fix its gate's parser. Gaps: "Known sweep gaps" on the rules page. Promote after M125, or if a vignette regresses past the sweep.
- **Vignette tables from `knitr::kable()` to gt**, added 2026-09-14 by M124 gate (the maintainer is moving from kable to gt). The vignettes hold 22 `kable` calls, and gt is not a dependency yet, so a switch needs a dependency gate and D-entry. By default gt gives each rendered table a random id, so the `vignette-precompute` staleness check likely needs the id pinned (not yet tried). Promote when the maintainer schedules the switch.
- **Windows compile cost in the CRAN check** — the install step carries no timing in any of the three Windows logs read, so the compile cost stays unmeasured. M120's win-builder run bounds the whole untimed residual at 105 s against a 3 min 23 s - 4 min 23 s overall, so the promotion condition — the install step alone eating the budget — is **not met**. Promote when a log finally times the install step and it dominates, or when a check cannot reach 8 minutes with tests and vignettes at zero.
- **CRAN-dark boundary invariants** (M120 review, G4). M120 moved 416 blocks off CRAN's own check, and five that a past milestone added against a specific previously-fixed bug now have no CRAN-live equivalent able to see that defect: `ssm_analyze_long`'s first-appearance occasion order (its designated live block uses `T1`/`T2`, where both orders coincide, so it is blind to a flip); the SEM latent group contrast near ±180° and the reference-group relabel flip; `test-ssm_trajectory.R`'s seam battery; `test-cpm_boundary.R`'s free-scaling pole recovery (D-009); `angle_dist`'s exact-half-turn atom. **Not an AC4 failure** — AC4 promises one live block per *exported* function reddening under a magnitude or wrap defect, which was measured. CI runs all of them every push, so the exposure is CRAN's own check only. Promote into whichever milestone next opens these files, or on a wrap/order regression reaching CRAN.
- **M120 instrument strength** (M120 review, G5+G6). The `tools/m120-*` instruments and the vignette staleness guard credit more than they check — eleven weaknesses across self-sourced domains, skip detection by literal `skip_on_cran()` line only (so `skip_if_not_installed()` makes two AC4(iii) designations silently conditional), chunk-granular volatile regions, an unrefreshed committed `tools/m120-domain.txt`, uncompared figures, and a test body reseeding the global RNG. Enumerated in full in the M120 archive's Review; not restated here. Promote into whichever milestone next opens these instruments, or when one is relied on outside M120.
- ~~The master-red alert stays silent on `startup_failure` and `timed_out`~~ **graduated 2026-08-21 → M99**; experiments M101–M103 (probe repo `jmgirard/gha-startup-failure-probe`; ledger in `.github/workflows/master-red-alert.yaml`'s header; histories in those archives). Open, each also promoting into whichever milestone next opens the alert workflow: (a) a scheduled sweep as a second detector (declined at the M99 gate) — promote when a master break goes unannounced; (b) whether `workflow_run` fires for a run that fails to start — promote when its self-retrigger hazard is worth designing around; (c) what produces `startup_failure` at all — promote on such a run seen in the wild.
- **The graduated doctrine module has no size discipline of its own** (M104 review, [O] F11 and F6): `cairn/test-craft.md` is 26 lines / 7,293 bytes — observed 2026-08-22 — and no cap or validator watches it. `cairn_validate`'s `LINE_CAPS` lives in the cairn plugin repo, so mechanizing a cap belongs there and is owed a candidate row in that repo; this repo's remedy is the budget the module's own header states (< 35 lines, < 9,000 bytes), read at ROADMAP hygiene passes. The **ownership exit** — folding the module into `PROFILE.md`'s `test-doctrine` slot — is refused by D-046, which names "a preference for fewer tracking files" explicitly insufficient to reopen; superseding it is its own decision. Reachability is not at issue. Promote on the module actually drifting past its stated budget.
- **A Codecov upload token is committed in plaintext** (M95 review, [O] diff-bug F14): `codecov.yml:36` carries `token: b885341a-…`, committed since M12 (7480a67a), while `.github/workflows/test-coverage.yaml:94` already supplies `secrets.CODECOV_TOKEN` — so the committed copy is redundant as well as readable in a public repo. Out of M95's diff and left there at its review gate (Jeff's call): rotating it happens in Jeff's Codecov account, not in the repo. Promote when Jeff has rotated the token, at which point the dead line is deleted in the same pass; deleting it before rotation removes the evidence without closing the exposure.
- ~~Master's CI gates: GitHub-native branch protection, and the PR check gate blind to the release platforms~~ **graduated → M105 (2026-08-22), M93 (2026-08-17) and M96 (2026-08-18)**; histories in those archives, the two rulesets in D-047 and pinned by `tools/branch-protection.json`. **Open remainder, declined at the M105 gate:** an always-reporting aggregator job in `R-CMD-check.yaml` so a required check reports on `cairn/**`-only PRs, which today report none (PR #133). Declined because the maintainer needs `always` bypass for direct docs-only pushes regardless, so the aggregator buys enforcement only against a non-admin contributor while reopening M93's deliberately fail-closed classifier. Promote when a non-admin contributor path exists, or into whichever milestone next opens `R-CMD-check.yaml`.
- ~~The degeneracy criterion's accuracy floor, and the guards that skipped under check~~ **graduated → M108–M111, M113–M117, M118** (all eleven shipped; histories in those archives, D-044/D-048–D-054, RR18–RR21). Remainder in `cairn/DESIGN.md`: six latent defects under **Known fragilities** (count corrected 2026-09-05) — promote into whichever milestone next opens the certificate, its tests or the oracle driver — and the rest under **Accepted limitations**. Also open: (i) the relocated exemplar-B fixture has no generator or seed (profile provenance rule) — promote on a decision about provenance for an artifact with no reproducible origin; (ii) nothing detects a repeat of the silent skip if lavaan renames again; (iii) RR21 rec 5 (surface the certificate's estimate on computed fits) wants its own GP4 gate. M113's F1/F2 rejected as defects; certifying the derivation prose stays descoped.
- ~~The norms-audit roster builder's remaining unnamed and undiscriminated shapes~~ **graduated 2026-08-15 → M86** (all six M84 review findings; histories in the M84/M86 archives). Open remainder, parked at the M87 plan gate: **whether the audit script's ~225-line argument-guard surface should shrink at all** — `validate_batch()`, `validate_roster()` and `roster_from_objects()` in `data-raw/audit-norms.R` are called only by the test suite and the run block, though `validate_batch()` still protects the one human-reached path (a hand-edit of `AUDIT_BATCH` when instrument 16 is added). Promote on evidence no maintainer path reaches the roster-side guards, or when a later milestone opens that file anyway.
- ~~The norms-audit cross-discrimination matrix reddens against a same-binding twin~~ — the matrix was deleted by M87 and the walk-helper half (M87 review F13) **graduated 2026-08-15 → M88**, but the **same-binding-twin conflation stays open on its original condition**: the repair is a shared base deriving "same (kind, key)" with the differing-binding filter layered on for the stack roster (history in the M83 archive). Promote on any evidence a twin is added. ~~Also open from the M88 review: F11, F12 and F4~~ **all three closed 2026-08-20 by M98, dispositions in D-045**; the full scored list stays in M88's archived Review.
- Longitudinal deferrals (D-013 spec lineage; Builds A/B/C promoted → M25/M26/M27, 2026-07-16; §1.1 sugar → M28 and §1.4 `ssm_ci_accuracy()` occasions → M29, 2026-07-17). Remaining, each with standing rationale against building now: pairwise-deletion occasions semantics (spec §1.3 — ships listwise-only on RR06 R6 estimand grounds; reopening needs a use case plus a superseding D-entry); occasions × measures correlation path (§1.2a — defer until a concrete use case exists); occasions × contrast × grouping difference-of-differences (§1.2b — a new estimand needing its own design pass); Stan companion (§5.4 stay-out criteria, revisit only if ≥2 of 3 conditions hold).
- CPM simulation paper (research-paper track, not a package milestone): the engine is written, reviewed, and ratified in `devel/cpm-sim/` (registered plan `devel/cpm-simulation-paper-plan.md`; not yet run). A package-side milestone (e.g., sim-backed guidance in docs/vignettes) is replanned only if the paper's results motivate one.
- Continuous / infrastructure refactors (fold into the milestone that next touches the code): analytic-CI Hessian recomputation — `cpm_analytic_se` recomputes a Hessian it could reuse (minor perf; oracle-validate when done); the D-003 pole-snap cosmetic; the strict-tier syntax single-sourcing row below. Parked opportunistic fold-ins per M14's scope note, never rejected. (Graduated: RR11 never-NaN gap → M62; the `wc` unit-diagonal assumption → M69.)
- Disposition of the three `devel/` transcription files once M7 archives (deferred out of M40/M41, 2026-07-19): `ar2004-`, `cr2002-` and `m4-zw-transcription.md` are cited from M7's open work log, so they stay byte-untouched until M7 archives; then decide per file between a "superseded by `cairn/references/<citekey>.md`" pointer header and outright retirement — the transcription-protocol records (two-channel diffs, paper-internal inconsistencies found) are the part worth keeping.
- Strict-tier syntax *emission* single-sourcing across the single/multi-group branches in `R/ssm_sem_syntax.R` (descoped from M8 T4, 2026-07-12): the two emitters are structurally distinct and byte-pinned by `exp_strict_*` snapshots — a high-risk rewrite of statistical output for minor DRY gain. Revisit only if that emitter is reworked for another reason.
- CIRCUM free-scaling post-v2.0.0 extensions (deferred out of M18; see its archive + D-009): bootstrap σ CIs; covariance-matrix input — shipping it re-triggers the D-011 paired T calibration at non-unit σ truths first. The point-estimate oracle already shipped in M18.
- **True animation** for trajectory viz: excluded from M37 at its plan gate on minimal-deps + custom-ggproto-coord grounds — revisit only on a concrete teaching use case, and only via the dependency gate (no new Import or Suggests without a D-entry). The rest of the visualization expansion shipped across M30–M37 (D-018).
- **Forward-source prospects (nagy2019/E1, weide2021/E2, rogoza2021/E3, tracey2000/E4)** — four sources Jeff shelved as future research material, characterized in `cairn/references/forward-source-prospects.md` (what each contains, what it could seed, what a taking-up milestone must verify). No per-source page owed until a milestone relies on one (D-023/D-024). tracey2000's shelf copy is an OCR scan → any extraction needs a second channel (M42-D1).
- **FIML follow-ons, post-build** (RR12 B-4/§4; neither rejected): planned-missingness designs (three-form and similar) are excluded by the mandatory zero-joint-coverage refusal even though the structured model stays identified there — support means coverage-aware standardization, shadow and PD machinery; and lavaan's `missing = "two.stage"` as an SE-corrected alternative to one-stage FIML, needing the same FIML-metric standardization first. Promote either on a concrete use case.
- **Axes-reliability deferred-in-spec extensions** (out of M54, per RR09/D-026). Remaining: **block membership on the `circumplex_instrument` class** — M63 takes `blocks =` as an argument only, since no bundled instrument records blocks and a class field would ship empty; revisit if an instrument whose blocks are known is bundled. Quasi-circumplex/unequal-spacing weights stay refused on RR09 §4's standing holding, not deferred. (Everything else graduated across M59–M65; lineage in those archives.)
- **Didactic vignettes → ebook** (2026-08-04 design interview, GP5 note): Jeff may move the teaching content into an ebook and simplify the vignettes to demonstration size. GP5's precision bar travels with the content. Promote only when Jeff opens the project.
- **Norms provenance audit** — the transcription audit itself shipped across M72–M75 (all fifteen instruments verified; per-instrument status in `cairn/references/norms-audit.md`). Open correspondence: **ask Hatcher and Markey where the iis32 and ipipipc norms came from** — neither cited article publishes them, no other venue was found, and the 2018 correspondence is unrecoverable, so both ship an "unconfirmed" credit and a `source-not-identified` ledger disposition. Ask Robert Hatcher (IIS-32: the n = 1380 sample's octant M/SD) and Patrick Markey (IPIP-IPC: Study 2's n = 274 descriptives). Closes on a reply identifying either sample; promote to a milestone only if a reply shows a shipped value wrong.
- ~~Norms fitness and what the standardizing call tells the user~~ **graduated 2026-08-08 → M76 + M77**; the rename question was closed on the merits by RR16 (D-041, reopening conditions there). Still parked: a `getOption()` global mute beside M76's `quiet` argument — promote if `quiet` proves insufficient in loops or knitted documents.
- ~~`shipped_roster()` and the `roster` argument accept or silently drop what they should refuse~~ **graduated 2026-08-14 → M84 + M85** (histories in those archives; the twelfth finding declined on the merits). ~~Open remainder from the M80 review (F1): the `note-only-sample` emitter emits no cell for the `anchor` its dedupe key discriminates on~~ **declined 2026-08-20 by M98, disposition in D-045** (finding 4): both fixes widen an internal checker's promise for a case that has never occurred. Promote into whichever milestone next opens `audit_norms()`; D-045 also reopens it if a source note citing one sample to two different tables actually arrives.
- **IIP T-score conversion tables** (M75 gate): the manual norms the IIP through per-scale T lookup tables (Appendices B/F), not the shipped M/SD z-scoring. Declined: the Mind Garden permission covers means/SDs only, and the printed tables are not the linear transform the manual's own p. 30 describes. Measured 2026-08-07: the divergence is a mild S-shape confined to high scores (median 0.3 T-points; octant-differential spread reaching ~0.44 SD only above raw 11) — smaller than the effect of choosing a different reference sample — and a normalized T would distort the interval metric SSM's cosine fit assumes. Promote only on a fresh permission covering the tables *and* a concrete use case; it would be a new estimand needing its own design pass.
- **CSIG source may print duplicated PA/NO statistics** (M72): `locke2014` Figure 2 (p. 436) prints identical α/M/SD (.73/2.96/.68) for both PA and NO; the package transcribes both faithfully, so the question is about the source (see `cairn/references/locke2014.md`). Flagged to Locke at Jeff's choice. Promote if he confirms an error (a shipped-value fix); leave if the figure is correct.
- **`cais`'s adult-sample norms exceed the instrument's own printed response anchors** (M74 review): sample 2's PA 5.19 / LM 6.52 / NO 6.14 exceed the 1–5 anchors; the leading explanation (2026-08-08) is that **sodano2006 Table 4's M/SD rows are transposed between its CAIS and IAS column blocks** — evidence and ruled-out alternatives in `references/sodano2006.md`. The code half shipped 2026-08-08 (hotfix PR #103, D-040): `norm_standardize()` refuses any shipped sample whose means leave its instrument's anchor range. **M112 withdrew the sample outright (D-052)** on unusability, so the refusal has no shipped violator. Open: the author query to Sodano (Tracey copied, 2026-08-08, also carrying the 204-vs-213 *N* question). **A reply does NOT promote this row**: "swapped" re-adds corrected values under a fresh gate (D-039); "correct as printed" closes it.
- ~~`data-raw/audit-norms.R` robustness follow-ons~~ **graduated 2026-08-08 → M79 + M80** (histories in those archives; the untagged-block fallback declined as deliberate). Open remainder: six sub-threshold M75 review findings against the instrument-tagged-block machinery (details in the M75 archive). Promote when the audit next extends to a new instrument — a silently-dropped sample is what extension would hit first.
- **Boundary-regime interval improvement** (post-M4 lineage; 2026-08-04 design interview): interval methods with better coverage where the measured record fails — β ~.77 flat in N at boundary truths (percentile bootstrap), analytic angle CIs outside the band until N ≈ 50000. BCa is refused for circular displacement (D-002); the search space is other interval families or corrections, each needing its own coverage oracle. Until promoted, the `summary()` cautions are the mitigation.
- **Scaled-statistic residues** (lineage RR13 B-1 → M66/M67/M68/M70, D-036; histories in those archives): still open — the **scaled *difference* test** for nested model comparison (no such comparison in the API today; promote if one appears), and a **Swain/Bartlett-type small-sample mean correction to `T`** (RR14), which oracle mean-recentering shows would essentially close the small-N over-rejection M68 documents rather than fixes. Promote the latter on evidence the documented small-N behaviour misleads users, and only with its own oracle and calibration cells.
