# circumplex Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-08-13 (M81 merged and archived, PR #108, after a review return on AC1 and an RB17/RR17 escalation; its successor candidate row stands. M75's terminal row pruned for the 5-row retention. No lesson added or retired: M81's snow-worker finding folded into the existing installed-vs-`load_all()` line, its gfortran build quirk being machine setup, not a repo lesson)._

Pre-migration history: see `cairn/legacy/` and git log.

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M78 | Per-sample reference-kind field in the shipped norms | done | — | high | milestones/archive/M78-norms-reference-kind-field.md |
| M79 | Bind the norms audit's batch to the shipped roster | done | — | normal | milestones/archive/M79-norms-audit-roster-binding.md |
| M81 | Enumerate the norms-audit abort registry from the script's parse tree | done | M79 | normal | milestones/archive/M81-norms-audit-abort-registry.md |
| M80 | Give the norms-audit coverage report a machine-readable key | in-progress | M79, M81 | normal | milestones/M80-norms-audit-report-schema.md |
| M7 | v2.0.0 CRAN release preparation | blocked | M25, M26, M27, M31, M32, M33, M34, M35, M36, M37, M38, M54, M55 | high | milestones/M7-v2-release-prep.md |
| M76 | Disclose the reference sample at the standardizing call site | done | — | normal | milestones/archive/M76-norms-call-site-disclosure.md |
| M77 | Say precisely what the shipped reference statistics are | done | M76 | normal | milestones/archive/M77-norms-fitness-docs.md |

## Candidates

_Candidates carry no milestone ID — an `M<NN>` is assigned only at planning time. Legacy milestone/decision IDs (≤ M6) remain valid citations into `cairn/legacy/`; M7 is the first cairn-era ID._

- **Norms-audit abort enumeration, successor to M81** (2026-08-09, RR17 rev 2 BC6-BC11, partitioned there and not yet planned): the denylist sweep for alternative abort spellings (`rlang::abort`, `cli_abort`, `do.call`-dispatched and aliased heads), the composite site identity `(kind, enclosing binding, key, ordinal)` with build-time duplicate refusal, stack-bound fixtures for shared-key sites, matcher floors with the C-locale pin, and the cross-discrimination matrix over declared pairs. Depends on M81, strictly: all of it quantifies over the classification-complete enumeration M81 ships. RR17 flags BC7+BC8+BC10 as inseparable — cutting them apart reproduces the joint-satisfiability failure its own revision 1 demonstrated. Promote once M81 merges; the criteria carry measured baselines and get their own ingest audit at plan time, revision 1 having twice reintroduced an unbounded claim.
- Longitudinal deferrals (D-013 spec lineage; Builds A/B/C promoted → M25/M26/M27, 2026-07-16; §1.1 sugar → M28 and §1.4 `ssm_ci_accuracy()` occasions → M29, 2026-07-17). Remaining, each with standing rationale against building now: pairwise-deletion occasions semantics (spec §1.3 — ships listwise-only on RR06 R6 estimand grounds; reopening needs a use case + a superseding D-entry); occasions × measures correlation path (spec §1.2a — defer until a concrete use case exists); occasions × contrast × grouping difference-of-differences (spec §1.2b — a new estimand needing its own design pass, i.e. a design milestone before any build); Stan companion (spec §5.4 stay-out criteria, revisit only if ≥2 of 3 conditions hold).
- CPM simulation paper (research-paper track, not a package milestone): the engine is written, reviewed, and ratified in `devel/cpm-sim/` (registered plan `devel/cpm-simulation-paper-plan.md`; not yet run). A package-side milestone (e.g., sim-backed guidance in docs/vignettes) is replanned only if the paper's results motivate one.
- ~~**`axes_scaling_factor()`'s nonpositive-diagonal guard is NA-unsafe** (`R/axes_scaled_fit.R:103`, shipped by M68)~~ **graduated 2026-08-04 → M70** (AC5/T5). Captured at the M69 round-2 review (A1, scored 48 — out of M69's scope because the guard predates it): `any(diag(sigma) <= 0)` is `NA` on an `NA`/`NaN` diagonal entry, so `if (NA)` errors instead of returning the named-reason NA its header promises, while M69's fix left the sibling `axes_corrected_se()` NA-safe. This row's promotion condition — "whichever milestone next opens `R/axes_scaled_fit.R`" — was **waived at the M70 amendment gate, not met**: M70 opens the caller (`R/axes_reliability.R`) and the guard's own test file, not the guard's source file. Reason-code parity between the two surfaces was declined at the same gate (M70 Out).
- ~~**`axes_scaling_factor()` computes a factor from a `+Inf` fitted diagonal instead of refusing** (`R/axes_scaled_fit.R:116`)~~ **graduated 2026-08-04 → M71** (AC1/T1–T3): the guard tests `<= 0`, which `+Inf` fails, and `cov2cor()` then maps that row/column to zeros with a unit diagonal rather than to `NaN`, so `solve()` succeeds and `all(is.finite(si))` is `TRUE` — a scaling factor is computed from a corrupted matrix and reported as a corrected statistic, the one failure D-036/M68-D1 name as undetectable by the user. Measured live at the M70 review (F5, scored 82; the introduced half, a comment overclaiming a general non-finite guarantee, was fixed there). Pre-existing: `<= 0` has never caught `+Inf`, so M70's `na.rm` fix neither caused nor widened it. The fix is to refuse on `!all(is.finite(diag(sigma)))` and add an `Inf` case to the AC5 regression test. Not a hotfix on the same reasoning the sibling row carried: `axes_scaling_factor()` is unexported and no converged lavaan fit is known to produce an infinite fitted diagonal. Promote into whichever milestone next opens `R/axes_scaled_fit.R`, or on any evidence a real fit can reach it.
- **`axes_scaling_factor()` scales silently at a huge-but-finite fitted diagonal** (`R/axes_scaled_fit.R:145`): measured at the M71 review (F5→F1, scored 15 as out of M71's scope, reproduced live at the gate), `sigma[4, 4] <- 1e10` returns `scale = 0.9579018` with `reason = NULL` and no warning, while `axes_corrected_se()` on the same Σ̂ refuses with `"unidentified"` — so one surface reports NA SEs and the other reports scaled fit statistics from the same degenerate matrix. M71 refused the `+Inf` endpoint of that continuum, which the row it graduated conceded "no converged lavaan fit is known to produce"; the finite neighbourhood is the reachable part and is unguarded. Not planned into M71 because its plan gate scoped the milestone to `+Inf` deliberately. Promote on any evidence a real fit reaches the finite regime, or if the two surfaces' disagreement is reported. Deciding it needs a *criterion* for degeneracy, not another point guard — likely a conditioning or magnitude test on the implied correlation matrix, and possibly its own oracle.
- **`axes_corrected_se()` reports `"unidentified"` for an infinite fitted diagonal** (`R/axes_corrected_se.R:241`): measured at the M71 criteria audit, `+Inf` at all 24 diagonal positions of the octant probe already refuses there with all three vectors NA and a warning — it prices the raw `sigma` before normalizing, so the rank-deficient `info` is caught at `:162-163`. No wrong number is ever computed, so this is a label defect only, and relabelling it to M71's `"infinite_diagonal"` was **declined at the M71 plan gate** (M71 Out) on the same ground M70 declined reason-code parity between the two surfaces. Promote on a user reporting the label, or on any need for the two surfaces to agree on a shared reason vocabulary.
- Continuous / infrastructure refactors (fold into the milestone that next touches the code): analytic-CI Hessian recomputation — `cpm_analytic_se` recomputes a Hessian it could reuse (minor perf; oracle-validate when done). A parked opportunistic fold-in per M14's scope note (alongside the D-003 pole-snap cosmetic and the strict-tier syntax single-sourcing row below), never rejected. Graduated from this row: the RR11 Beyond 2 never-NaN gap absorbed 2026-07-26 (`axes_reliability()`'s boundary guard missing ξ1 ≥ 1) → **M62**, which also carries the unvalidated `sd` argument found at its plan gate; and the **unit-diagonal assumption in `axes_corrected_se()`'s `wc` construction** → **M69** (2026-08-03), whose plan-gate criteria audit measured the discrepancy live at 1.05e-3 relative on ξ1 — not the 0.3% this row had carried across from the factor M68 shares machinery with — and found the anchor drifted to `R/axes_corrected_se.R:156-158` (M68, corrected M69).
- Disposition of the three `devel/` transcription files once M7 archives (deferred out of M40/M41's Out clauses, 2026-07-19): `ar2004-`, `cr2002-`, and `m4-zw-transcription.md` are cited by name from M7's open work log, so they stay byte-untouched until M7 is archived. Once their source notes exist (M40/M41) and M7 is done, decide whether each gets a "superseded by `cairn/references/<citekey>.md`" pointer header or is retired outright — the transcription *protocol* records (two-channel diffs, paper-internal inconsistencies found) are the part worth keeping, not the values, which the source notes will carry with anchors.
- Strict-tier syntax *emission* single-sourcing across the single/multi-group branches in `R/ssm_sem_syntax.R` (descoped from M8 T4, 2026-07-12): the two emitters are structurally distinct (plain vs `c()`-vector cross-group labels) and byte-pinned by `exp_strict_*` snapshots — a high-risk rewrite of statistical output for minor DRY gain. Revisit only if that emitter is reworked for another reason.
- CIRCUM free-scaling post-v2.0.0 extensions (deferred out of M18; see `milestones/archive/M18-circum-free-scaling-build.md` + D-009): bootstrap σ CIs; covariance-matrix input — shipping it re-triggers the D-011 paired T calibration at non-unit σ truths first. The point-estimate oracle (frozen Grassi App. A + live OpenMx) already shipped in M18.
- **True animation** for trajectory viz (gganimate or otherwise): excluded from M37 (the static-arrowed-paths build) at its plan gate on minimal-deps + custom-ggproto-coord grounds, and stays a candidate — revisit only if a concrete teaching use case appears, and only via the dependency gate (no new Import or Suggests without a D-entry). The rest of the visualization expansion shipped across M30–M37 and folded into v2.0.0 (D-018; `ssm_plot_*` retained as convenience wrappers per D-018b).
- **Forward-source prospects (nagy2019/E1, weide2021/E2, rogoza2021/E3, tracey2000/E4)** — four sources Jeff deliberately shelved as future research material, characterized in `cairn/references/forward-source-prospects.md` (the M46 survey synthesis note: what each contains, what it could seed — covariate-CI oracle, Bayesian-vs-ML inference oracle, three-step SSM+Procrustes workflow, `fit_structure()` evaluation reference — and what a taking-up milestone must verify). Captured by intent, not citation count; no per-source `<citekey>.md` page owed until a milestone comes to rely on one (D-023/D-024). tracey2000's shelf copy is an OCR scan → any extraction needs a second channel (M42-D1).
- ~~CI dependency-install cost — remaining trim after M52~~ **closed 2026-07-25 at the M58 plan gate** (lineage M51 scope note → M52 took brms → M58). Its two halves split: the OpenMx/glmmTMB removal it named is **declined outright** by D-029 (the premise went stale when M54's `skip_on_cran`-free test file landed; install is under 7% of a run), while the un-trimmed pkgdown lockfile it never noticed is M58's scope.
- **FIML follow-ons, post-build** (out of RR12's B-4 and §4, recorded 2026-07-26; neither rejected): **planned-missingness designs** (three-form and similar) are excluded by the build's mandatory zero-joint-coverage refusal even though RR12 V-F shows the *structured* model stays identified there — supporting them means making the standardization, shadow and PD machinery coverage-aware; and lavaan's **`missing = "two.stage"`** as an SE-corrected alternative to one-stage FIML, which would need the same FIML-metric standardization first and buys nothing until a case appears where one-stage's observed-information SEs are inadequate. Promote either on a concrete use case.
- **Axes-reliability deferred-in-spec extensions** (out of M54, per RR09/D-026). Remaining: **block membership on the `circumplex_instrument` class** — M63 takes `blocks =` as an argument only, since `Items` is `Number, Text` and no bundled instrument records blocks, so a class field would ship empty; revisit if an instrument whose blocks are known is bundled. Quasi-circumplex/unequal-spacing weights stay refused on RR09 §4's standing holding, not deferred. No new dependency for any of it (lavaan/OpenMx already Suggests). Graduated from this row: the correlation-matrix input → **M59**; non-octant equally spaced types (b, c) → **M60**; single-item types (e, f) → **M61**; blockwise ζ2 → **M63**; **FIML on items** → **M64** (2026-07-26, escalated to a Fable brief rather than built directly; **D-033 = GO**, build row above — this row's premise that lavaan's `missing = "ml"` mean structure conflicts with the model was **corrected at the M64 plan gate**: lavaan frees every item intercept itself, so the mean structure is saturated and costs no df, and RR12 located the real problem elsewhere, in the standardizing moments). **D-026's deferral list is now empty**; the block-membership item above was never on it. The FIML **build** row graduated from here 2026-07-26 → **M65** (`Driving RR: RR12`, BC1–BC16 ingested verbatim; its planning note — BC10/BC13's ≥200 replicates will not fit an ordinary `devtools::test()` run, M64-D8 — is answered by M65 T6's script + stored `.rds` + live-smoke harness). Lineage: `strack2013` M48 capture → M53 design (source note `strack2013.md`) → M54 build → M55 vignette.
- **Didactic vignettes → ebook** (2026-08-04 design interview, GP5 note): Jeff may at some point move the teaching content into an ebook and simplify the vignettes to demonstration size. GP5's precision bar travels with the content wherever it lives. Promote only when Jeff opens the project; nothing package-side blocks on it.
- **Norms provenance audit** (2026-08-04 design interview, wart routing): re-verify the shipped instrument norms in `data/` against their published sources — a transcription audit with provenance records per instrument (source, table, page anchors), since nothing currently validates them and a transcription error silently corrupts `norm_standardize()` for every user of that instrument. Highest-stakes of the four interview-confirmed warts; two-channel extraction per M42-D1 where the source is a scan. Batch 1 (csie, csig, csip, csiv, iitc) **landed 2026-08-06 as M72**; **batch 2 (iis32, iis64, ipipipc, isc) is M73** and **batch 3 (cais, iei, igicr, iipsc) → M74**, which between them take up all six of M72's logged review findings (join key ignores `Sample` → M74 AC1; the `Reference` containment rule and the authored note-side credit → M73 AC5; the test pin list unbound to `norms-audit.md`'s verdicts → M73 AC6; the empty-ledger stamp crash → M73 AC4; `?iitc`'s "(in press)" → M73 AC7). **New open item from M73: ask Hatcher and Markey where the iis32 and ipipipc norms came from.** Neither cited article publishes them (hatcher2012 reports no descriptive statistics; markey2009 reports them only for a sample other than the shipped n = 274), a web search found no other venue, and the 2018 correspondence that supplied them is unrecoverable — so both instruments now ship an "unconfirmed" norms credit and a `source-not-identified` ledger disposition rather than a wrong number. Ask Robert Hatcher (IIS-32: the n = 1380 sample and its octant M/SD) and Patrick Markey (IPIP-IPC: the Study 2 n = 274 descriptives). Closes on a reply identifying either sample, which turns that instrument's disposition into an ordinary source note; promote to a milestone only if a reply shows a shipped value is wrong. Remaining here: **iip32/iip64**, which M72 recorded as needing an unobtainable-source disposition — **corrected 2026-08-06 at the M73 plan gate**: Jeff holds publisher permission to ship the IIP norming functions but not item text, so these are ordinary audit targets, not an unobtainable source. **Graduated 2026-08-07 → M75** (batch 4), the manual now being on the shelf. This row's further claim that "the item-map field is by-design absent for these two" was **false and is corrected here (M73, corrected M75)**: both ship a complete item→octant key in `Scales$Items` (1–32 and 1–64), M74's partition sweep already verifies it, and the maintainer confirmed 2026-08-07 that scoring the items is permitted — only item *text* is unlicensed, and that is what the `$Items` placeholder withholds. ~~New open item from M75: ask Mind Garden for the 2000 edition~~ **closed 2026-08-07 during M75 implementation, never opened** (M75 plan, corrected M75). The plan gate premised it on the 3rd edition printing no IIP-32 descriptives; it prints them — **Table F.5, p. 91**, inside Appendix F after the T-score tables rather than beside the IIP-64's Table 4.4 — and all 48 shipped iip32 values reconcile exactly against it at `divisor = 4`, as all 48 iip64 values do against Table 4.4 (pp. 27–29) at `divisor = 8`. So both instruments are ordinary transcription audits with no unsourced disposition and no need for the earlier edition. Per-instrument status stays in `cairn/references/norms-audit.md`.
- ~~**Norms fitness and what the standardizing call tells the user**~~ **graduated 2026-08-08 -> M76** (call-site disclosure) **and M77** (what the docs claim), split at the plan gate because the doc half describes the behavior the code half ships. Two of this row's measured figures were **false and are corrected here (M74, corrected 2026-08-08)**: the shipped samples are **not** all American -- csig's single sample is "MTurkers from US, Canada, and India" -- and **11 of the 24**, not 15, carry a college- or undergraduate-student `Population` label (the 15 was the instrument count copied one line down). Verified figures: 24 samples across 15 instruments, 7 with n < 300, 6 of them the IIP national standardization sample, and reference *choice* moving scores 0.44 SD on average and 0.78 at the extreme against ~0.12 SD from reference-moment sampling error at n = 106. ~~Declined at the M76 gate and parked here: renaming the `Population` column or any `norm*` identifier.~~ **Closed on the merits 2026-08-08 by RR16, not deferred (D-041)**: "norms" is the interpersonal-circumplex field's own word for these tables, the hazard lives in claims rather than identifiers and is closed by M76+M77, and a rename would understate the 6 genuine standardization samples while it stopped overstating the other 18. What a rename would have flattened ships instead as the kind-field row below. Reopening conditions are in D-041. Also parked: a `getOption()` global mute beside M76's `quiet` argument -- promote if `quiet` proves insufficient in loops or knitted documents.
- ~~**Per-sample reference-kind field in `Norms[[2]]`**~~ **graduated 2026-08-08 → M78**, which takes both wording fixes below into AC4 (widened middle kind; the third kind classifies the statistics, not the sample), the `data-raw/` derivation record into AC5, and adds a task the plan gate's criteria audit surfaced: `norms-audit.md`'s citekey map marks no standardization sample, so the audit record cannot support the derivation until a per-sample kind table is added to it. Original capture (2026-08-08, RR16 R3/BC3, routed here at the ingest gate rather than into M76 or M77, both of whose Scope excludes a data change): add a machine-readable column classifying each shipped sample's reference-distribution kind under three values -- drawn-to-represent standardization sample, identified published source, no identified source -- populated from `cairn/references/norms-audit.md`, printed by `norms()`, and carried by M76's disclosure message and returned attribute (RR16 B2, which BC3 itself does not require). This is what RR16 chose over a rename: it preserves the distinction the data contains (6 of 24 samples genuinely are standardization samples) at the two moments a user meets it -- reading `norms()` to choose, and calling `norm_standardize()` to use. Verified 2026-08-08 against the source tree: 24 samples, partitioning 6 (iip32 and iip64) / 16 / 2 (iis32 sample 1, ipipipc sample 1); RR16's own falsifier F2 does not fire, only csie sample 1 and csiv sample 1 being hard to place, under its threshold of 4. **Two wording fixes the plan must carry:** widen the middle kind past "identified-*study* participant pool", since the audit ties csie's and csiv's website tables to no study sample; and say whether "no identified source" means the sample or its statistics, ipipipc sample 1 having an identified study (markey2009 Study 2, n = 274) but unsourced M/SD. **Also unowned and needed:** IP5 requires the derivation be recorded in `data-raw/`, the kind being a package-authored inference rather than a published value -- the same standing `Population` already has. **Plan before the v2.0.0 submission:** GP4 is inert until 2.0.0 ships (D-040), and `norms()`'s printed output is a GP4 commitment (D-039), so after submission this needs its own `irreversible-api` gate. Sequencing against M77 is a plan-gate question: landing it first lets M77's characterization passage teach the field rather than describe the samples in prose alone.
- **`norm_standardize()`'s roxygen promises a custom-norms path the signature does not offer** (2026-08-08, RR16 B1, unrouted at the RR16 ingest gate and captured here at the M78 plan gate): the help page opens with "normative data (from the package or custom)" (`R/tidying_functions.R:139`), but `instrument` must be a `circumplex_instrument`, so "custom" is reachable only by hand-constructing an instrument object, which no documentation teaches. M77's sweep pattern matched none of the phrase, and M78 declines it as unrelated to the kind field. Either drop "or custom" or say what it means — a decision small enough for the trivial tier once made, and worth folding into whichever milestone next opens that roxygen block.
- **`shipped_roster()` and the `roster` argument accept or silently drop what they should refuse** (2026-08-09, graduated from M79's return-4 below-threshold findings; none reached the action bar, all five measured against the code at `e6a7595d`): the roster sweep that M79 built to stop the audit reporting a clean run over data it never read has five latent holes of its own, all in `data-raw/audit-norms.R` around `:408-430` and `:470-475`. A caller-supplied `roster` is unvalidated, so an empty one, or one whose columns are misspelt `Instrument`/`Sample`, audits clean with gap count 0 (45) — AC1 sanctioned it, an explicit roster being the fixture path and `batch` unvalidated on the same footing, so this is the argument's contract and not a criterion failure. `sort()` drops `NA`, so a norms row whose `Sample` is `NA` is neither rostered nor reported (68); a norms table with no `Sample` column raises "arguments imply differing number of rows: 1, 0" rather than naming the instrument (45); and `is.null(norms) || !nrow(norms)` raises "invalid 'y' type" on a non-data-frame `Norms[[1]]` (40). Separately, `shipped_roster(objects)` still derives the roster from `objects` — the pre-T18 fusion — so a call written `roster = shipped_roster(objects)` against *real* instruments reproduces the return-2 defect with no guard (55); all six current callers are synthetic fixtures, which is what makes it latent. None is reachable from a shipped instrument today: the sweep returns 24 pairs over 15 instruments and every one has a non-`NA` `Sample`. Promote into whichever milestone next opens `audit_norms()` — M81 and M80 both do — or on any evidence a real instrument reaches one of the four silent paths. The deciding question is whether the roster gets a validating constructor or the argument is withdrawn in favour of a predicate the caller passes.
- **IIP T-score conversion tables** (2026-08-07, raised at the M75 gate): the IIP manual norms the IIP-32 and IIP-64 through per-scale T-score lookup tables (Appendices B and F), not through the M/SD z-scoring `norm_standardize()` implements. (This row's further claim that for the IIP-32 the tables are the *only* published norming content was **false and is corrected here (M75 plan, corrected M75)**: Table F.5, p. 91 prints the IIP-32 descriptives, and the shipped values reconcile against it exactly.) Adopting the tables was declined at the M75 gate on two grounds: the Mind Garden permission licenses "Normative Data (Means and SDs)" and a conversion table is neither, so shipping one needs a fresh permission request; and the printed tables are not the linear transform the manual's own p. 30 describes, so they are not equivalent to z-scoring. **Measured 2026-08-07 against the shipped M/SD, and the effect is small**: over the 408 IIP-32 cells not capped at the tables' 99 ceiling, published-minus-linear T has median 0.3 points (0.03 SD), 90th percentile 1.3, and maximum 4.0 (0.4 SD); below T = 60 it never exceeds 1.2 points. The divergence is a mild S-shape from the distribution's positive skew, confined to high scores — the headline gaps at raw 15–16 are the 99-ceiling, not disagreement. Octant-differential spread, which is what SSM shape sees, runs 0.10–0.23 SD in the normal range and ~0.44 SD only above raw 11 — smaller than the 0.44-average/0.78-extreme effect of choosing a different reference *sample* (norms-fitness row). Adopting the tables would therefore also be statistically backwards for this package: SSM fits a cosine to standardized scores on an interval metric, and a normalized T is a nonlinear monotone transform that distorts that structure. Promote only on a fresh permission covering the conversion tables *and* a concrete use case; a lookup-table norming path would also be a new estimand needing its own design pass.
- **CSIG source may print duplicated PA/NO statistics** (2026-08-06, measured at M72): `locke2014` Figure 2 (p. 436) prints identical alpha, M and SD -- .73 / 2.96 / .68 -- for both the PA and NO octants, and the package transcribes both faithfully, so this is a question about the *source*, not a packaging defect (see `cairn/references/locke2014.md`). Three identical statistics for two different octants is more consistent with a figure-production slip than with a coincidence, but nothing in the article settles it. Jeff chose at the M72 gate to flag it to the author rather than only record it. Promote if Locke confirms an error, which would make the shipped csig PA or NO values wrong and turn this into a data fix; leave as-is if he confirms the figure is correct.
- **`cais`'s adult-sample norms exceed the instrument's own printed response anchors** (2026-08-07, surfaced at the M74 review as F1's pre-existing half, scored 35): `cais$Anchors` runs 1 ("Never") to 5 ("Always"), but three of sample 2's shipped octant means -- PA 5.19, LM 6.52, NO 6.14 (American college students, n = 194) -- exceed 5, while all eight of sample 1's sit inside it. The M74 audit is structurally blind to this: it compares shipped against source and the two agree, so a wrong anchor set, a different adult-sample metric, or an error in sodano2006's own Table 4 all pass. ~~Promote on any evidence identifying which -- a CAIS manual, a reply from Sodano or Tracey, or a second source printing the adult-sample descriptives.~~ **The promotion condition was met 2026-08-08 and the third option is now the leading answer: Table 4's M and SD rows appear to be transposed between its CAIS and IAS column blocks, so the package has probably been shipping the IAS's adult means and SDs as the CAIS's.** Evidence and what was ruled out (sum scores; a published erratum) are in [sodano2006.md](references/sodano2006.md), "On the adult sample's M and SD". The second option is dead: p. 320 states the 5-point format for the instrument itself, not only for the child pilot, so no wider adult metric is licensed. **The code half shipped the same day by a hotfix, PR #103 (D-040)**: `norm_standardize()` refuses any shipped sample whose means leave its instrument's anchor range, through the shared `norm_sample_usable()` predicate at `R/tidying_functions.R:128`, and `tests/testthat/test-norms-anchor-range.R` sweeps every shipped sample of every shipped instrument by the same enumeration the provenance pins use and pins the violation set to exactly `"cais sample 2"`, so both a new violation and this one's disappearance fail the suite. **Two of this row's claims were therefore false by the time it was next read, and are corrected here (M74, corrected 2026-08-08):** that a fail-closed refusal at `norm_standardize()` was still merely "justified today" rather than shipped, and that the roster-wide invariant "is not yet a test in the suite". **What remains open is correspondence only:** an author query went to Sodano, Tracey copied, on 2026-08-08, asking whether the rows are transposed and carrying M74's unreconciled Table 2 *N* question (204 vs 213). A "swapped" reply makes the IAS-labelled block the correct sample-2 M/SD, a numeric norms change needing its own gate (IP5, D-039); a "correct as printed" reply means the adult sample is irreconcilable with a 5-point CAIS and should be withdrawn or hard-flagged. Either reply turns the standing refusal into a repair or a withdrawal and changes the pinned violation set, so promote on a reply and not before.
- ~~**`data-raw/audit-norms.R` robustness follow-ons**~~ **graduated 2026-08-08 → M79** (the seven where the audit can silently under-report what it read: the shipped-roster sweep, the untagged-note `claimed` collision, the marker and tag parser at both scanning sites, and the untested abort paths) **and M80** (the five that key the coverage report and validate the batch: the `field` and `instrument` column mangling, the note-only duplication, `divisor`, and the `Items` asymmetry). Planned ahead of this row's own promotion condition, which is **not** met — the audit has not extended to a new instrument — at Jeff's choice at the 2026-08-08 plan gate. The twelfth finding, `parse_source_note()` returning a note's single untagged block when the caller names no instrument, is **declined on the merits, not deferred**: the design note at `data-raw/audit-norms.R:138-143` makes it deliberate and it is unreachable through `audit_norms()`. Two of this row's framings were **corrected at that plan gate (M74/M75, corrected 2026-08-08)**: the highest-scored finding loses a whole *instrument*, not only a sample — dropping `isc` from the batch silently costs 17 audited values with the gap count still 0 and no coverage row naming it — and the coverage report already *is* asserted over the real roster by `tests/testthat/test-norms-provenance.R:478`, so what is missing is a roster-identity check, not a zero-gap one. Original capture (2026-08-07, six sub-threshold M74 review findings, scored 30-78): a norm sample no `AUDIT_BATCH` row names is dropped with every coverage count clean, there being no shipped-side equivalent of `note-sample-not-audited` (78, the highest); coverage rows mangle `field` into `"M (sample 1)"` so the CSV cannot be joined back to the ledger (68); `validate_batch()` does not validate `divisor` (68); `values_agree()` normalises `Items` on the shipped side only (50); and two not-live-today shapes -- instrument-level rows in a `scales = FALSE` note dropped uncovered (45), `note-only` coverage rows duplicating per pass for a multi-sample citekey (40). Promote when the audit next extends to a new instrument, since a silently-dropped sample is the failure that extension would hit first. **Extended 2026-08-07 at the M75 review** with six more sub-threshold findings against the same file, five of them in the instrument-tagged-block machinery M75 added (scored 50–72): `parse_source_note()` returns the untagged block instead of aborting when asked with `instrument = NULL` (55, not reachable through `audit_norms()`); two instruments sharing an *untagged* note still collide in the `claimed` key, so one's unaudited samples are covered by the other's claims (55); the begin/end markers are matched by a loose prefix with no fence-awareness or `-->` terminator, so a marker inside a fenced example parses as a real block (50); tag extraction accepts anything after the prefix, inventing tags like `"ning"` from `audit-values-beginning` (55); the new abort paths (block nesting, duplicate tags) are untested (72); and the coverage report's `instrument` column now also holds `"citekey (tag)"`, extending the `field`-mangling join defect this row already carries (50). M75 fixed only the block-level silent-loss finding (88) that these sit beside.
- ~~**`iei`'s norms `Sample` key is miscoded, and `norm_standardize()` errors on the instrument**~~ **fixed 2026-08-07 by a hotfix, PR #99** (`data-raw/iei.R:27` built the column with `rep(1:2, times = 8)`, interleaving the two samples so each held four octants twice and four not at all; `norm_standardize()` raised `"Multiple normative rows match 90 degrees"` on either sample). Found 2026-08-06 while answering a question during M73. The misspelled `Reference` ("Locker" for Locke) went with it. Regression tests sweep every shipped instrument and sample rather than naming iei, and pin iei sample 1's z-scores to the published Study 1 values. **Residue for M74:** the `iei` norms `URL` still points at the OSF project (`https://osf.io/w37dj/`) rather than the author's norms page, `https://kennethlocke.org/IEI/IEI_Norms.html`, whose eight octant M/SD and N of 1,223 match the shipped sample-1 values exactly (fetched 2026-08-06). That page was not on the shelf when M74 was planned, and M74 AC3 premises `horner2024` as an OSF-hosted source — so which of the two is the norms' provenance anchor is a question for M74's gate, not a bug.
- **Boundary-regime interval improvement** (post-M4 note lineage; 2026-08-04 design interview, wart routing): interval methods with better coverage where the measured record fails — β ~.77 flat in N at boundary truths (percentile bootstrap), analytic angle CIs outside the band until N ≈ 50000. BCa is refused for circular displacement (D-002); the search space is other interval families or corrections, each needing its own coverage oracle. Until promoted, the `summary()` cautions are the mitigation.
- **Heywood-regime user guidance** (2026-08-04 design interview, wart routing): user-facing guidance — a vignette section or diagnostic — on interpreting CPM fits at field N, where 59–91% of fits at N ≤ 500 contain ζ̂ > .995 and T is a mixture; the existing cautions state the facts but do not teach applied readers what to do. Docs-mission work (see DESIGN.md Purpose & scope); may draw on the CPM simulation paper's results when they exist.
- **Satorra–Bentler-style scaled test statistic for the axes-reliability χ²** (RR13 B-1, low priority): the global χ² and fit indices carry the correlation-as-covariance approximation in the *other* direction — measured E[T] = 261.1 against df = 273 at the probe population, so fit is flattered ~4% — and the same Γ machinery M66 builds could scale the statistic. M66 quantified the direction in prose and explicitly left the statistic uncorrected. **Graduated from this row 2026-08-02 → M68** (D-036 supersedes D-035's "keep their caveat" holding; enters v2.0.0 without gating M7, and scales χ², p, RMSEA and CFI on all three input paths). Its residue is the **scaled *difference* test** for nested model comparison, out of M68's scope because this API exposes no such comparison — promote if one appears; a **Swain/Bartlett-type small-sample mean correction to `T`** (RR14 future work), which RR14's oracle mean-recentering shows would essentially close the residual small-N over-rejection that M68 documents rather than fixes — promote on evidence that the documented small-N behaviour is misleading users, and only with its own oracle and calibration cells. Graduated from this row 2026-08-03 → **M70**: reporting `p*` and `N` in `details` (RR14 rec 4), and naming which lavaan variant the scaled `cfi`/`rmsea` correspond to — M68 ships the equivalent of `cfi.scaled`/`rmsea.scaled`, not the `*.robust` (Brosseau-Liard/Savalei) forms, and no user-facing surface says so (M68 review round 2, F6 at 66). That second item's promotion condition — a user reporting the confusion, or the release walk asking for it — was **waived at the M70 plan gate, not met**. Graduated earlier, 2026-07-27: the corrected component SEs (D-035, RR13 BC1–BC6) → **M66**, and M65's seven third/fourth-pass review fold-ins → **M67**.
- ~~**`cpm_fit.R`'s roxygen emits four unresolvable-link warnings, and interval notation is the cause of three**~~ **fixed 2026-08-08 by a direct commit** (trivial tier: roxygen comments only, no runtime surface, `man/` and `NAMESPACE` byte-unchanged). Two of this row's claims were false and are **corrected here (2026-08-08)**. The count was **nine, not four**: the four named (`:431`, `:615`, `:1034`, and the `[cpm_gradient()]` link at `:1127`) were read off a truncated `document()` log tail, and five more stood above them -- `[cpm_rho()]` at `:47`, and the four bracketed blocks of the gamma* layout schematic on the single line `:131`. And **nothing user-facing was affected**: all nine sites sit inside `@noRd` blocks (`cpm_rho_deriv`, `cpm_spec`, `cpm_start_values`, `cpm_engine`, `cpm_rmsea_ci`, `cpm_hessian_fd`), so no `.Rd` page is generated from them and no CRAN reviewer reads them -- the row took roxygen's `@details`/`@description` warning labels to name the exported `cpm_fit()`, which they do not. The real cost was nine standing warnings desensitizing every later `document()` run to a new one that matters, so `PROFILE.md`'s consistency-gate now requires a warning-free `document()`, not only a diff-free one. All nine protected with backticks, the convention already dominant in this file.
