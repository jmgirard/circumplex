# circumplex Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-07-21 (/milestone-review M47: merged PR #73, archived → done. Shipped 3 estimator source notes — wright2009 + gurtman1998 (the definer) + gurtman2003 — and owes-no-page for gurtman 1991/1993/1994; review F1/F2 fixed. Validate green; M48 still planned; PR #70 still open; 47 advisories remain M7's. Pruned M42 terminal row per retention.)_

Pre-migration history: see `cairn/legacy/` and git log.

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M7 | v2.0.0 CRAN release preparation | blocked | M25, M26, M27, M31, M32, M33, M34, M35, M36, M37, M38 | high | milestones/M7-v2-release-prep.md |
| M47 | SSM estimator source notes (Wright 2009 + defining Gurtman) | done | — | normal | milestones/archive/M47-estimator-source-notes.md |
| M48 | Fit-index and uncited shelf sources (browne1993 twin + strack2013) | in-progress | — | normal | milestones/M48-fitindex-uncited-sources.md |
| M45 | Source notes for the RANDALL structure test pair | done | M40 | normal | milestones/archive/M45-reference-notes-randall.md |
| M46 | Prospect notes for the four forward-looking shelf sources | done | — | normal | milestones/archive/M46-prospect-notes-forward-sources.md |
| M44 | LESSONS.md consolidation and retirement pass | dropped | — | normal | milestones/archive/M44-lessons-consolidation.md |
| M43 | Source notes for the structure criteria and the validity source | done | M40 | normal | milestones/archive/M43-reference-notes-structure.md |

## Candidates

_Candidates carry no milestone ID — an `M<NN>` is assigned only at planning time. Legacy milestone/decision IDs (≤ M6) remain valid citations into `cairn/legacy/`; M7 is the first cairn-era ID._

- Longitudinal deferrals (D-013 spec lineage; Builds A/B/C promoted → M25/M26/M27, 2026-07-16; §1.1 sugar → M28 and §1.4 `ssm_ci_accuracy()` occasions → M29, 2026-07-17). Remaining, each with standing rationale against building now: pairwise-deletion occasions semantics (spec §1.3 — ships listwise-only on RR06 R6 estimand grounds; reopening needs a use case + a superseding D-entry); occasions × measures correlation path (spec §1.2a — defer until a concrete use case exists); occasions × contrast × grouping difference-of-differences (spec §1.2b — a new estimand needing its own design pass, i.e. a design milestone before any build); Stan companion (spec §5.4 stay-out criteria, revisit only if ≥2 of 3 conditions hold).
- CPM simulation paper (research-paper track, not a package milestone): the engine is written, reviewed, and ratified in `devel/cpm-sim/` (registered plan `devel/cpm-simulation-paper-plan.md`; not yet run). A package-side milestone (e.g., sim-backed guidance in docs/vignettes) is replanned only if the paper's results motivate one.
- Continuous / infrastructure refactors (fold into the milestone that next touches the code): analytic-CI Hessian recomputation (minor perf; oracle-validate when done). (The `coord_circumplex()` `amax`/`center` non-finite guard and the `ssm_plot_trajectory()` missing-legend-glyph defect were promoted → M36, 2026-07-18.) (Nesting seed promoted → M22, 2026-07-16. The RR01 S3-follow-ups clause re-added by the 2026-07-16 cleanup was stale — all four items, including the as_degree/as_radian keep-internal decision M13-D1, had already shipped in M13, PR #37 squash 95936f2; struck.)
- Rim-ring and bare-coord-vignette-figure candidates (added 2026-07-18 out of the PR #62 hotfix) promoted → M38 the same day; the vignette-figure item rides along as M38's consumer-side scope rather than a separate milestone.
- Radial axis labels illegible over data layers (added 2026-07-18 out of M38's T6 render-and-inspect sweep) — promoted → **M39**, 2026-07-19. **The row's original diagnosis was wrong and is corrected here so it does not outlive the row:** it recorded the amplitude labels as "panel furniture drawn beneath the geom layers" and proposed "drawing the radial guide above the layers" as one of two remedies. The labels are already drawn above every geom layer (verified at the M39 plan gate on all three reported figures — panel children draw in the order `grill` → geom layers → axis gTree, and recolouring the axis text renders each hidden label legible on top of the mark that had hidden it). The real defect is contrast: grey30 text over dark arrowheads and markers, or over a dense pale scatter. Only the row's other proposed remedy, a backdrop behind the labels, is live; M39 takes it.
- Cite the published equations `R/cpm_fit.R` implements (surfaced at M41's review, confirmed and widened by M42, 2026-07-19; swept — no existing candidate, DECISIONS entry, or archived milestone carries it as live work). The engine implements Browne & Cudeck (1992) eqs. 13/14 at `R/cpm_fit.R:1049,1011-1028` and Browne (1992) eqs. 2–6, 30, 34 throughout, with **no per-line attribution**: the file header and `@references` name the papers, but nothing tells a corrector which published equation a given line is an implementation of. Both source notes now exist (`browne1992a.md`, `browne1992.md`) with the equations banked verbatim and anchored, so the work is mechanical — add equation-number comments at the implementing lines and point them at the reference pages. This is a package-code change and so was out of scope for M40–M43, which are documentation-only. Rides along cheaply with whatever milestone next edits the CPM engine; needs no milestone of its own. A second opportunity to fold in: `browne1992.md` records that Browne's own Table 11 (p. 494) prints the m = 1 estimates digit for digit as Grassi's Table 2, so a second published anchor for the m = 1 oracle is available without new source work.
- **`LESSONS.md` consolidation pass** (surfaced at M43's post-merge hygiene, 2026-07-19) — promoted → **M44**, then **M44 dropped 2026-07-20** at Jeff's request: the file passes both live checks but sits at the `weight caps` edge (49/50 lines), so rather than a standalone pre-emptive pass, the compression is deferred to whenever the next lesson capture forces it (that milestone's post-merge hygiene, per D-051/D-015). See the archived milestone. The original premise is left below as the historical record. The file was at **49 items against a hard cap of 50**, so the next milestone to capture a lesson fails the `weight caps` CHECK outright, and it sits ~300 chars over the `record density` advisory threshold. M43 already consolidated rather than appended (its lesson became case (e) of the existing "prove by mutation" line), which is the last cheap move available. Retirement is the doctrine-preferred outflow (a lesson is retired when a test now *fails* on the mistake it warns about, or another file's slot owns its content), but it must be scoped to what a milestone ships — so this needs its own pass rather than riding along. Do not buy budget by compressing the lines carrying exact formulas (M20's ulp window, M26's `%%`-vs-`modu`, M27/M33's unwrap expressions): those are statistical-correctness records and precision outranks the advisory.
- Source notes for RANDALL's two sources — Hubert & Arabie (1987), *Evaluating order hypotheses within proximity matrices*, Psych Bull 102(1) 172-178, and Tracey (1997), *RANDALL*, EPM 57(1) 164-168 — **promoted → M45, 2026-07-20** (both PDFs now shelved, clearing the M43 "blocked on the shelf" gate). `fit_structure()`'s fifth test RANDALL is implemented in `structure_randall()`/`structure_randall_test()` and attributed to these two at `R/fit_structure.R:714,773-778`, so the repo *relies* on them and they owe pages. M45 also fixes the found-in-passing miscount at `tests/testthat/test-fit_structure_api.R:2` ("the five Acton & Revelle (2004) structure tests" → four A&R's + RANDALL; comment only, nothing computes differently), which the row had carried because M43 changed no package file.
- Fit-index guidance in `vignettes/evaluating-circumplex-structure.Rmd:92-96` omits two caveats its own sources state (surfaced while authoring the M41 source notes, 2026-07-19; no overlap swept in candidates or the archive). Hu & Bentler (1999) report that ML-based TLI, Mc, and RMSEA **overreject true-population models at small sample size**, and circumplex work often runs at modest n — `zimmermann2017.md` puts several SSM accuracy thresholds in the n = 50–200 range — yet the vignette offers the .95 CFI/TLI benchmark unqualified. Browne & Cudeck likewise call their own cutoffs "based on subjective judgment… cannot be regarded as infallible or correct", which the vignette does not carry. Both caveats are now recorded with anchors on `cairn/references/hu1999.md` and `browne1992a.md`, so a milestone taking this up has its sources ready. M41 deliberately changed no package file.
- ΔCFI as a labeled secondary invariance criterion in `ssm_sem()` (surfaced at the M41 re-size gate, 2026-07-19; **never decided, never rejected** — `devel/m5-sem-design.md` §12.2 item 2 records the Cheung & Rensvold 2002 transcription landing 2026-07-07 and says "whether to offer it remains Jeff's call at T4", and T4 shipped without it). Δχ² remains the sole shipped criterion and is self-contained — a computed quantity needing no literature constant — so nothing is broken today. Two things any future milestone must face: the source's own scope caveats are **two groups, plain ML, normality, never validated for robust indices**, while `ssm_sem()` defaults to `estimator = "MLR"` (`R/ssm_sem.R:1159`); and the transcription records an internally contradictory sentence at C&R p. 251. Taking this up is what makes `sources/cheung2002.pdf` owe a source note — authored by that milestone, per the milestone-takes-the-dependency rule, not backfilled by M41.
- Disposition of the three `devel/` transcription files once M7 archives (deferred out of M40/M41's Out clauses, 2026-07-19): `ar2004-`, `cr2002-`, and `m4-zw-transcription.md` are cited by name from M7's open work log, so they stay byte-untouched until M7 is archived. Once their source notes exist (M40/M41) and M7 is done, decide whether each gets a "superseded by `cairn/references/<citekey>.md`" pointer header or is retired outright — the transcription *protocol* records (two-channel diffs, paper-internal inconsistencies found) are the part worth keeping, not the values, which the source notes will carry with anchors.
- Strict-tier syntax *emission* single-sourcing across the single/multi-group branches in `R/ssm_sem_syntax.R` (descoped from M8 T4, 2026-07-12): the two emitters are structurally distinct (plain vs `c()`-vector cross-group labels) and byte-pinned by `exp_strict_*` snapshots — a high-risk rewrite of statistical output for minor DRY gain. Revisit only if that emitter is reworked for another reason.
- CIRCUM free-scaling post-v2.0.0 extensions (deferred out of M18; see `milestones/archive/M18-circum-free-scaling-build.md` + D-009): bootstrap σ CIs; covariance-matrix input — shipping it re-triggers the D-011 paired T calibration at non-unit σ truths first. The point-estimate oracle (frozen Grassi App. A + live OpenMx) already shipped in M18.
- Plotting/visualization expansion (2026-07-17): promoted → M30–M34 and folded into v2.0.0 (D-018); `ssm_plot_*` retained as convenience wrappers (D-018b). The M33 on-circle movement-path remainder was promoted → M37 (2026-07-18) as **static arrowed paths**; its revisit conditions (trajectory viz shipped, M31 canvas API settled) both held. **True animation** (gganimate or otherwise) was excluded from M37 at that plan gate on minimal-deps + custom-ggproto-coord grounds and stays a candidate: revisit only if a concrete teaching use case appears, and only via the dependency gate — no new Import or Suggests without a D-entry.
- **nagy2019 — covariate-extension feature / covariate-CI oracle** (M46 prospect E1, 2026-07-20): Nagy, Etzel & Lüdtke (2019)'s SEM extension of Browne's CPM models covariate–circumplex relationships without moving indicator angles and derives interval estimates for them. Could seed a `cpm_fit()` covariate-extension feature or an oracle/benchmark for covariate-to-circumplex CIs. Characterized in `cairn/references/forward-source-prospects.md` (E1); no reference page owed until relied upon (D-024).
- **weide2021 — Bayesian-vs-ML inference-comparison oracle / higher-level-scores reference** (M46 prospect E2, 2026-07-20): Weide, Scheuble & Beauducel (2021) contrast Bayesian and ML CFA of the IIP three-factor (Dominance/Love circumplex + Distress) structure with circumplex loading priors (N = 822). Could seed an inference-comparison oracle/benchmark or a higher-level-scores reference. See `forward-source-prospects.md` (E2); no page owed until relied upon (D-024).
- **rogoza2021 — three-step SSM + Procrustes method / vignette** (M46 prospect E3, 2026-07-20): Rogoza, Cieciuch & Strus (2021) give a three-step circumplex workflow (SEM structure → SSM location of external variables → Procrustes congruence), with tutorials. Could seed vignette/method material tying the package's SSM to a full workflow, or a Procrustes step. See `forward-source-prospects.md` (E3); no page owed until relied upon (D-024).
- **tracey2000 — circumplex-evaluation reference for `fit_structure()`** (M46 prospect E4, 2026-07-20): Tracey (2000)'s handbook chapter surveys circumplex-evaluation tools (circulant vs quasi-circumplex operationalizations; Hubert & Arabie order tests). Could seed a background/reference page for the `fit_structure()` family or vignette context. Currently uncited by shipped code (the CAIS `@source` is Sodano & Tracey 2006, a different work). See `forward-source-prospects.md` (E4); the shelf copy is an OCR scan, so any extraction needs a second channel (M42-D1); no page owed until relied upon (D-024).
- **Triage of the 8 unaccounted shelf sources** (surfaced 2026-07-20 while implementing M46; swept — no prior candidate/DECISIONS/archive covered them) — **promoted → M47 + M48, 2026-07-20.** M47 takes the relied-upon SSM-estimator sources: `wright2009` (Wright, Pincus, Conroy & Hilsenroth 2009 — `aw2009` provenance at `R/example_data.R:4` + the closed-form SSM parameters) and the shelf Gurtman work defining the "conventional **Gurtman estimator**" at `R/ssm_analysis.R:1183` / `test-ssm_sem_syntax.R:6` (the statistically-adjacent identity read across the five gurtman* PDFs — 1991/1993/1994/1998/2003; Gurtman 1992 and Gurtman & Pincus 2000 are vignette-cited but **not on the shelf**). M48 takes the fit-index + uncited pair. **The row's original `browne1993` = "uncited" claim was wrong and is corrected here:** `browne1993` (Browne & Cudeck 1993, the Bollen & Long chapter) IS cited in user-facing text — `vignettes/evaluating-circumplex-structure.Rmd:93,613` and `tests/testthat/_snaps/ci_accuracy.md:38` cite "Browne & Cudeck, 1993" for the RMSEA cutoffs, while the banked `browne1992a.md` is the 1992 journal twin; M48 resolves the twin via a cross-reference (not a duplicate page). `strack2013` (genuinely uncited; CFA circumplex-axes reliability) → M48 captures it as a prospect candidate row (D-024). NOT a gap: `browne1982_pp95-96.pdf` backs `browne1982.md` (filename ≠ citekey).
