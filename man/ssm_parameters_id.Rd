% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_parameters_id.R
\name{ssm_parameters_id}
\alias{ssm_parameters_id}
\title{Calculate SSM parameters for each person}
\usage{
ssm_parameters_id(data, scales, angles = octants(), id = NULL)
}
\arguments{
\item{data}{Required. A data frame or matrix containing at least
circumplex scales, with one row per person or (with \code{id}) per
person-occasion.}

\item{scales}{Required. The variable names or column numbers for the
variables in \code{data} that contain circumplex scales to be analyzed.}

\item{angles}{Optional. A numeric vector containing the angular
displacement of each circumplex scale included in \code{scales}, in degrees
(default = \code{octants()}). The closed-form SSM estimator used here equals
the ordinary-least-squares cosine fit for equally spaced \code{angles} --
more generally, for any angle set satisfying first- and second-harmonic
balance; see \code{\link[=ssm_parameters]{ssm_parameters()}}.}

\item{id}{Optional. A single variable name or column number identifying
persons. If \code{NULL} (default), each row is scored as its own person;
otherwise rows sharing an id are averaged within person before scoring.
Missing id values are an error (a person cannot be silently dropped).}
}
\value{
A data frame of class \code{"circumplex_ssm_id"} with one row per
person, in order of first appearance: the id column (named after \code{id},
or \code{id} when \code{NULL}), \code{n_obs} (rows contributing to that person),
\code{na_rate} (proportion of missing scale cells among those rows), and the
SSM parameters \code{Elev}, \code{Xval}, \code{Yval}, \code{Ampl}, \code{Disp} (degrees in
[0, 360], with the 0/360 pole reported as 360 per the package's
LM = 360 convention), and \code{Fit}. Use \code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}} for
group-level summaries with circular statistics for displacement.
}
\description{
Score each person's own circumplex profile through the closed-form SSM
transform and return a per-person parameter table. When \code{id} is \code{NULL},
every row of \code{data} is treated as one person's profile (like
\code{\link[=ssm_score]{ssm_score()}}, but returning a fresh table rather than appending columns).
When \code{id} names a column, rows sharing an id (e.g., occasions of intensive
longitudinal data) are first averaged within person -- each scale's mean
uses that person's available (non-missing) rows -- and the within-person
mean profile is scored.
}
\details{
Degenerate profiles keep their row and are reported as \code{NA}, never
silently dropped: a flat (zero-variance) profile has undefined
displacement and fit, a profile with real variance but zero
first-harmonic amplitude has undefined displacement and a fit of 0, and a
person with a completely missing scale has an undefined profile (all
parameters \code{NA}). The \code{na_rate} column exposes each person's share of
missing scale cells so missingness is visible alongside its consequences.
}
\examples{
data("aw2009")
ssm_parameters_id(
  aw2009,
  scales = c("PA", "BC", "DE", "FG", "HI", "JK", "LM", "NO")
)

}
\seealso{
Other ssm functions:
\code{\link[=plot.circumplex_ci_accuracy]{plot.circumplex_ci_accuracy()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_analyze_long]{ssm_analyze_long()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_draws]{ssm_draws()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}},
\code{\link[=ssm_table]{ssm_table()}},
\code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}}

Other analysis functions:
\code{\link[=cpm_fit]{cpm_fit()}},
\code{\link[=cpm_simulate]{cpm_simulate()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_analyze_long]{ssm_analyze_long()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_draws]{ssm_draws()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}},
\code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}}
}
\concept{analysis functions}
\concept{ssm functions}
