% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_analyze_long.R
\name{ssm_analyze_long}
\alias{ssm_analyze_long}
\title{Perform SSM analyses on long-format repeated-measures data}
\usage{
ssm_analyze_long(
  data,
  scales,
  angles = octants(),
  id,
  occasion,
  grouping = NULL,
  contrast = FALSE,
  boots = 2000,
  interval = 0.95,
  parallel = "no",
  ncpus = 1,
  method = "bootstrap"
)
}
\arguments{
\item{data}{Required. A data frame (or matrix) in long format containing an
identifier column, an occasion column, and the circumplex scale scores
(one set of score columns, repeated across occasions in different rows).}

\item{scales}{Required. A character vector of column names, or a numeric
vector of column indexes, giving the circumplex scale scores in \code{data}
(the same scales measured at every occasion).}

\item{angles}{Optional. A numeric vector containing the angular displacement
of each circumplex scale included in \code{scales} (in degrees) (default =
\code{octants()}).}

\item{id}{Required. A single column name or index identifying the person
that each row belongs to.}

\item{occasion}{Required. A single column name or index identifying the
occasion (wave) that each row belongs to. Occasion order -- which governs
the second-minus-first direction of a paired contrast -- is taken from the
factor levels of this column when it is a factor, and otherwise from the
order in which the occasions first appear in \code{data}. It is never sorted
alphabetically, so a \code{T10}/\code{T2} pair keeps its supplied order.}

\item{grouping}{Optional. A single column name or index giving a
time-invariant grouping variable (one value per person; an error is raised
if a person's grouping value varies across occasions).}

\item{contrast}{Optional. A logical value; if \code{TRUE} (and the data contain
exactly two occasions in a single group), the paired within-person
contrast (second occasion minus first) is calculated (default = FALSE).}

\item{boots, interval, parallel, ncpus, method}{Optional. Passed through to
\code{\link[=ssm_analyze]{ssm_analyze()}}; see its documentation.}
}
\value{
A list containing the results and description of the analysis, as
returned by \code{\link[=ssm_analyze]{ssm_analyze()}} (with an \code{Occasion} column). See
\code{\link[=ssm_analyze]{ssm_analyze()}}.
}
\description{
A convenience wrapper around the \code{occasions} interface of \code{\link[=ssm_analyze]{ssm_analyze()}}
for data stored in long format (one row per person per occasion). It
reshapes the data into the wide, one-row-per-person layout that
\code{\link[=ssm_analyze]{ssm_analyze()}} consumes and then delegates to it; all estimation is
performed by \code{\link[=ssm_analyze]{ssm_analyze()}} unchanged. See the \code{occasions} argument of
\code{\link[=ssm_analyze]{ssm_analyze()}} for the analysis semantics -- per-occasion profiles, paired
within-person contrasts, and the listwise-only handling of missing waves
(a person missing any occasion is dropped from all occasions).
}
\examples{
# Build a small two-occasion dataset in long format (one row per person per
# occasion). In practice `data` already stores the repeated occasions this
# way; here we stack two copies of jz2017 as an illustration.
data("jz2017")
scales <- c("PA", "BC", "DE", "FG", "HI", "JK", "LM", "NO")
t1 <- jz2017[, scales]
t1$id <- seq_len(nrow(t1))
t1$occasion <- "T1"
t2 <- t1
t2$occasion <- "T2"
long <- rbind(t1, t2)

\donttest{
# Per-occasion SSM profiles from long-format data
ssm_analyze_long(long, scales = scales, id = "id", occasion = "occasion")
}
}
\seealso{
\code{\link[=ssm_analyze]{ssm_analyze()}} for the wide-format interface and the analysis
semantics this wrapper delegates to.

Other ssm functions:
\code{\link[=plot.circumplex_ci_accuracy]{plot.circumplex_ci_accuracy()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_draws]{ssm_draws()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}},
\code{\link[=ssm_table]{ssm_table()}},
\code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}}

Other analysis functions:
\code{\link[=cpm_fit]{cpm_fit()}},
\code{\link[=cpm_simulate]{cpm_simulate()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_draws]{ssm_draws()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}},
\code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}}
}
\concept{analysis functions}
\concept{ssm functions}
