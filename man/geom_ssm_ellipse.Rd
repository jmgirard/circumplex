% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_ssm.R
\name{geom_ssm_ellipse}
\alias{geom_ssm_ellipse}
\title{Draw a joint confidence ellipse for SSM coordinates in circumplex space}
\usage{
geom_ssm_ellipse(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  level = 0.95,
  n = 100,
  na.rm = TRUE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, stat, position, show.legend, inherit.aes, ...}{Standard
\pkg{ggplot2} layer arguments. \code{mapping} must supply the \code{x0}, \code{y0},
\code{var_x}, \code{var_y}, and \code{cov_xy} aesthetics: the centre and the variances
of \code{x} and \code{y} and their covariance, all in the score metric of the
Cartesian coordinates.}

\item{level}{A single number strictly between 0 and 1: the confidence level
of the region (default 0.95).}

\item{n}{The number of vertices on each outline (default 100); the path is
closed by repeating the first vertex.}

\item{na.rm}{If \code{FALSE}, warn (with the dropped-row count) before removing
rows with a non-finite centre or covariance element; if \code{TRUE} (the
default) remove them silently. A covariance that is not positive
definite is an error, not a missing value.}
}
\value{
A \pkg{ggplot2} layer.
}
\description{
A \pkg{ggplot2} layer that draws, for each profile, the ellipse of a
bivariate normal region on the Cartesian \verb{(x, y)} coordinates of the
Structural Summary Method, on a circumplex canvas built with
\code{\link[=coord_circumplex]{coord_circumplex()}} (for example the canvas from \code{\link[=ggcircumplex]{ggcircumplex()}}). Each
row supplies the centre \verb{(x0, y0)} and the three elements of a 2 by 2
covariance matrix; the layer computes the ellipse's outline in Cartesian
space, converts each vertex to a displacement and an amplitude, and hands
those to the coordinate system, which owns the polar transform.
}
\details{
The outline is the contour \verb{(v - c)' S^-1 (v - c) = qchisq(level, 2)},
where \code{c} is the centre and \code{S} the covariance: under a bivariate normal
approximation to the distribution of \verb{(x, y)}, sampling or posterior, it
encloses the joint region at confidence \code{level}. \code{\link[=ssm_ellipse_data]{ssm_ellipse_data()}} computes the five
columns from an \code{\link[=ssm_draws]{ssm_draws()}} object. The ellipse is a statement about the
Cartesian pair, not about amplitude and displacement separately; the wedge
\code{\link[=geom_ssm_arc]{geom_ssm_arc()}} draws is the pair of marginal intervals on those two
parameters, and the two regions need not coincide.

Vertices are unwrapped along the outline so that an ellipse straddling the
0/360 seam is drawn across it, and an ellipse containing the origin winds
once round the centre of the canvas. Unwrapped displacements may therefore
fall outside \verb{[0, 360)}. Each input row is one outline: the layer sets the
\code{group} aesthetic to the row index, replacing any \code{group} the mapping
supplies.
}
\examples{
set.seed(1)
draws <- cbind(rnorm(500, 0.4, 0.1), rnorm(500, 0.3, 0.05),
               rnorm(500, -0.2, 0.05))
res <- ssm_draws(draws, type = "parameters")
ggcircumplex(octants(), amax = 0.5) +
  geom_ssm_ellipse(
    data = ssm_ellipse_data(res),
    mapping = ggplot2::aes(
      x0 = x0, y0 = y0, var_x = var_x, var_y = var_y, cov_xy = cov_xy
    )
  ) +
  geom_ssm_point(
    data = res$results,
    mapping = ggplot2::aes(amplitude = a_est, displacement = d_est)
  )
}
\seealso{
Other circumplex layers:
\code{\link[=coord_circumplex]{coord_circumplex()}},
\code{\link[=geom_ssm_arc]{geom_ssm_arc()}},
\code{\link[=geom_ssm_path]{geom_ssm_path()}},
\code{\link[=geom_ssm_point]{geom_ssm_point()}},
\code{\link[=ggcircumplex]{ggcircumplex()}},
\code{\link[=scale_x_circumplex]{scale_x_circumplex()}},
\code{\link[=theme_circumplex]{theme_circumplex()}}
}
\concept{circumplex layers}
