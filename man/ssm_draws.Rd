% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_draws.R
\name{ssm_draws}
\alias{ssm_draws}
\title{Summarize posterior draws as SSM parameters}
\usage{
ssm_draws(draws, angles = NULL, interval = 0.95, type = NULL)
}
\arguments{
\item{draws}{Required. A numeric matrix or data frame of posterior draws:
one row per draw, columns per the shape rules above.}

\item{angles}{Optional. A numeric vector of angular displacements (in
degrees) for profile draws, one per column of \code{draws}; \code{NULL} (default)
for parameter draws.}

\item{interval}{Optional. A single number between 0 and 1 giving the
credible level for the equal-tailed intervals (default = 0.95).}

\item{type}{Optional. \code{"parameters"} or \code{"profiles"}, required only where
the shape is ambiguous (\code{angles = NULL} with exactly 3 columns); when
given elsewhere it must not contradict \code{angles}.}
}
\value{
An object of class \code{"circumplex_ssm_draws"} holding \code{draws} (the
SSM parameter draws, one row per posterior draw, columns e, x, y, a, d,
fit, displacement in degrees [0, 360)), \code{results} (the point summaries
and credible bounds), and \code{details}. Printing shows the summary table;
\code{summary()} adds the analysis details.
}
\description{
Transform posterior draws from a user-fitted Bayesian model (e.g., a brms
cosine regression) into Structural Summary Method parameter draws and
summarize them with the package's circular-statistics machinery. Two draw
shapes are accepted, distinguished explicitly (never guessed):
}
\details{
\itemize{
\item \strong{Parameter draws} (\code{angles = NULL}, \code{type = "parameters"}): a numeric
matrix or data frame with exactly three columns interpreted \strong{in column
order} as (e, x, y) -- elevation (intercept), the cosine coefficient
(x), and the sine coefficient (y). Each row is mapped to amplitude
\code{a = sqrt(x^2 + y^2)} and displacement \code{d = atan2(y, x)} wrapped to
[0, 360). Column names are not used to reorder: when names are present
but do not look (intercept, cos, sin)-like, a message states the
assumed mapping. A row with exactly zero amplitude has undefined
displacement (\code{NA}); model fit is undefined for parameter draws
(\code{fit = NA}) because no profile is available to measure it against.
\item \strong{Profile draws} (\code{angles} supplied): a numeric matrix with one column
per circumplex scale (\code{ncol(draws) == length(angles)}); each row goes
through the closed-form SSM transform exactly as a bootstrap replicate
would, inheriting the standing degenerate-profile NA semantics.
}

With \code{angles = NULL} and a column count other than 3 the input matches
neither shape and an error explains both. With \code{angles = NULL} and
exactly 3 columns the shape is ambiguous (a p = 3 instrument's profile
draws look like parameter draws), so \code{type = "parameters"} is required.

Point estimates are posterior medians for e, x, y, a, and fit (amplitude
is right-skewed, so a mean would be biased upward), and the circular mean
for displacement. Marginal summaries are not jointly coherent: the
reported a is not \code{sqrt(x^2 + y^2)} of the reported (x, y), and the
reported d is not their direction -- each is the honest marginal summary
of its own posterior. Intervals are equal-tailed credible intervals
(percentile quantiles of the draws), with displacement handled by the
package's circular quantile machinery (centered on the circular mean, so
intervals straddling 0/360 wrap correctly). Draws with undefined
displacement are excluded from the displacement summaries only, which are
therefore conditional on estimability (measure-zero for continuous
parameter-draw posteriors; can bind for profile draws). A diffuse
posterior with zero circular resultant has an undefined circular mean,
reported as \code{NA} rather than invented.

Note that independent priors on (x, y) induce a non-uniform prior on
(a, d) -- roughly Rayleigh-shaped on amplitude, with mass pushed away
from a = 0 -- so the prior on the SSM scale should be inspected (e.g.,
by prior-predictive simulation) rather than assumed flat; see the
package's Bayesian SSM vignette.
}
\examples{
# Parameter draws (e.g., brms fixed-effect draws b_Intercept, b_cos, b_sin)
set.seed(1)
draws <- cbind(rnorm(500, 0.4, 0.1), rnorm(500, 0.9, 0.1),
               rnorm(500, -0.3, 0.1))
ssm_draws(draws, type = "parameters")

}
\seealso{
Other ssm functions:
\code{\link[=plot.circumplex_ci_accuracy]{plot.circumplex_ci_accuracy()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}},
\code{\link[=ssm_table]{ssm_table()}},
\code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}}

Other analysis functions:
\code{\link[=cpm_fit]{cpm_fit()}},
\code{\link[=cpm_simulate]{cpm_simulate()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}},
\code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}}
}
\concept{analysis functions}
\concept{ssm functions}
