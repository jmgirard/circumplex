% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_ci_accuracy.R
\name{ssm_ci_accuracy}
\alias{ssm_ci_accuracy}
\title{Assess the accuracy of SSM confidence intervals by simulation}
\usage{
ssm_ci_accuracy(
  ssm_object,
  reps = 1000,
  amplitude_factors = c(1, 0.5, 0.25, 0),
  structure = c("cpm", "observed"),
  m = NULL,
  cpm = NULL,
  data = NULL,
  digits = 3,
  parallel = "no",
  ncpus = 1
)
}
\arguments{
\item{ssm_object}{Required. A \code{circumplex_ssm} object from \code{\link[=ssm_analyze]{ssm_analyze()}}.}

\item{reps}{Optional. The number of simulated datasets per amplitude
condition (default = 1000, binomial SE about 0.7 percentage points;
500 is a reasonable quick-look floor).}

\item{amplitude_factors}{Optional. Numeric vector of amplitude scaling
factors in \verb{[0, 1]} defining the ladder conditions (default =
\code{c(1, 0.5, 0.25, 0)}). Must include 1, the as-estimated condition that
the verdict is keyed to. Applied to every profile row jointly.}

\item{structure}{Optional. \code{"cpm"} (default) simulates from the Browne
model's model-implied scale correlations; \code{"observed"} bypasses the model
and uses the pooled within-group correlation matrix directly (a
sensitivity switch: if the two verdicts differ, structure uncertainty is
itself material).}

\item{m}{Optional. The number of harmonics passed to \code{\link[=cpm_fit]{cpm_fit()}} (default
\code{NULL} uses \code{min(3, floor((p - 1) / 2))}).}

\item{cpm}{Optional. A pre-fitted \code{circumplex_cpm} object to reuse for the
population structure instead of refitting (its scales must match the ssm
object's). Ignored when \code{structure = "observed"}.}

\item{data}{Optional. The original data set, required only for ssm objects
created before sufficient statistics were stored at analysis time; the
statistics are then recomputed and checked against the stored profile
vectors.}

\item{digits}{Optional. The rounding digits of the certification rule
\code{round(a_lci, digits) > 0} (default = 3, matching the printed
\code{\link[=ssm_analyze]{ssm_analyze()}} output).}

\item{parallel}{Optional. \code{"no"} (default), \code{"multicore"}, or \code{"snow"};
distributes the simulation replicates across \code{ncpus} cores. Results are
identical for a given seed regardless of these settings (see
Reproducibility).}

\item{ncpus}{Optional. Number of cores when \code{parallel} is not \code{"no"}
(default = 1).}
}
\value{
A \code{circumplex_ci_accuracy} object: a list with \code{coverage} (per
Profile x Parameter x Condition: coverage, its Monte Carlo SE, the
one-sided miss rates, median CI width, and for displacement the
certification-conditional coverage with the number of certified
replicates behind it -- for a contrast row this conditional column is
retained as a joint-certification descriptive that no display consumes;
\code{Structural} flags the amplitude rows whose zero
coverage is a theorem rather than a measurement), \code{guardrail} (per
Profile x Condition: certification rate with its 95\% Wilson score
interval, the user-expectation benchmark \code{(1 - interval) / 2}, the
stored false-certification caution decision at the \code{c = 0} rung
(\code{Caution}, true when the Wilson lower bound exceeds the benchmark;
\code{NA} off that rung, and \code{NA} for a contrast row, which
\code{print.circumplex_ssm()} never gates), the implied threshold, fit-pass
rate, and the
branch-pathology rate -- the rate at which a displacement point estimate
falls geometrically outside its own interval), \code{verdict}
(Wilson-vs-Bradley classification of elevation, amplitude, and
displacement coverage at the as-estimated condition -- a profile's
displacement is classified certification-conditionally (\code{Parameter}
\code{"d_conditional"}), a contrast's unconditionally (\code{Parameter} \code{"d"}) --
plus an
overall worst-of row per profile; note the printed verdict headline
additionally elevates to CAUTION whenever the guardrail \code{Caution} fired,
so it can read worse than the overall coverage class),
\code{cpm} (the embedded \code{\link[=cpm_fit]{cpm_fit()}} object, or \code{NULL} when
\code{structure = "observed"}), \code{population} (per profile row: the population
profile vectors, truth parameters, and any positive-semidefiniteness
repair magnitude, by condition), and \code{details}. The \code{plot()} method
draws coverage against the amplitude ladder with the Bradley band
shaded; \code{summary()} adds a plain-language verdict (see
\code{\link[=summary.circumplex_ci_accuracy]{summary.circumplex_ci_accuracy()}}).
}
\description{
Estimate, by simulation, whether the confidence intervals of a fitted
\code{\link[=ssm_analyze]{ssm_analyze()}} object would cover the true SSM parameters at their nominal
rate if the population looked like the fitted estimates, at the observed
sample size(s). Following Zimmermann & Wright (2017), the population's scale
intercorrelation structure is characterized by fitting Browne's (1992)
circular process model (\code{\link[=cpm_fit]{cpm_fit()}}) to the pooled within-group scale
correlations; each of \code{reps} datasets is then simulated from that plug-in
population at the object's exact group sizes and the object's own interval
procedure (same engine, same \code{boots}, same \code{interval}) is rerun on it.
Coverage is reported per profile row, parameter, and amplitude condition,
with 95\% Wilson score intervals classified against Bradley's (1978) liberal
robustness band at the as-estimated condition.
}
\details{
Displacement coverage is angular: the truth is inside the reported interval
as an arc (membership modulo 360 degrees), so populations peaking at the
0/360 boundary and contrast intervals reported beyond +/-180 degrees are
handled without special-casing. Because displacement is only interpreted
when the printed amplitude guardrail certifies it, displacement coverage is
also reported conditional on certification under the shipped decision rule
\code{round(a_lci, digits) > 0} (the rule the printed \code{\link[=ssm_analyze]{ssm_analyze()}} output
applies); the
implied certification threshold, \code{0.5 * 10^-digits} in amplitude units, is
echoed in the output because it is scale-dependent. A contrast row is a
signed difference, not a prototypicality measure, so
\code{print.circumplex_ssm()} never certification-gates it; its displacement
verdict and printed coverage are therefore reported unconditionally
(matching that profiles-only stance). Its certification-conditional
coverage -- where "certified" means both profile rows were certified -- is
still computed and retained in the returned object as a descriptive that no
display consumes.

The \code{amplitude_factors} ladder manufactures populations whose closed-form
amplitude is scaled toward zero (the regime where percentile amplitude
intervals are theoretically weakest) while keeping the residual profile
content fixed. The ladder is defined through the estimator functional (a
3x3 solve on the images of 1, cos, and sin), so the condition-\code{c} truths are
exact for any angle spacing: elevation is unchanged and the amplitude is
exactly \code{c} times the estimate. Truths are nevertheless recomputed from
each condition's population profile. At \code{c = 0} amplitude coverage is
structurally zero (a percentile interval of strictly positive amplitude
replicates cannot contain 0; such rows are flagged in the \code{Structural}
column) and displacement truth is undefined (reported \code{NA}); the guardrail
certification rate carries the inferential weight there, and the
informative rungs for amplitude coverage are the small \code{c > 0} ones.

When a profile row's amplitude estimate is itself below half its observed
CI width, the relative ladder degenerates: the analysis already sits in
the near-zero regime. One absolute rung is then added at the certification
margin (\code{c} chosen so \code{c} times the amplitude estimate equals the observed
amplitude-CI half-width, the largest such \code{c} across affected rows) and
\code{summary()} notes the regime. On the correlation path this rung is dropped
with a warning if it would push a population correlation to +/-1.
}
\section{Reproducibility}{

This function is stochastic: call \code{set.seed()} immediately before it.
It draws one \code{sample.int()} value from the caller's random number stream
to seed an internal L'Ecuyer-CMRG stream, gives every simulated dataset
its own deterministic substream, and then restores the caller's
\code{.Random.seed} and generator kind on exit, so results for a given seed
are identical regardless of \code{parallel}/\code{ncpus} and the caller's stream
is advanced by exactly that one draw.
}

\section{Limitations}{

Coverage is evaluated at the fitted structure, not the unknown truth
("would the procedure work in a population like your estimates", not
"did your interval cover"). Simulated populations are multivariate
normal with the fitted correlation structure; heavy tails or skew in the
real data can degrade coverage further than reported. The diagnostic
assesses the complete-data procedure (missing data are not simulated),
and groups are assumed to share one circumplex structure. When the
Browne model fits poorly, the simulated population may misrepresent the
data; the embedded fit and its diagnostics are returned for inspection.
}

\examples{
\donttest{
data("jz2017")
set.seed(12345)
res <- ssm_analyze(
  jz2017[1:200, ],
  scales = c("PA", "BC", "DE", "FG", "HI", "JK", "LM", "NO"),
  boots = 100
)
# Small reps/boots keep the example fast; use the defaults in practice
set.seed(23456)
acc <- ssm_ci_accuracy(res, reps = 25, amplitude_factors = c(1, 0.25))
acc
summary(acc)
}
}
\references{
Zimmermann, J., & Wright, A. G. C. (2017). Beyond description
in interpersonal construct validation: Methodological advances in the
circumplex Structural Summary Approach. \emph{Assessment, 24}(1), 3-23.

Browne, M. W. (1992). Circumplex models for correlation matrices.
\emph{Psychometrika, 57}(4), 469-497.

Bradley, J. V. (1978). Robustness? \emph{British Journal of Mathematical
and Statistical Psychology, 31}(2), 144-152.
}
\seealso{
Other ssm functions:
\code{\link[=plot.circumplex_ci_accuracy]{plot.circumplex_ci_accuracy()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}},
\code{\link[=ssm_table]{ssm_table()}}

Other analysis functions:
\code{\link[=cpm_fit]{cpm_fit()}},
\code{\link[=cpm_simulate]{cpm_simulate()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}}
}
\concept{analysis functions}
\concept{ssm functions}
