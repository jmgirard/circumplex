% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_plot.R
\name{theme_circumplex}
\alias{theme_circumplex}
\title{Circumplex canvas theme}
\usage{
theme_circumplex(base_size = 12)
}
\arguments{
\item{base_size}{A single positive number giving the base font size (in pt)
for the theme (default = 12).}
}
\value{
A \pkg{ggplot2} theme object, to be added to a plot with \code{+}.
}
\description{
The \pkg{ggplot2} theme applied to the circumplex canvas built by
\code{\link[=ggcircumplex]{ggcircumplex()}}. It is built on \code{\link[ggplot2:theme_minimal]{ggplot2::theme_minimal()}} so that the
amplitude rings, displacement spokes, and labels drawn by \code{\link[=coord_circumplex]{coord_circumplex()}}
are themed panel furniture that respond to further theming. Apply it to a
custom circumplex plot, and add \verb{+ theme_*()} or \code{+ theme()} on top to
restyle the canvas.
}
\examples{
# Restyle the canvas with a larger base font
ggcircumplex(octants()) + theme_circumplex(base_size = 16)
}
\seealso{
\code{\link[=ggcircumplex]{ggcircumplex()}}, \code{\link[=coord_circumplex]{coord_circumplex()}}
}
