% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_circumplex.R
\name{scale_x_circumplex}
\alias{scale_x_circumplex}
\title{Angle-labeled x-axis scale for circumplex plots}
\usage{
scale_x_circumplex(angles = octants(), labels = NULL, instrument = NULL, ...)
}
\arguments{
\item{angles}{Optional. A numeric vector of the angular position (in
degrees) of each circumplex scale (default = \code{octants()}). Ignored if
\code{instrument} is supplied.}

\item{labels}{Optional. Either \code{NULL} or a character vector of axis labels,
one per angle and in the same order (default = \code{NULL}, which labels each
break with its angle in degrees, or with the instrument's scale
abbreviations when \code{instrument} is supplied).}

\item{instrument}{Optional. Either \code{NULL} or a \code{circumplex_instrument}
object (see \code{instrument()}) from which to take the scale angles and (unless
\code{labels} is given) abbreviations (default = \code{NULL}).}

\item{...}{Additional arguments passed to
\code{\link[ggplot2:scale_x_continuous]{ggplot2::scale_x_continuous()}}, such as \code{name} or \code{limits}.}
}
\value{
A \pkg{ggplot2} scale object that can be added to a plot with \code{+}.
}
\description{
A \pkg{ggplot2} continuous position scale for the angle axis of a linear
circumplex plot, such as the score-by-angle curve drawn by
\code{ssm_plot_curve()}. It places axis breaks at the circumplex scale angles and
labels them, by default, with their angular position in degrees. Custom text
labels or a \code{circumplex_instrument} can be supplied instead, using the same
conventions as \code{\link[=ggcircumplex]{ggcircumplex()}}, so the linear axis and the circular canvas
label their scales consistently.
}
\examples{
# Degree-labeled angle axis
scale_x_circumplex(octants())

# Label the axis with an instrument's scale abbreviations
scale_x_circumplex(instrument = csip)
}
\seealso{
\code{\link[=ggcircumplex]{ggcircumplex()}}
}
