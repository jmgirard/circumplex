% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_structure_oop.R
\name{plot.circumplex_structure}
\alias{plot.circumplex_structure}
\title{Plot a circumplex-structure configuration}
\usage{
\method{plot}{circumplex_structure}(x, amax = 1, legend = TRUE, ...)
}
\arguments{
\item{x}{A \code{circumplex_structure} object from \code{\link[=fit_structure]{fit_structure()}}.}

\item{amax}{A single positive number giving the communality represented by
the canvas's outer ring (default = 1, the maximum possible communality).}

\item{legend}{A logical: draw a legend keying the colours to the scale names
(default = \code{TRUE}).}

\item{...}{Not used. Supplying an unrecognized argument produces a warning.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Draw the two-factor loading configuration of a \code{\link[=fit_structure]{fit_structure()}} object on
the circular canvas from \code{\link[=ggcircumplex]{ggcircumplex()}}. Each scale is placed at its
estimated angle (\code{atan2} of its two principal-axis loadings) and at a radius
given by its communality (the share of its variance on the first two
factors), so a clean circumplex shows the scales spread evenly around a ring
of similar radius, unequal axes show scales at differing radii (what the
Fisher Test measures), and simple structure shows scales bunched near a few
angles (what the Gap and interstitiality tests measure). The canvas spokes
mark the same estimated angles, labelled by scale.
}
\examples{
data("jz2017")
scales <- c("PA", "BC", "DE", "FG", "HI", "JK", "LM", "NO")
plot(fit_structure(jz2017, scales = scales))
}
\seealso{
\code{\link[=fit_structure]{fit_structure()}}, \code{\link[=ggcircumplex]{ggcircumplex()}}
}
