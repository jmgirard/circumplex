% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_sem.R
\name{ssm_sem_parameters}
\alias{ssm_sem_parameters}
\title{Calculate latent SSM parameters from a fitted lavaan measurement model}
\usage{
ssm_sem_parameters(
  fit,
  scales,
  angles = octants(),
  measures = NULL,
  ci_method = c("mvn", "boot"),
  boots = 2000,
  interval = 0.95,
  contrast = FALSE,
  parallel = "no",
  ncpus = 1
)
}
\arguments{
\item{fit}{Required. A fitted lavaan object whose model preserves the
\code{\link[=ssm_sem_syntax]{ssm_sem_syntax()}} parameter structure (factors \code{g}, \code{cx}, \code{cy}; the
measures covarying with them). For \code{ci_method = "mvn"}, fit the model
with robust (sandwich) standard errors (lavaan's
\code{se = "robust.huber.white"}, \code{\link[=ssm_sem]{ssm_sem()}}'s default) so the propagated
covariance stays valid when the fixed-angle model is an approximation;
see the \code{se} argument of \code{\link[=ssm_sem]{ssm_sem()}}.}

\item{scales}{Required. A character vector with the scale (indicator)
names, in the same order as \code{angles}.}

\item{angles}{Optional. A numeric vector of the scales' theoretical angles
in degrees (default = \code{octants()}). Must be the angles the model was
generated with.}

\item{measures}{Optional for multi-group fits, required otherwise. A
character vector of the measure names; \code{NULL} on a multi-group fit
selects the latent MEAN path (the fit must carry the mean structure:
scale intercepts and latent means).}

\item{ci_method, boots, interval, contrast, parallel, ncpus}{See \code{\link[=ssm_sem]{ssm_sem()}}.
Note that for a multi-group fit the CONTRAST DIRECTION (and the group
labels in the output) follows the fit's own group order -- lavaan's
default is order of appearance in the data unless \code{group.label} was
supplied at fitting time -- so read the direction from the output's
\code{Group} column, not from factor-level conventions.}
}
\value{
A \code{circumplex_ssm_sem} object; see \code{\link[=ssm_sem]{ssm_sem()}}.
}
\description{
The low-level adapter behind \code{\link[=ssm_sem]{ssm_sem()}}: take an already fitted
\pkg{lavaan} model of a fixed-angle circumplex measurement structure (as
generated by \code{\link[=ssm_sem_syntax]{ssm_sem_syntax()}}, possibly user-modified -- e.g., a
partial-invariance respecification) and compute latent SSM parameter
estimates with in-package confidence intervals. Compatibility with the
expected parameter structure is checked structurally (the named loading,
factor-covariance, and measure-covariance parameters must be present), not
by provenance.
}
\details{
\strong{Important:} multi-group fits are supported here as the partial-invariance
escape hatch, and this path \emph{bypasses} the invariance gating that
\code{\link[=ssm_sem]{ssm_sem()}} applies. Where \code{\link[=ssm_sem]{ssm_sem()}} fits a configural-metric-scalar
ladder and refuses a latent group contrast when the required rung is
rejected, \code{ssm_sem_parameters()} computes the contrast from whatever
multi-group fit you supply without testing invariance at all. You own the
comparability claim: the groups are compared on this instrument's latent
metric only to the extent the model you fitted makes them comparable.
}
\section{Reproducibility}{

This function consumes R's random number stream for both \code{ci_method}
settings (\code{"mvn"} through the package's own draws; \code{"boot"} through a
seed handed to lavaan's bootstrap). Call \code{set.seed()} immediately before
\code{ssm_sem()} for reproducible confidence intervals.
}

\examples{
\dontshow{if (requireNamespace("lavaan", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
data("jz2017")
scales <- c("PA", "BC", "DE", "FG", "HI", "JK", "LM", "NO")
syn <- ssm_sem_syntax(scales = scales, angles = octants(), measures = "NARPD")
# Robust (sandwich) SEs so the mvn engine propagates a
# misspecification-consistent covariance (ssm_sem()'s default)
fit <- lavaan::cfa(syn, data = jz2017, se = "robust.huber.white")
set.seed(12345)
ssm_sem_parameters(fit, scales = scales, measures = "NARPD", boots = 500)
}
\dontshow{\}) # examplesIf}
}
\seealso{
Other ssm functions:
\code{\link[=plot.circumplex_ci_accuracy]{plot.circumplex_ci_accuracy()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_analyze_long]{ssm_analyze_long()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_draws]{ssm_draws()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_table]{ssm_table()}},
\code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}}

Other analysis functions:
\code{\link[=cpm_fit]{cpm_fit()}},
\code{\link[=cpm_simulate]{cpm_simulate()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_analyze_long]{ssm_analyze_long()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_draws]{ssm_draws()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=summary.circumplex_ssm_id]{summary.circumplex_ssm_id()}}
}
\concept{analysis functions}
\concept{ssm functions}
