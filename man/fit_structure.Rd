% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_structure.R
\name{fit_structure}
\alias{fit_structure}
\title{Evaluate circumplex structure (Acton & Revelle, 2004)}
\usage{
fit_structure(
  data,
  scales,
  scoring = c("deviation", "raw"),
  ridge = 0,
  n_perm = NULL
)
}
\arguments{
\item{data}{A data frame (or matrix) containing the circumplex scales.}

\item{scales}{A character vector of column names (or a numeric vector of
column indexes) selecting the circumplex scales, \strong{in hypothesised circular
order} (the order is RANDALL's order hypothesis). At least four scales are
required.}

\item{scoring}{Either \code{"deviation"} (the default; row-mean-center the scales
before analysis) or \code{"raw"} (analyze the scores as given). Selects the
matching interpretive cutoffs.}

\item{ridge}{A non-negative ridge added to the diagonal of the correlation
matrix (then rescaled to a unit diagonal) to repair a non-positive-definite
matrix before factoring; default \code{0}, which matches the cutoff calibration.
Raise it only if factoring fails, noting that a nonzero ridge moves the
statistics off the calibrated scale.}

\item{n_perm}{\code{NULL} (the default) to compute RANDALL's p-value by exact
enumeration, available for up to nine scales; otherwise a single positive
whole number of Monte Carlo relabelings (required for ten or more scales).
The Monte Carlo path draws from the global RNG stream, so set a seed with
\code{\link[=set.seed]{set.seed()}} beforehand for reproducibility.}
}
\value{
An object of class \code{circumplex_structure} with \code{print()},
\code{\link[=summary]{summary()}}, and \code{\link[=plot]{plot()}} methods. Its components are \code{results} (a data
frame with one row per factor-analytic criterion: statistic, cutoffs, and
interpretive category), \code{randall} (the RANDALL index, p-value, and method),
\code{loadings} (the two unrotated principal-axis factors), and \code{details}.
}
\description{
Run the exploratory circumplex-structure criteria of Acton and Revelle
(2004) on a set of scales and return one object bundling all of the tests.
Four criteria are computed from the first two unrotated principal-axis
factors of the scales' correlation matrix -- the \strong{Fisher Test} of equal
axes, the \strong{Gap Test} of equal spacing, the \strong{Variance Test} (VT2) and
\strong{Rotation Test} of interstitiality -- and each statistic is classified
against simulation-derived, scoring- and scale-count-specific cutoffs. A
fifth test, \strong{RANDALL} (Hubert & Arabie, 1987; Tracey, 1997), evaluates the
hypothesised circular \emph{order} of the scales with a randomization test that
yields a genuine p-value.
}
\details{
The four factor-analytic criteria have the most power when there is no large
general factor, which \emph{deviation scoring} (centering each respondent on their
own mean across the scales, exactly what \code{\link[=ipsatize]{ipsatize()}} does) approximates by
removing it (Acton & Revelle, 2004, p. 9). Deviation scoring is therefore the
default and is applied to all five tests; pass \code{scoring = "raw"} to leave the
scores untouched. The two scorings carry different cutoffs, matched
automatically.

The interpretive cutoffs are \strong{heuristic likelihood classifications read off
simulated distributions, not significance tests}, and they are specific to
the number of scales. Only eight scales (the canonical octant instrument) are
calibrated; with any other count the statistics are still reported but no
interpretation is attached (see \code{\link[=print]{print()}}/\code{\link[=summary]{summary()}}). The cutoffs were
re-derived under Acton and Revelle's own generating model at eight scales;
see \code{vignette("evaluating-circumplex-structure")}. RANDALL needs no cutoffs:
with up to nine scales its null distribution is enumerated exactly, so its
p-value is available at any scale count of four or more.
}
\examples{
data("jz2017")
scales <- c("PA", "BC", "DE", "FG", "HI", "JK", "LM", "NO")
res <- fit_structure(jz2017, scales = scales)
res
summary(res)
}
\references{
Acton, G. S., & Revelle, W. (2004). Evaluation of ten psychometric criteria
for circumplex structure. \emph{Methods of Psychological Research Online},
9(1), 1-27.

Hubert, L., & Arabie, P. (1987). Evaluating order hypotheses within proximity
matrices. \emph{Psychological Bulletin}, 102(1), 172-178.

Tracey, T. J. G. (1997). RANDALL: A Microsoft FORTRAN program for a
randomization test of hypothesized order relations. \emph{Educational and
Psychological Measurement}, 57(1), 164-168.
}
\seealso{
\code{\link[=cpm_fit]{cpm_fit()}} for a confirmatory circumplex model; \code{\link[=ipsatize]{ipsatize()}} for
deviation scoring.
}
\concept{structure functions}
