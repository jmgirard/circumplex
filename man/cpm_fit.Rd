% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpm_fit.R
\name{cpm_fit}
\alias{cpm_fit}
\title{Fit Browne's circular stochastic process model (circumplex fit statistics)}
\usage{
cpm_fit(
  data = NULL,
  scales = NULL,
  angles = octants(),
  cormat = NULL,
  n = NULL,
  m = 3,
  model = c("quasi-circumplex", "constrained-angles", "equal-communality", "circulant"),
  reference = 1,
  interval = 0.95,
  ci_method = c("analytic", "bootstrap"),
  boots = 2000,
  listwise = TRUE
)
}
\arguments{
\item{data}{A data frame or matrix containing the circumplex scales (raw-data
path). Supply exactly one of \code{data} or \code{cormat}.}

\item{scales}{For the raw-data path, a character vector of column names (or a
numeric vector of column indexes) selecting the circumplex scales. For the
\code{cormat} path, optional labels for the variables (defaults to the matrix
dimnames, or \code{V1}, \code{V2}, ...).}

\item{angles}{A numeric vector of the theoretical angular displacement of
each scale, in degrees, used both as the reference/identifying angle and as
optimization start values (default = \code{\link[=octants]{octants()}}). Its length must match
the number of scales.}

\item{cormat}{A correlation matrix (the matrix-input path, CircE-style).
Supply exactly one of \code{data} or \code{cormat}. Must be symmetric with a unit
diagonal and positive definite.}

\item{n}{For the \code{cormat} path, the sample size (number of observations) the
correlation matrix was computed from. The test statistic uses \code{N - 1} (the
Wishart degrees of freedom); pass the raw sample size here.}

\item{m}{The number of harmonics in the correlation function (default = 3,
the octant-scale convention). Capped at \code{floor((p - 1) / 2)} for the
free-angle variants and \code{floor(p / 2)} for the fixed-angle variants.}

\item{model}{The model variant (design of Browne 1992): \code{"quasi-circumplex"}
(default; free angles and communalities), \code{"constrained-angles"} (angles
fixed at their theoretical values), \code{"equal-communality"} (a single shared
communality), or \code{"circulant"} (both constraints).}

\item{reference}{The index into \code{scales} of the variable whose angle is fixed
at its theoretical value to identify the rotation (default = 1).}

\item{interval}{The confidence level for the parameter intervals (default =
0.95). The RMSEA interval is always the conventional 90 percent.}

\item{ci_method}{How to construct confidence intervals: \code{"analytic"}
(default) uses Wald intervals from the information matrix. \code{"bootstrap"}
(raw-data path) is added in a later release. On the \code{cormat} path only
\code{"analytic"} is available (there is no raw data to resample).}

\item{boots}{The number of bootstrap resamples (reserved for the bootstrap
method; default = 2000).}

\item{listwise}{Whether to handle missing values by listwise deletion. Only
listwise deletion is supported in this release (default = TRUE).}
}
\value{
A \code{circumplex_cpm} object: a list with \code{results} (a data frame of
estimated angles and communality indices with confidence intervals),
\code{betas} (the correlation-function weights), \code{fit} (the fit indices),
\code{corfun} (the estimated correlation function), \code{matrices} (the sample and
model-implied matrices and residuals), and \code{details} (model, diagnostics,
and settings). See \code{\link[=print.circumplex_cpm]{print.circumplex_cpm()}} and \code{\link[=summary.circumplex_cpm]{summary.circumplex_cpm()}}.
}
\description{
Estimate Browne's (1992) circular stochastic process model (CPM) for the
correlational structure of a set of circumplex scales or items, the native
replacement for the archived CircE package. Each variable is modeled as a
point on a circle at an estimated angle, with a communality index and a
shared correlation function; the fit of that structure is summarized with the
usual covariance-structure indices (chi-square, RMSEA, SRMR, CFI, TLI).
}
\section{Confidence intervals}{

Analytic (Wald) intervals are asymptotically valid but can materially
mis-cover at field-typical sample sizes; \code{summary()} prints a caution below
\code{n = 2000}. Prefer the bootstrap on the raw-data path when available.
}

\examples{
# Raw-data path on the eight IIP-SC octant scales
data("jz2017")
scales <- c("PA", "BC", "DE", "FG", "HI", "JK", "LM", "NO")
fit <- cpm_fit(jz2017, scales = scales)
fit

# Matrix-input path (supply the sample size)
R <- cor(jz2017[scales])
cpm_fit(cormat = R, scales = scales, n = nrow(jz2017))

}
\references{
Browne, M. W. (1992). Circumplex models for correlation matrices.
\emph{Psychometrika, 57}(4), 469-497.
}
\seealso{
Other analysis functions:
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_score]{ssm_score()}}
}
\concept{analysis functions}
