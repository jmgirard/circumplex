% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_parameters_id.R
\name{summary.circumplex_ssm_id}
\alias{summary.circumplex_ssm_id}
\title{Summarize per-person SSM parameters at the group level}
\usage{
\method{summary}{circumplex_ssm_id}(object, ...)
}
\arguments{
\item{object}{Required. An object of class \code{"circumplex_ssm_id"} created
by \code{\link[=ssm_parameters_id]{ssm_parameters_id()}}.}

\item{...}{Ignored (S3 consistency).}
}
\value{
A one-row data frame with columns \code{n} (persons), \code{n_na_d}
(persons with undefined displacement, excluded from the circular
summaries), \code{e_mean}, \code{x_mean}, \code{y_mean}, \code{a_mean} (arithmetic means),
\code{d_mean} (circular mean of displacement, degrees in [0, 360], the
0/360 pole reported as 360), and
\code{d_res} (mean resultant length in [0, 1]; \code{NA} when no displacement
is defined, and undefined direction at zero resultant reports
\code{d_mean = NA}).
}
\description{
Aggregate a per-person SSM parameter table (from \code{\link[=ssm_parameters_id]{ssm_parameters_id()}})
into group-level summaries, using circular statistics for displacement:
arithmetic means are meaningless for angles, so displacement is summarized
by its circular mean (the direction of the summed unit vectors) and the
mean resultant length (a 0 to 1 measure of directional concentration).
}
\details{
Persons with undefined (\code{NA}) displacement are stripped before the
circular aggregation -- \code{n_na_d} reports how many -- while the arithmetic
means of the other parameters use all persons with defined values. Two
aggregation caveats apply. (1) The circular mean of per-person
displacements weights every person's direction equally; it is a
\emph{different quantity} from the displacement of the group mean profile
(e.g., from \code{\link[=ssm_analyze]{ssm_analyze()}}), which weights persons by amplitude -- on
heterogeneous samples the two can differ substantially. (2) By the
triangle inequality, the amplitude of the group mean profile is at most
the mean per-person amplitude (\code{a_mean}), strictly smaller when
directions disperse; relatedly, the mean resultant length \code{d_res} falls
below 1 as directions disperse.
}
\examples{
data("aw2009")
res <- ssm_parameters_id(
  aw2009,
  scales = c("PA", "BC", "DE", "FG", "HI", "JK", "LM", "NO")
)
summary(res)

}
\seealso{
Other ssm functions:
\code{\link[=plot.circumplex_ci_accuracy]{plot.circumplex_ci_accuracy()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_analyze_long]{ssm_analyze_long()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_draws]{ssm_draws()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}},
\code{\link[=ssm_table]{ssm_table()}}

Other analysis functions:
\code{\link[=cpm_fit]{cpm_fit()}},
\code{\link[=cpm_simulate]{cpm_simulate()}},
\code{\link[=ssm_analyze]{ssm_analyze()}},
\code{\link[=ssm_analyze_long]{ssm_analyze_long()}},
\code{\link[=ssm_ci_accuracy]{ssm_ci_accuracy()}},
\code{\link[=ssm_draws]{ssm_draws()}},
\code{\link[=ssm_parameters]{ssm_parameters()}},
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}},
\code{\link[=ssm_score]{ssm_score()}},
\code{\link[=ssm_sem]{ssm_sem()}},
\code{\link[=ssm_sem_parameters]{ssm_sem_parameters()}}
}
\concept{analysis functions}
\concept{ssm functions}
