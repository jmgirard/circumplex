% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_sem_syntax.R
\name{ssm_sem_syntax}
\alias{ssm_sem_syntax}
\title{Generate lavaan syntax for a fixed-angle circumplex measurement model}
\usage{
ssm_sem_syntax(
  instrument = NULL,
  scales = NULL,
  angles = NULL,
  measures = NULL,
  model = c("scaled", "strict"),
  n_groups = 1,
  invariance = c("configural", "metric", "scalar", "strict_residuals"),
  include_defined = NULL
)
}
\arguments{
\item{instrument}{Optional. A \code{circumplex_instrument} object; its scale
abbreviations and angles are used. Supply this or both \code{scales} and
\code{angles}.}

\item{scales}{Optional. A character vector of circumplex scale (column)
names. Ignored when \code{instrument} is supplied.}

\item{angles}{Optional. A numeric vector of scale angles in degrees, the same
length as \code{scales}. Ignored when \code{instrument} is supplied.}

\item{measures}{Optional. Either \code{NULL} or a character vector of external
measure (column) names to relate to the circumplex factors.}

\item{model}{Optional. The measurement-model tier: \code{"scaled"} (default) or
\code{"strict"}. See Details.}

\item{n_groups}{Optional. A single positive whole number: the number of
groups for a multi-group model. \code{1} (default) emits the single-group
model. When \code{n_groups >= 2} the generator emits multi-group syntax with a
mean structure and the invariance constraints selected by \code{invariance};
fitting it requires \code{\link[lavaan:cfa]{lavaan::cfa()}} called with \verb{group = } a grouping
variable whose number of levels equals \code{n_groups}. Group ordering follows
the factor-level order of that variable: the \strong{first} level is the
reference group, whose factor metric (and, from the scalar rung, whose
latent means) is fixed.}

\item{invariance}{Optional. The cross-group invariance rung to emit when
\code{n_groups >= 2}: \code{"configural"} (default), \code{"metric"}, \code{"scalar"}, or
\code{"strict_residuals"}. Must be left at its default when \code{n_groups == 1}
(supplying it there is an error). Under the \code{"strict"} tier the \code{"metric"}
rung is vacuous (all loadings are fixed) and emits the configural
structure with an explanatory comment. See the package design notes
(section 6.2) for the adapted fixed-angle invariance ladder.}

\item{include_defined}{Optional. (Single-group models only: for
\code{n_groups >= 2} the lines are unavailable and an explicit \code{TRUE} is
ignored with a warning.) Whether to append inspection-only \verb{:=}
definitions of the covariance-metric \verb{(e, x, y)} coordinates for each
measure. \code{NULL} (default) emits them automatically under the \code{"strict"}
tier when at least one measure is present (there they are linear), and
omits them otherwise. \code{TRUE} forces emission (an error under \code{"scaled"},
where they would be nonlinear); \code{FALSE} always suppresses them.}
}
\value{
A single character string of \pkg{lavaan} model syntax, with
attributes \code{angles} (a \code{circumplex_degree} vector), \code{scales}, \code{model},
\code{weights} (the 3-by-p OLS projection matrix, rows \code{e}, \code{x}, \code{y}),
\code{n_groups}, and (when \code{n_groups >= 2}) \code{invariance}.
}
\description{
Emit \pkg{lavaan} model syntax for a structural-equation-model formulation of
the Structural Summary Method, with the circumplex scale angles held fixed at
their theoretical values. This is the syntax-generation layer of the
SEM-based SSM (see the package's design notes); it is a pure function of its
arguments and does \strong{not} require \pkg{lavaan} to be installed (only fitting
the emitted model does).
}
\details{
Two model tiers are available. The \code{"scaled"} tier frees a general saturation
and a circumplex saturation per scale while fixing each scale's angle; the
circumplex plane is held isotropic and orthogonal, and the general factor is
held orthogonal to the plane (freeing the general-plane covariances
alongside free saturations makes the model locally unidentified exactly at
zero covariance, so they cannot be estimated in this tier). The \code{"strict"}
tier fixes every loading to the unit cosine pattern
\verb{(1, cos(angle), sin(angle))} and frees the 3x3 factor covariance matrix --
including the general-plane covariances, making it the tier that can model
a general factor leaning into the plane. The angles enter only as evaluated
cosine and sine constants; no angle is ever a free parameter.

The returned string always carries a \code{weights} attribute: the
ordinary-least-squares projection matrix that maps a profile vector to the
structural summary coordinates \verb{(e, x, y)}. For equally spaced angles this
equals the conventional closed-form estimator; for unequally spaced angles it
is the least-squares projection and can differ. The emitted syntax never
contains \verb{:=} definitions for amplitude or displacement: those are nonlinear
functions whose confidence intervals must be constructed in-package, not by
\pkg{lavaan}'s delta method (which ignores the angular branch cut).
}
\examples{
# Octant instrument, default scaled tier
syn <- ssm_sem_syntax(scales = paste0("s", 1:8), angles = octants())
attr(syn, "weights")
cat(syn)
}
\seealso{
\code{\link[=ssm_analyze]{ssm_analyze()}} for the observed-data SSM.
}
