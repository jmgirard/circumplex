% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_growth_formula.R
\name{ssm_growth_formula}
\alias{ssm_growth_formula}
\alias{print.circumplex_growth_formula}
\title{The joint growth model on SSM coordinates as an engine's formulas}
\usage{
ssm_growth_formula(
  engine = c("glmmTMB", "nlme", "brms"),
  time = "wave",
  id = "person"
)

\method{print}{circumplex_growth_formula}(x, ...)
}
\arguments{
\item{engine}{Optional. One of \code{"glmmTMB"} (default), \code{"nlme"} or
\code{"brms"}. The engine is named only; it is not loaded or called.}

\item{time}{Optional. The name of the time column in the long table
(default \code{"wave"}), the same \code{time} given to \code{\link[=ssm_growth_data]{ssm_growth_data()}}.}

\item{id}{Optional. The name of the person column in the long table
(default \code{"person"}), the same \code{id} given to \code{\link[=ssm_growth_data]{ssm_growth_data()}}.}

\item{x}{An object of class \code{"circumplex_growth_formula"}.}

\item{...}{Ignored (S3 consistency).}
}
\value{
A list of class \code{"circumplex_growth_formula"} with attribute
\code{engine}. Its elements are formula objects, named as the engine's fit
function names its arguments. For \code{"glmmTMB"}: \code{formula},
\verb{value ~ 0 + dv + dv:<time> + us(0 + dv | <id>)}, and \code{dispformula},
\code{~ 0 + dv}. For \code{"nlme"}: \code{fixed}, \verb{value ~ 0 + dv + dv:<time>},
\code{random}, \verb{~ 0 + dv | <id>}, and \code{weights}, \code{~ 1 | dv}, the form that
\code{nlme::varIdent()} takes. For \code{"brms"}: \code{formula},
\verb{value ~ 0 + dv + dv:<time> + (0 + dv | <id>)}, and \code{sigma},
\code{sigma ~ 0 + dv}, the two parts of a \code{brms::bf()} call.
}
\description{
Return the package's fixed joint growth model in the formula pieces one
mixed-model engine takes. The model reads the long table
\code{\link[=ssm_growth_data]{ssm_growth_data()}} builds: one intercept and one linear time slope per
coordinate (\code{e}, \code{x}, \code{y}), a person-level random intercept per
coordinate with the three intercepts free to correlate, and a separate
residual variance per coordinate. Printing the object shows the complete
fit call for the engine and the line that extracts what
\code{ssm_trajectory()} takes from the fit.
}
\details{
The model has no options. Adding a covariate, a quadratic time term or
another random-effects structure changes the model whose interval
coverage the package validated, so the pieces are built for the user to
paste and, if they choose, to edit by hand with that in mind.
}
\examples{
ssm_growth_formula("glmmTMB", time = "wave", id = "person")
ssm_growth_formula("nlme")
ssm_growth_formula("brms")
}
\seealso{
Other growth functions:
\code{\link[=ssm_growth_data]{ssm_growth_data()}}
}
\concept{growth functions}
