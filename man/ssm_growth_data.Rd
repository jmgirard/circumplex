% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_growth_data.R
\name{ssm_growth_data}
\alias{ssm_growth_data}
\title{Build the long table for a growth model on SSM coordinates}
\usage{
ssm_growth_data(data, scales, angles = octants(), id, time)
}
\arguments{
\item{data}{Required. A data frame or matrix with one row per person per
time point, containing the circumplex scales, an id column and a time
column.}

\item{scales}{Required. The variable names or column numbers for the
variables in \code{data} that contain circumplex scales.}

\item{angles}{Optional. A numeric vector containing the angular
displacement of each circumplex scale included in \code{scales}, in degrees
(default = \code{octants()}).}

\item{id}{Required. The name of the column in \code{data} identifying persons.
A column name, not a number. Missing values are an error.}

\item{time}{Required. The name of the numeric column in \code{data} holding
each row's time point. A column name, not a number. A \code{Date}, factor or
character column is an error, as are missing values: the growth model
fits time as a number, so the caller chooses its origin and unit.}
}
\value{
A data frame with \code{3 * nrow(data)} rows and four columns, named
\verb{<id>} (a factor), \verb{<time>} (numeric), \code{dv} (a factor with levels \code{e},
\code{x} and \code{y}) and \code{value}. Rows are ordered by input row, then by \code{dv}.
Each \code{value} is the \code{Elev}, \code{Xval} or \code{Yval} that
\code{\link[=ssm_parameters_id]{ssm_parameters_id()}} with \code{id = NULL} gives that input row.
}
\description{
Score every row of \code{data} as its own circumplex profile and stack the
three SSM coordinates, elevation \code{e}, \code{x} and \code{y}, into the long table that
the package's growth recipe fits. One input row gives three output rows,
one per coordinate, and the output holds each row's id and time value
beside the coordinate's name and value. \code{\link[=ssm_growth_formula]{ssm_growth_formula()}} gives the
fit call that takes this table.
}
\details{
Growth in displacement is modeled through \code{x} and \code{y}, never through the
angle itself, so the table carries no amplitude or displacement. A flat
profile has \code{x = y = 0} and a defined \code{e}, and a profile with a scale
entirely missing has \code{NA} on all three coordinates. Both keep their rows.
}
\examples{
data("simulated_growth")
long <- ssm_growth_data(
  simulated_growth,
  scales = PANO(),
  id = "person",
  time = "wave"
)
head(long, 6)
}
\seealso{
Other growth functions:
\code{\link[=ssm_growth_formula]{ssm_growth_formula()}}
}
\concept{growth functions}
