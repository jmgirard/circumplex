% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_trajectory_helper.R
\name{ssm_trajectory}
\alias{ssm_trajectory}
\alias{print.circumplex_ssm_trajectory}
\alias{rbind.circumplex_ssm_trajectory}
\title{Trajectory of SSM parameters from a fitted growth model}
\usage{
ssm_trajectory(
  coef,
  vcov,
  times,
  draws = NULL,
  time = "wave",
  interval = 0.95,
  n_draws = 4000,
  contrast = NULL
)

\method{print}{circumplex_ssm_trajectory}(x, digits = 2, ...)

\method{rbind}{circumplex_ssm_trajectory}(...)
}
\arguments{
\item{coef}{The fixed effects: a named numeric vector. Required with
\code{vcov}, and absent with \code{draws}.}

\item{vcov}{The covariance matrix of \code{coef}: a symmetric numeric matrix
with one row and column per element of \code{coef}, its dimnames equal to
\code{names(coef)} when present. Required with \code{coef}, and absent with
\code{draws}.}

\item{times}{Required. A numeric vector of the time values at which to
evaluate the trajectory, one output row each.}

\item{draws}{Optional. A numeric matrix of coefficient draws, one row per
draw and one named column per coefficient, in place of \code{coef} and
\code{vcov}. When given, \code{n_draws} is ignored.}

\item{time}{Optional. The name of the time column in the output, and the
suffix of the slope coefficients (default \code{"wave"}), the same \code{time}
given to \code{\link[=ssm_growth_data]{ssm_growth_data()}} and \code{\link[=ssm_growth_formula]{ssm_growth_formula()}}.}

\item{interval}{Optional. A single number between 0 and 1 giving the
level of the intervals (default = 0.95).}

\item{n_draws}{Optional. The number of coefficient vectors to draw from
\code{coef} and \code{vcov} (default = 4000).}

\item{contrast}{Optional. \code{NULL} (default) for the default contrast, or a
function of one time value returning a 3 by \code{p} numeric matrix whose
rows are \code{e}, \code{x} and \code{y} and whose columns follow the order of \code{coef}
or of the columns of \code{draws}.}

\item{x}{An object of class \code{"circumplex_ssm_trajectory"}.}

\item{digits}{The number of decimal places to print (default = 2).}

\item{...}{For \code{rbind()}, trajectory tables to stack.}
}
\value{
A data frame of class \code{"circumplex_ssm_trajectory"} with
attribute \code{time} naming its time column and attribute \code{input} recording
the input shape, \code{"coef_vcov"} or \code{"draws"}, one row per value of
\code{times}.
Its columns are \verb{<time>}; \code{e_est}, \code{e_lci}, \code{e_uci}, and the same three
for \code{x}, \code{y}, \code{a} and \code{d}; and \code{certified}. The estimates and bounds
are those \code{\link[=ssm_draws]{ssm_draws()}} reports: medians and equal-tailed interval
bounds for \code{e}, \code{x}, \code{y} and \code{a}, and the circular mean with circular
quantile bounds in degrees for \code{d}. A \code{d} interval that straddles 0/360
degrees has \code{d_lci > d_uci}. \code{certified} is the displacement
certification at that time, and at an uncertified time the \code{d} interval
is not interpretable. Printing shows the table rounded, marks each
uncertified row, and ends with the caution for the input shape: the
small-sample caution under \code{coef} and \code{vcov}, and under \code{draws} that the
intervals summarize the draws as given. A subset that drops the
attribute, or an \code{rbind()} of tables of different shapes, prints a
caution that says the shape is not recorded.
\code{\link[=ssm_plot_trajectory]{ssm_plot_trajectory()}} plots the object with no \code{time} argument.
}
\description{
Turn the fixed effects of the joint growth model that
\code{\link[=ssm_growth_formula]{ssm_growth_formula()}} builds into a table of Structural Summary Method
parameters at each time point, with intervals and the displacement
certification. Two input shapes are accepted. The frequentist shape is
\code{coef} and \code{vcov}, the fixed effects and their covariance from a glmmTMB
or nlme fit. The function draws \code{n_draws} coefficient vectors from the
multivariate normal distribution they define, the same large-sample step
the package's Monte Carlo method takes. The Bayesian shape is \code{draws}, a
matrix of posterior coefficient draws such as \code{as.matrix(fit)} from brms,
used as given. Under either shape, each draw's \code{e}, \code{x} and \code{y} at time
\code{t} are its intercept plus \code{t} times its slope, and the draws at each
time go through \code{\link[=ssm_draws]{ssm_draws()}} with \code{type = "parameters"}.
}
\details{
The default contrast reads six coefficient names: \code{dve}, \code{dvx}, \code{dvy} and
\verb{dve:<time>}, \verb{dvx:<time>}, \verb{dvy:<time>}, where \verb{<time>} is the \code{time}
argument. Those are the names the model from \code{\link[=ssm_growth_formula]{ssm_growth_formula()}} gives
its fixed effects on the long table \code{\link[=ssm_growth_data]{ssm_growth_data()}} builds. A \code{b_}
prefix on the column names of \code{draws} is dropped, and other columns, such
as brms's \code{sd_}, \code{cor_} and \code{lp__} columns, are ignored. A missing name
is an error. A \code{contrast} function replaces the default for a model with
other terms, such as a quadratic time term; it must return, for one time
value, the 3 by \code{p} matrix that maps the \code{p} coefficients to \code{e}, \code{x} and
\code{y} at that time, in coefficient order.

The displacement interval at a time point is wrong when \code{x(t)} and \code{y(t)}
are treated as independent, which is what fitting the coordinates in
separate models does. So the \code{coef} and \code{vcov} shape refuses a \code{vcov}
whose implied covariance between \code{x(t)} and \code{y(t)} is exactly zero at
every time in \code{times}, unless \code{vcov} is zero everywhere. The check reads
only the given \code{times}, so with a single time it sees only the covariance
terms that time reaches. A \code{draws} matrix is not checked for a joint fit.
The intervals from a REML fit's \code{coef} and \code{vcov} condition on its
estimated variance components and are too narrow at small samples; the
"Growth Models on SSM Parameters" vignette, Section 7, states what the
package's coverage oracle measured and that no shipped correction exists.
Intervals from \code{draws} summarize those draws as given.
}
\examples{
# Fixed effects and covariance in the shape a joint fit returns
coef <- c(
  dve = 0.52, dvx = 0.61, dvy = -0.10,
  "dve:wave" = 0.00, "dvx:wave" = 0.00, "dvy:wave" = 0.065
)
vcov <- diag(c(7e-4, 3e-4, 3e-4, 5e-5, 2e-5, 2e-5))
vcov[2, 3] <- vcov[3, 2] <- 4e-5
dimnames(vcov) <- list(names(coef), names(coef))
set.seed(1)
trajectory <- ssm_trajectory(coef, vcov, times = 0:4)
trajectory
ssm_plot_trajectory(trajectory)

# The same from a matrix of coefficient draws, here drawn by hand; a brms
# fit gives one as `as.matrix(fit)`
set.seed(1)
draws <- matrix(rnorm(500 * 6), nrow = 500) \%*\% chol(vcov)
draws <- sweep(draws, 2, coef, "+")
colnames(draws) <- names(coef)
ssm_trajectory(times = 0:4, draws = draws)
}
\seealso{
Other growth functions:
\code{\link[=ssm_growth_data]{ssm_growth_data()}},
\code{\link[=ssm_growth_formula]{ssm_growth_formula()}}
}
\concept{growth functions}
