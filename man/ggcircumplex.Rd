% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_plot.R
\name{ggcircumplex}
\alias{ggcircumplex}
\title{Create a circumplex plotting canvas}
\usage{
ggcircumplex(
  angles = octants(),
  labels = NULL,
  amax = 0.5,
  font_size = 12,
  instrument = NULL,
  grid = c("polar", "cartesian"),
  angle_labels = FALSE
)
}
\arguments{
\item{angles}{Optional. A numeric vector of the angular position (in
degrees) of each circumplex scale, going counterclockwise from the right
(default = \code{octants()}). Ignored if \code{instrument} is supplied.}

\item{labels}{Optional. Either \code{NULL} or a character vector of text labels
to draw around the circle, one per angle and in the same order (default =
\code{NULL}, which draws the numeric angles). If \code{instrument} is supplied,
\code{NULL} uses the instrument's scale abbreviations.}

\item{amax}{Optional. A single positive number giving the amplitude at the
outer ring, which sets the amplitude-axis labels; the center of the circle
is fixed at amplitude 0 (default = 0.5).}

\item{font_size}{Optional. A single positive number giving the size (in pt)
of the scale and amplitude labels (default = 12).}

\item{instrument}{Optional. Either \code{NULL} or a \code{circumplex_instrument}
object (see \code{instrument()}). When supplied, the scale \code{angles} and (unless
\code{labels} is given) the scale abbreviations are taken from the instrument
(default = \code{NULL}).}

\item{grid}{Optional. A single string naming the canvas furniture, passed to
\code{\link[=coord_circumplex]{coord_circumplex()}}. \code{"polar"} (the default) draws amplitude rings,
displacement spokes and the amplitude axis. \code{"cartesian"} draws one rim
ring, a crosshair with a tick mark and a signed label at each amplitude
break between the center and the rim on every half-axis, and tick marks
outward from the rim at the scale angles (the canvas of Nagy et al.,
2019). The labels on the 180 and 270 halves are negative Cartesian
coordinates, not negative amplitudes.}

\item{angle_labels}{Optional. A single logical, independent of \code{grid}.
\code{TRUE} appends each scale's angle to its label, in parentheses with the
degree sign, the angle rounded to the nearest whole degree and the 0/360
pole written as 360, and turns each label to read along its radius
(default = \code{FALSE}). It applies to text labels, from \code{labels} or an
instrument; the default degree labels already show the angle and are left
as they are. Each side of the plot margin is widened to fit the labels
that point toward it: a label reaches past a side by its length,
estimated at half of \code{font_size} per character, times the share of its
direction that points toward that side. A side that no label points
toward keeps the theme's margin, so the circle can sit off-centre on the
page. Add \code{+ theme(plot.margin = ...)} to set the margin yourself.}
}
\value{
A \pkg{ggplot2} object containing the empty circumplex canvas.
}
\description{
Build an empty circular canvas -- the amplitude rings, displacement spokes,
and scale labels that circumplex figures are drawn on -- as a \pkg{ggplot2}
object. Additional layers (points, arcs, annotations) can be added to it
with \code{+}, so it serves as the reusable foundation for custom circumplex
visualizations. The package's own \code{ssm_plot_circle()} draws on the same
canvas.
}
\examples{
# A default octant canvas
ggcircumplex()

# Label the scales with their circumplex pole abbreviations
ggcircumplex(octants(), labels = PANO())

# Derive the angles and labels from a circumplex instrument
ggcircumplex(instrument = csip)

# A Cartesian grid: one rim ring and a labelled crosshair
ggcircumplex(octants(), labels = PANO(), grid = "cartesian")

# Each scale labelled with its angle, read along the radius
ggcircumplex(octants(), labels = PANO(), grid = "cartesian", angle_labels = TRUE)
}
\seealso{
\code{\link[=coord_circumplex]{coord_circumplex()}}, which owns the transform this canvas is built
on; \code{\link[=ssm_plot_circle]{ssm_plot_circle()}}, which draws SSM results on this canvas.

Other circumplex layers:
\code{\link[=coord_circumplex]{coord_circumplex()}},
\code{\link[=geom_ssm_arc]{geom_ssm_arc()}},
\code{\link[=geom_ssm_ellipse]{geom_ssm_ellipse()}},
\code{\link[=geom_ssm_path]{geom_ssm_path()}},
\code{\link[=geom_ssm_point]{geom_ssm_point()}},
\code{\link[=scale_x_circumplex]{scale_x_circumplex()}},
\code{\link[=theme_circumplex]{theme_circumplex()}}
}
\concept{circumplex layers}
